# Четвёртая ночь — утренний отчёт

Ветка `night/play-findings` от `main` (4c973b9, после merge PR #51), один PR.
Семнадцать находок живой игры, девять стадий. Отчёт пишется по ходу.

## 1. Итог одной строкой

**Девять стадий из девяти, ни одного отката, один fix-коммит из десяти
разрешённых.** Диагностика нашла три РАЗНЫЕ болезни там, где выглядело как
одна: блок жив, но экран врал; улучшений за золото не было написано вовсе;
цепочка наград храма была цела, а конвейер тика её не вызывал. Из
незапланированного: перенос дохода на находки сломал три измерительных
прибора, и все три оказались сломаны об изменение, а не изначально. Одно
задание не выполнено так, как задумывалось: `TTK_DRIFT_MAX` сузить нечем —
замер показал, что дрейф, ради которого сужали, живёт в другом механизме
(раздел 5).

## 2. Диагностика трёх молчаний (стадия 1)

Три вещи выглядели как «не работает». Оказались тремя разными болезнями, и
починка у каждой своя.

### 2.1. Блок: механика жива, прибавка есть, ВРЁТ ОТОБРАЖЕНИЕ

**Механика блока работает по-настоящему.** В тике стоит настоящий бросок
(`rollBlock`, `combat.ts:148`): если бросок ниже `blockChance`, из удара
вычитается `blockValue`. Модель знает о нём той же формулой
(`expectedMonsterDamage`, `combat.ts:203`), блок кормит ресурс через талант
(`blockResourceShare`) и отражает урон через капстоун (`reflectPerSecond`).
Стат не мёртвый ни в одной из трёх точек.

**Сила блока входит в дело и растёт от предмета.** `loot.ts:107` умножает
`blockValue` шаблона на силу предмета, то есть щит 80 уровня блокирует
заметно больше щита 20-го.

**Прибавка к шансу блока у щита ЕСТЬ: 25 %.** В `data/items.ts:117` у шаблона
щита `blockChance: new Decimal(0.25)`.

**Ломается ровно на экране.** Щит отдаёт `blockChance` модификатором вида
`kind: 'base'` (он ЗАМЕНЯЕТ базу «без щита не блокируется ничего», а не
прибавляет). А в `ui/ItemMods.svelte` ветка для `base` идёт мимо процентного
форматирования:

```js
if (mod.kind === 'base') {
  const v = mod.stat === 'weaponSpeed' ? `${mod.value.toFixed(2)}с` : formatNumber(mod.value)
  return `${name}: ${v}`
}
```

`formatNumber(0.25)` — это `d.lt(1000) → d.toFixed(0)` — печатает «0».
Игрок читает «шанс блока: 0» и делает единственно возможный вывод: блока нет.
Соседняя строка «сила блока: 12» при этом верна, потому что она целая.

Проверка формата в списке статов (`ui/statFormat.ts`) устроена правильно —
`blockChance` там в `PERCENT_STATS` и печатается как «25%». То есть в игре
ДВА форматтера, и разъехались они именно на этом.

**Вывод для стадии 2:** чинить надо не стат и не данные, а ветку `base` в
`ItemMods.svelte`. Прятать строку не нужно — она не нулевая, она неверно
напечатана.

### 2.2. Улучшения за золото: НЕ РЕАЛИЗОВАНЫ ВОВСЕ

Не «спрятаны» и не «заперты уровнем» — их нет в коде. Поиск по всему
`src/lib` не находит ни одного совпадения: `buyUpgrade`, `purchaseUpgrade`,
`goldUpgrade`, `autoSell`, `autoDust`, второй специализации.
`INVENTORY_SIZE = 24` — жёсткая константа `data/balance.ts:733`, которую
пять мест читают напрямую; расширять её нечем.

Подтверждение нашлось и в списке задач проекта: «Шаг 8: лестница покупок»
до сих пор помечен незакрытым, с пометкой «ТРЕБУЕТ PR 5 аудита». Задача
была отложена и не сделана — это единственная механика документа «Кривая,
зоны и золото», которая так и не появилась.

**Вывод для стадии 5:** делать с нуля, а не чинить видимость.

### 2.3. Награды храма: ЦЕПОЧКА ЦЕЛА, МОЛЧИТ ЭКРАН

Воспроизвёл ровно случай игрока — забег до 14 волны со смертью на 14-й:

```
ВХОД: canEnter=true, рекорд=0, следующая награда {этаж 1, 12 пыли, 2500 золота}
ПЕРЕД СМЕРТЬЮ: волна 14, пройдено 13
ИТОГ: оплачено 1..13, пыль 505, золото 105 540
ПОСЛЕ ВЫХОДА: золото 0 → 105 540, пыль 0 → 505, рекорд 0 → 13
```

Всё по замыслу: платят этажи выше рекорда, смерть засчитывается, этаж, на
котором добили, не оплачивается. Ни один шаг цепочки не рвётся —
`clearTempleWave` отмечает этаж по смерти бойца (`tick.ts:886`),
`finishTempleRun` платит на выходе, смерть героя идёт через
`leaveTemple(..., credit=true)` (`tick.ts:86`), добровольный выход — тем же
путём из стора.

**Тогда почему игрок ничего не увидел.** Три причины, и все три про экран,
а не про логику:

1. **Награды выдаются ТОЛЬКО НА ВЫХОДЕ, а HUD об этом молчит.** На четырнадцатом
   этаже у игрока уже заработано 505 пыли и 105 тысяч золота, но на экране —
   только «этаж 14 из 20» и «лучшее: 0». Накопленного не видно нигде. Пока
   не выйдешь, кажется, что не платят вовсе.
2. **Закрытая вкладка отдаёт всё.** Брошенный забег не засчитывается — это
   осознанное правило (`resumeOutside`), но предупреждение о нём показывается
   один раз на входе, а не там, где игрок принимает решение.
3. **HUD прямо говорит неправду.** Подпись «Выход и смерть одинаково
   заканчивают забег — попытка уже потрачена» описывает игру с ограниченным
   числом попыток. Их нет: кулдауна у храма нет, зайти можно сразу снова.

**Отдельно про «Венец испытаний».** Рецепт `trial-crown` существует
(`data/recipes.ts:549`), реагент «Обетный знак» тоже (`data/materials.ts:36`),
и связаны они через `clearReward` за полную зачистку. **Игроку они не видны
нигде:** панель храма перечисляет только `milestones` — этажи 5, 10 и 15, —
а награда за двадцатый этаж в этот список не входит по устройству данных.
Идти на двадцатый этаж не за чем: игра не называет, что там.

**Вывод для стадии 7:** логику начисления не трогать вовсе. Чинить показ
накопленного, текст HUD и видимость награды за полную зачистку.

## 3. По стадиям

### Стадия 1 — диагностика трёх молчаний

Только отчёт, ни строчки правки. Три диагноза — в разделе 2. Golden не двинулся,
быстрый набор 1003/1003.

### Стадия 2 — экран говорит правду

Правки только в `ui/`. Golden не двинулся; **балансный отпечаток сошёлся
побитово — 1853 величины из 1853** (полная матрица, 113 тестов, 479 с).

### Стадия 3 — целые бонусы на предметах

**Что сделано.** Граница целого и дробного заведена данными:
`ITEM_STAT_GRAIN` в `data/items.ts` — `Record<StatId, 'whole' | 'fraction'>`,
то есть новая характеристика не пройдёт проверку типов, пока про неё не решат.
`'whole'` — атрибуты, урон оружия, сила атаки, запасы, сила блока; `'fraction'` —
доли, секунды и величины в секунду. Применяется в ОДНОЙ точке (`grained`
в `game/loot.ts`): через `weaponMods`/`shieldMods`/`armorMods` проходят все три
пути появления вещи — находка, ковка, стартовый комплект.

**Смещение измерено, а не обещано.** Сумма всех прибавок по сетке
«шаблон × тир × уровень» (5 тиров × 11 уровней × все шаблоны и все главные
атрибуты брони): **138 701.9 → 138 710.0, сдвиг +0.006 %**. Требование стадии —
меньше процента; запас в полтора порядка. Худшая ОТДЕЛЬНАЯ строка — +20 %:
это общий довесок брони (2) на первом уровне необычного тира, то есть
2.5 → 3. Мельче двойки в данных прибавок нет, и тест «ни одна прибавка не
округляется в ноль» держит это правило вслух.

**Где округления НЕТ и почему.** `averageArmorMods` — единственное место,
и это решение, а не забытая строка. Там считается не вещь, а матожидание по
популяции вещей. Ступенька в эталоне запрещена тем же доводом, что и
`floor`/`ceil` в `farmCycle`: округлённый эталон стал бы ступенчатой функцией
уровня вещей, и каждый контракт, который меряется по лестнице уровней, начал
бы прыгать на границах ступеней. Граница простая и записана в CLAUDE.md:
**конкретная вещь — целая, матожидание — непрерывное**. Оружие и щит эталона
округляются как все: эталонный герой носит один полуторник и один заслон,
а не «средний меч».

**Первая попытка была другой, и её пришлось откатить — это стоит записать.**
Сперва `averageArmorMods` считал матожидание ОТ УЖЕ ОКРУГЛЁННЫХ значений
(точное среднее настоящей популяции: `[round(6p) + 3·round(2p)] / 4` и
`round(4p) / 4`). Формально это точнее. Итог: эталонный герой на входе в
ПЕРВЫЙ данж потерял 0.63 силы из 94.88 (−0.66 %) и 2 маны из 303, и цепочка
`sunken-barrow` перестала проходиться — герой гиб на третьем боссе, когда у
того оставалось 2.3 % запаса.

Замер разъяснил, что это не регрессия игры, а край в приборе:

| прогон | до округления | после |
| --- | --- | --- |
| `rng = () => 0.5` (как в тесте данжей) | прошёл, цены 72.7 / 85.1 / 83.5 % | **погиб** на третьем боссе |
| двадцать настоящих сидов | прошёл 19 из 20 | прошёл 19 из 20 |

Постоянный `rng = () => 0.5` — это герой, который НИКОГДА не критует
(`rng() < critChance` при 0.5 не срабатывает никогда). На настоящих сидах
схватки с боссами стоят 7–66 % запаса вместо документированных 69–89 %, и
цепочка проходится с большим запасом. То есть контракт «схватка стоит около
80 %» стоит на герое без критов. **Это находка, а не задача стадии 3:**
переписывать прибор внутри маленькой стадии значило бы переписать заодно и
контракт цены схватки. Записано открытым вопросом (раздел 10).

**Проба баланса упала, и это оказалась третья находка про прибор.**
`test:balance:sample` уронил контракт полного пути: смертей в час 1.02 при
пороге 1 (было 0.09). Ассерт написан ПО МЕДИАНЕ намеренно — «один неудачный
сид это невезение рулетки находок, а не сломанный баланс», — но три сида шли
только в ночном прогоне (`RUN_ALL_SEEDS=1`), а в пробе путь считался ОДНИМ
сидом, то есть медиана бралась из одного числа.

Замер по всем трём сидам:

| сид | часов до | часов после | смертей/ч до | смертей/ч после |
| --- | ---: | ---: | ---: | ---: |
| 4242 | 11.01 | 10.78 | 0.09 | 1.02 |
| 7 | 11.02 | 16.80 | 0.27 | 0.36 |
| 1234 | 10.94 | 11.64 | 0.27 | 0.00 |
| **медиана** | **11.01** | **11.64** | **0.27** | **0.36** |

Медиана внутри контракта: смертей 0.36 < 1, часов 11.64, убийств 5178
(коридор 4700–5400). Красным был один сид.

**Что случилось на сиде 7 — разобрано, а не списано на шум.** Журнал решений
модели игрока показывает развилку на четвёртом-пятом уровне: до округления
герой надевал одноручное 4 уровня и уходил в связку, которая держалась до
одиннадцатого; после — брал эпическое оружие ПЕРВОГО уровня (+17.5 % в тот
момент) и сидел в нём до десятого. Полоса 10–59 стоила ему 9.25 часа против
4.96, секунд на убийство 13.7 против 8.6. Числа предметов сдвинулись на доли
процента — решение перекинулось целиком.

Это ТА САМАЯ слепая зона, ради которой написана стадия 4: «лучше» считается
одной осью, разница между связками лежит внутри допуска этой оси, и любой
шум перекидывает выбор. Стадия 3 её не создала — она её показала.

Прибор починен отдельным коммитом (`fix(run)`): три сида идут везде, а не
только ночью. Цена замерена — 36 с против 108 с на четырёх ядрах; прежняя
экономия в семьдесят секунд стоила ложного отката стадии. Комментарий у
`RUN_SEEDS` этот случай уже описывал по прошлой ночи.

**Golden НЕ ДВИНУЛСЯ, хотя стадия обещала сдвиг.** Проверено явно:
`golden.json` не изменился ни байтом. Причина в устройстве прогона — билд
героя задаётся источниками статов, автонадевания в прогоне нет, а цена
продажи (`sellPrice`) считается по тиру и от модификаторов не зависит.
Находки в прогоне меняются, но ни на один снимаемый golden показатель они
не влияют. Двинулись только пресеты скриншотов (`mid.json`, `rich.json`) —
в них лежат настоящие вещи, и у них поменялись числа: `49.704 → 50`.

### Стадия 4 — две оси апгрейда и приоритет игрока

**Что было сломано.** «Лучше» считалось одной мерой — убийств в секунду, — и
живучесть входила в неё ТОЛЬКО через аптайм. В зоне, где герой не гибнет,
аптайм равен единице: панцирь не двигал оценку вовсе. Отсюда все три жалобы
разом — двуручное всегда обгоняет связку со щитом, броня на низких уровнях
слегка роняет оценку, защитные ветки талантов отстают в полтора раза. Это
третье появление одной слепой зоны: из-за неё во вторую ночь откатилось
лечение, третья ночь починила её в симуляции, здесь она чинится там, где её
видит игрок.

**Две оси, обе из того же конвейера.** Второй метрики не заведено:

| ось | что считает | откуда |
| --- | --- | --- |
| `damage` | прирост убийств в секунду | `estimateCombatRate`, как раньше |
| `survival` | снижение ЦЕНЫ БОЯ долей запаса | тот же вызов, формула контракта цены боя |

Обе берутся из ОДНОЙ пары состояний и одних и тех же трёх мобов зоны
(`axesOf`), а не из двух проходов: два места, где выбирается противник,
разъехались бы на первой правке.

Цена боя для оси берётся НЕТТО, с лечением, — и это отличие от контракта,
который стоит на валовой намеренно. Контракт мерит счёт, который выставляет
моб, до того как герой оплатит его маной; игроку в сумке нужен другой ответ:
сколько он РЕАЛЬНО потеряет за бой в этой вещи. Предмет на интеллект даёт
лишнее лечение и цену снижает — по валовой этого не видно вовсе, то есть
«выживание» умалчивало бы ровно про то, чем Страж и выживает. Дважды мана не
считается: потраченная на лечение, она не идёт в урон, и ось урона это уже
видит.

**Замер на герое 25 уровня в вещах 17-го, панцирь на живучесть:**

| живучесть | мелкая зона: урон / выживание | своя полоса: урон / выживание |
| ---: | --- | --- |
| 25 | −3.16 % / +5.34 % | −3.76 % / +9.37 % |
| 100 | −2.53 % / +85.41 % | −1.92 % / +44.89 % |
| 200 | −2.54 % / +97.89 % | +0.43 % / +67.26 % |

Видно и старое свойство (запас окупается УРОНОМ только там, где герой теряет
аптайм), и новое: цену боя панцирь снижает в обеих зонах, и по одной мере
игра об этом молчала.

**Приоритет игрока** — три положения в панели сумки (`data/upgrade.ts`),
по умолчанию БАЛАНС, в сейве (версия 26 + миграция + фикстура `save-v25`).
Правило одно на все три, и три обещания переключателя получаются из него
сами: апгрейд — если лучше хотя бы по одной из осей приоритета; лишнее —
если не лучше ни по одной. Ветвлений по конкретному положению в логике нет.

Старым сейвам миграция ставит БАЛАНС, а не «урон». Одноосная мера и была
дефектом, ради которого приоритет заведён; сохранять её ветеранам значило бы
оставить их с той же слепой зоной.

**Железное правило сильнее приоритета.** Находка, лучшая хоть по одной оси,
при полной сумке не продаётся и не распыляется — она вытесняет худшее.
Проверка (`betterOnAnyAxis`) вызывается БЕЗ приоритета, и это не упущение:
игрок, поставивший «урон», просил не звать панцирь апгрейдом, а не
выбрасывать панцирь. Тест гоняет все три положения переключателя.

**Пустой слот — аудит, который просили сделать отдельно.** Ответ: НЕТ, вещи
для пустого слота в золото не уходили. Правило пустого слота («не обязана
поднять, достаточно не уронить») существовало и до этой ночи, и `lootValue`
через него уже считал такую находку апгрейдом. Молча потерянных вещей не
было. Что было — «Апгрейд 0 %» на карточке: единственная ось в безопасной
зоне честно давала ноль. Теперь на той же карточке стоит настоящее число,
потому что цену боя первая в жизни вещь снижает заметно. Оба свойства
закреплены тестами, включая прямую проверку «сумка полна, слот пуст —
находка остаётся».

**Модель игрока в прогоне** берёт приоритет из состояния, отдельного правила
у неё нет — герой прогона строится теми же функциями, а там умолчание
«баланс». Из этого следует одно свойство, которое стоит знать вслух: порядок
«лучше» стал ЧАСТИЧНЫМ. Вещь, меняющая урон на живучесть, и обратная —
апгрейды друг над другом, и модель их переставляет. Замер полного пути это
выдерживает: сид 4242 даёт 11.61 часа против 11.01 до ночи, уровень 100
достигнут; перебор внутри одного прохода ограничен числом слотов.

**Полный путь по трём сидам после стадии** (медиана — контракт ночи):

| сид | часов до ночи | после стадии 3 | после стадии 4 |
| --- | ---: | ---: | ---: |
| 4242 | 11.01 | 10.78 | 11.61 |
| 7 | 11.02 | 16.80 | 11.37 |
| 1234 | 10.94 | 11.64 | 15.51 |
| **медиана** | **11.01** | **11.64** | **11.61** |

Смертей в час: медиана 0.27 до ночи, **0.00 после стадии 4** (по сидам
0.17 / 0.00 / 0.00). Доля привала упала с 25.7 % до 19.6 %. То есть герой,
который считает бронь апгрейдом, реже уходит отдыхать и почти перестаёт
гибнуть — при том же числе убийств (5178 против 5306, коридор 4700–5400).
Разброс по сидам остался велик (11.4–15.5 ч): он и был велик, просто
выпадает теперь другой сид.

**Golden двинулся ровно там, где обещано:** на шаге 12000 золото
4541.756 → 4536.756, суммарная цена сумки 175 → 180. Пять золотых — цена
одной находки, которая раньше уходила в автопродажу как «не апгрейд», а
теперь остаётся в сумке. Ни один другой показатель снимка не изменился.

### Стадия 5 — лестница открытий действительно закрывает

**Закрыто значит не видно.** Панели механик, до которых игрок не дорос, убраны
из разметки целиком — ни серых, ни с замком, ни строкой «откроется на N
уровне». Спрятаны: таланты (до 10), ремёсла (до 30), зачарование (до 50),
храм (до 70), задания (до 80). Коды отказа в логике ОСТАЛИСЬ — они нужны
тестам и оффлайну, просто ни один экран их больше не рисует.

Обратный отсчёт «первое задание выдадут на 80 уровне — осталось 55» отвечал
на вопрос, который лестница открытий держит закрытым, и всё это время занимал
место в разделе.

**Ремёсла — с 30 уровня.** До этой ночи вся кузня с кулинарией работали с
первого: `recipeUnlockLevel` возвращал `unlockLevel ?? 1`, а `unlockLevel` у
рядовых рецептов не задан. Теперь порог профессии — ПОЛ для всех её рецептов
(`PROFESSION_UNLOCK_LEVEL` в данных), а свой порог рецепта его только
поднимает. Реликварий при этом остался на своём 60-м.

**Травничество и зелья — с 40 уровня, целиком.** Один порог на сбор, рецепты
и склянки: «собирать можно, варить нельзя» было ровно тем состоянием, которое
хуже и запрета, и разрешения.

**Травы начинаются с полосы 41–45.** Раньше они росли с самого луга «и
копились до сорокового, ровно как материалы копятся до дорогих рецептов».
Разница в том, что материал лежит в мешке молча, а трава срезается временем и
показывает себя счётчиком: игрок двадцать уровней смотрел, как растёт запас
того, к чему у него нет ни рецепта, ни кнопки.

Полосы после правки (все три травы доступны сразу с первой травяной зоны —
герой, доросший до зелий, варит любое, не бегая по половине мира):

| трава | зоны |
| --- | --- |
| Горчелист | Топлёный ярус (41) и дальше все |
| Тлекорень | Топлёный ярус, с Серных ключей (51) и дальше |
| Стылоцвет | Топлёный ярус, Плесневый горизонт, с Продувного перевала (61) |

**Новое правило content:check.** Механика с уровнем открытия N не может иметь
источник ресурса в зоне ниже N; зона считается «ниже», если её мобы
НАЧИНАЮТСЯ ниже N. Битый образец обязателен и написан: трава в стартовой
зоне при травничестве с сорокового. Заодно переписана старая проверка «в
каждой зоне растёт хоть одна трава» — теперь она смотрит только зоны от
порога травничества, иначе два условия исключали бы друг друга.

**Улучшения за золото — сделаны с нуля.** Диагностика стадии 1 показала, что
их нет в коде вовсе: задача «Шаг 8: лестница покупок» так и осталась
незакрытой с прошлой итерации.

ОГОВОРКА ОБ ИСТОЧНИКЕ. Документа проекта «Кривая, зоны и золото» в
репозитории нет — он жил в переписке. Восстановить удалось только строку из
списка задач: «Семь ступеней, переключатель «не трогать/продавать/распылять»,
вторая специализация, версия сейва». Сделаны семь ступеней, переключатель и
версия сейва. **Вторая специализация талантов НЕ сделана намеренно:** это
отдельная система (два набора веток, переключение, цена), и собирать её из
трёх слов в списке задач значило бы угадывать чужой замысел. Ступени лестницы
заняты тем, что восстанавливается однозначно.

Что получилось (на каких уровнях и где на экране):

| ур. | покупка | цена | что даёт |
| ---: | --- | ---: | --- |
| 15 | Заплечный мешок | 2 ч дохода | +4 места в сумке |
| 25 | Скупщик в лагере | 3 ч | открывает «продавать лишнее» |
| 35 | Двойные ремни | 5 ч | +4 места |
| 45 | Вьючный короб | 8 ч | +4 места |
| 55 | Пылевой ларь | 12 ч | открывает «распылять лишнее» |
| 70 | Караванный сундук | 18 ч | +4 места |
| 85 | Бездонная торба | 26 ч | +4 места |

Всё это — в разделе **«Сумка»**, отдельным блоком «Купить за золото» под
переключателем приоритета. Показываются только доступные и ещё не купленные:
закрытая ступень — та же запертая кнопка, а купленная в списке «что можно
взять» уже не нужна.

Цена — в ЧАСАХ дохода своего уровня (`goldPerHourAt`), тем же приёмом, что и
пошлина крафта: числом её пришлось бы держать и в цене, и в кривой золота.
Итог: сумка растёт с 24 до 44 мест за всю игру.

**Ни единицы силы за золото не продаётся**, и это закреплено тестом: после
покупки всех семи ступеней блок статов героя совпадает с исходным побитово.
Иначе золото стало бы второй лестницей прокачки рядом с уровнями и
предметами, и бюджет силы пришлось бы делить ещё и с ним.

**Разбор на лету** (`lootPolicy`): лишняя находка — не лучшая ни по одной оси
приоритета — уходит сразу в золото или в пыль. Железное правило сумки сильнее
и здесь: вещь, лучшая хоть по одной оси, не продаётся и не распыляется ни при
каком положении. Автоматика убирает мусор, а не находки.

**Бюджет силы:** строка «зелья» помечена как действующая только с
`POTION_UNLOCK_LEVEL`, и в тест добавлена явная проверка, что замер идёт на
уровне не ниже порога. Сейчас он идёт на потолке (100), так что строка
законна — но опустись замер, и коридор мерил бы силу, которой у героя нет.

**Golden НЕ ДВИНУЛСЯ, хотя стадия ожидала сдвига.** Причина конкретная:
снимок golden не содержит `materials`, а единственное, что стадия меняет в
прогоне, — это сбор травы (герой первого уровня на лугу больше её не режет).
Ни один снимаемый показатель от этого не зависит. Проверено явно.

### Стадия 6 — порог привала ползунком

**Ползунок вместо четырёх пресетов.** 0–100 % шагом 10 %, одиннадцать
положений. Прежние 0 / 40 / 60 / 80 % врали дважды: между 40 и 60 разница в
цене боя ощутима, а выбрать там было нечего, и «никогда» стояло в одном ряду
с процентами, будто это такой же процент. Крайние положения теперь названы
словами: на нуле подпись говорит «привалов не будет вовсе».

**Потолок поднят с 0.95 до 1.** Прежний довод — «выше герой уходил бы
отдыхать после первой же царапины» — это ОПИСАНИЕ выбора, а не довод против
него. Ползунок обещает сто процентов, и обещание обязано быть правдой; плохой
выбор игрок имеет право сделать (см. правило «игра называет, что нельзя, и
никогда — чем кончится»).

Прижатие к шагу живёт в ОДНОЙ функции (`snapRestThreshold`), и зовут её и
экшен стора, и миграция сейва: «0.5499999» в правленом руками сейве читалось
бы на экране как 55 % при шаге в десять.

**Три таланта на ПОРОГ привала удалены.** Не один, как ожидала стадия, а три:
«Походная перевязка» у Стража и «Звериное чутьё» с «Чуткостью» у Изувера —
все давали `restThreshold` плоской прибавкой. Довод общий: порог привала —
НАСТРОЙКА ИГРОКА. Он ставит её ползунком и ждёт, что игра ей следует; талант,
молча сдвигающий её вверх, читается как поломка — на экране 60 %, а герой
уходит отдыхать на 72 %, и объяснения этому нигде нет. Прокачивается то, чем
герой ЯВЛЯЕТСЯ, а не то, что он себе назначил.

**На их местах — таланты на ДЛИНУ привала**, процентом от суммы конвейера:

| было | стало | эффект |
| --- | --- | --- |
| Походная перевязка (+2 % порога) | Скорые сборы | −4 % длины привала за ранг |
| Звериное чутьё (+2 % порога) | Лёгкий лагерь | −4 % за ранг |
| Чуткость (+1.5 % порога) | Неугомонность | −3 % за ранг |

Процентом, а не секундами, потому что секунды в тех же ветках уже заняты
(«Скорый лагерь» −0.5 с, «Крепкий сон» −0.3 с): процент складывается с ними
по-другому и даёт выбор, а не удвоение одного и того же. Глубина веток не
изменилась — 61 очко, закреплено тестом. Пол длины привала
(`MIN_REST_DURATION_S`) проверен на герое с максимально вложенной веткой.

**Очки возвращаются свободными.** Отдельного счётчика доступных очков в
состоянии нет — они считаются как «заработано минус вложено», — поэтому
миграции достаточно убрать ранги удалённых талантов (`REMOVED_TALENT_IDS`).
Фикстура `save-v27.json` несёт героя 60 уровня с 34 вложенными очками, из них
3 — в удалённый талант; тест проверяет, что после загрузки вложено ровно 31,
а доступных стало на 3 больше. Молча съесть очки у ветерана нельзя.

**Новое правило content:check:** таланту нельзя менять стат из
`SETTING_STAT_IDS` (сейчас там один `restThreshold`). Битый образец написан —
талант с прибавкой к порогу привала.

**Попутно убрана мёртвая иконка.** `talent-field-medicine` (аптечка) осталась
без единого владельца, и тест «неиспользуемых иконок в реестре нет» это
поймал — ровно за этим он и написан: каждая лишняя иконка это килобайт,
который грузит каждый игрок и не видит никто. Спрайт пересобран: 172 иконки,
269.4 КБ.

**Golden НЕ ДВИНУЛСЯ**, хотя стадия ожидала сдвига: билд прогона golden
талантов не вкладывает вовсе, а порог привала в нём стоит на умолчании,
которое не менялось. Проверено явно.

### Стадия 7 — храм: кривая и награды

**ГЛАВНОЕ: диагностика стадии 1 была НЕВЕРНА, и вот чем.** Раздел 2.3 гласил
«цепочка цела, молчит экран». Цепочка была РАЗОРВАНА. Шаг конвейера тика
`applyTempleWave` написан, снабжён комментарием «стоит ПОСЛЕ applyRespawn
намеренно» — и в список `PIPELINE` не попал. `clearTempleWave` не звался
никогда, `run.cleared` оставался нулём при любой глубине забега, а
`finishTempleRun` платит ровно за этажи выше рекорда, то есть за отрезок от
1 до 0. Игрок доходил до четырнадцатого этажа и получал ровно ничего — как и
сообщил.

Почему стадия 1 этого не увидела: она читала код начисления и проверяла его
ВЫЗОВОМ ФУНКЦИЙ РУКАМИ (`clearTempleWave` → `finishTempleRun`), а так
цепочка действительно цела. Не хватало одного — прогнать НАСТОЯЩИЙ тик.
Здесь это и сделано, замером: волны шли до седьмой, `cleared` стоял на нуле.
Тест на этот случай теперь есть отдельно от рукописного («ЖИВОЙ ТИК доводит
забег до награды»), потому что рукописный поломку не видел.

Правка — одна строка в `PIPELINE`.

**Кривая сложности переделана: этаж, а не герой.** Боец храма брал уровень
ГЕРОЯ, и храм получался ровным — на семидесятом герой дрался с семидесятыми,
на сотом с сотыми, и глубина от уровня почти не зависела:

| уровень | было этажей | стало |
| ---: | ---: | ---: |
| 70 | 5 | **5** |
| 80 | 5 | 9–10 |
| 90 | 5 | **15** |
| 100 | 5–7 | **20** (полная зачистка) |

Решение владельца — «примерно 5 / 15 / 20» — сходится точно.

Теперь этаж это АБСОЛЮТНАЯ ступень: `templeFloorLevel(N) = 66 + 2N` с
потолком в `LEVEL_CAP`. Числа не подобраны, а решены: замер показал, что
хорошо одетый герой держит разрыв уровней примерно до +8 и гибнет дальше;
отсюда «стена на этаже 5 при герое 70» даёт `base + 6·step = 78`, «стена на
этаже 15 при герое 90» даёт `base + 16·step = 98», то есть step = 2,
base = 66. Стеной работает ОБЩЕЕ правило мира (`levelGapDamageMult`,
+30 % урона за уровень разрыва после пятого), а не своя механика храма.

Ставки волны при этом урезаны: урон волны больше не множится вовсе
(1.07 → 1.0), здоровье растёт мягче (1.13 → 1.05). Сложность несёт уровень
этажа; накладывать сверху степень значило бы считать одно и то же дважды, и
стена вставала бы на третьем этаже.

**Тест двусторонний**, как у ворот данжей: одетый по контракту доходит до
ожидаемого ±1, недоодетый — СТРОГО МЕЛЬЧЕ (70: 5 против 3, 90: 14–15 против
8). Сравниваются два замера одного прогона, а не замер с константой: глубина
шумит на ±1 от рулетки волн, и требовать «меньше пяти» значило бы ловить
невезение, а не кривую.

Отставание здесь взято ВДВОЕ больше запаса ворот данжей, и это тоже замер, а
не осторожность. Первая попытка стояла на обычном запасе (15 пунктов) и
оказалась ровно на грани: отдельным прогоном она давала 5 против 4, а в общей
пробе — 5 против 5, то есть ловила шум. Внизу лестницы этажи стоят НИЖЕ
собственного уровня героя (первый этаж — 68-й уровень при герое 70), и стеной
там служит разрыв уровней, а не качество вещей; одного запаса ворот хватало
на один этаж разницы. На двойном разрыв устойчив.

**Тест на тот самый случай** написан: рекорд 0, забег до 14 этажа со смертью
на 14-м → платят ровно этажи 1–13, рекорд 13, рубежи 5 и 10 взяты, 15 нет,
полной зачистки нет. Плюс «второй забег той же глубины не платит ничего».

**Копилка забега теперь видна.** HUD показывает, сколько уже заработано и за
какие этажи, — той же арифметикой (`pendingTempleReward`), что и начисление.
Раньше игрок видел только номер этажа и получал награду молча одной строкой
лога после смерти.

**Ложная подпись HUD исправлена.** Стояло «Выход и смерть одинаково
заканчивают забег — попытка уже потрачена» — описание игры с ограниченным
числом попыток, которых нет: кулдауна у храма нет, зайти можно сразу снова.
Стало: выход и смерть засчитывают дошедшие этажи, закрытая вкладка — нет.
Это правда и это единственное различие, которое игроку надо знать.

**«Венец испытаний» назван в лестнице храма.** Рецепт существовал в данных,
реагент «Обетный знак» тоже, связаны они были через `clearReward` — и игрок
не знал ни о том, ни о другом: идти на двадцатый этаж было не за чем. Теперь
рубежи храма кончаются строкой «Все 20 этажей — «Обетный знак» и рецепт
«Венец испытаний»» с пояснением, что знак нужен самому рецепту.

**Тест переехал в дорогой набор** (`balance-temple.test.ts`): он гоняет
настоящий тик по лестнице и стоит 28 с — в быстром наборе это половина его
времени. Стоимость записана в таблицу очереди раннера.

### Стадия 8 — мобы своего уровня

**Что сделано.** Уровень моба при спавне больше не берётся по полосе зоны
равновероятно: он жмётся к уровню героя. Распределение лежит в ДАННЫХ —
`SPAWN_LEVEL_SPREAD` в `data/monsters.ts`: 70 % ровно уровень героя, по 10 %
на ±1, по 5 % на ±2. Полоса зоны сильнее распределения: смещения, выпадающие
за её край, отбрасываются, а оставшиеся веса нормируются, — герой выше полосы
получает мобов, прижатых к верхней границе, как и раньше. Порядок бросков не
тронут: сперва уровень, потом архетип из пула.

Модель научена ТОМУ ЖЕ распределению, иначе она считала бы другую игру.
`zoneSpawnVariants(zone, heroLevel)` — единственный источник «каких мобов
этому герою выдают и с какой вероятностью»; на него переведены темп зоны
(`zoneRate`), худший бой (`zoneSafety`), `maxMonsterHit` и `estimateZoneTtk`.
Равномерное усреднение по полосе (`zoneMonsterVariants`) осталось ровно для
вопроса «что вообще водится в зоне».

**Замер 1 — лестница смертности выровнялась.** Это главное, что стадия
сделала с игрой. Худший бой зоны (здоровяк с верхнего края полосы, доля
запаса героя):

| | было | стало |
| --- | --- | --- |
| стартовая зона | 38 % | 24.1 % |
| по всем двадцати зонам | 15–42 % | 18.0–25.8 % |

Коридор сузился вчетверо. Провал был там, где он больнее всего: новичок в
одном белом клинке встречал на лугу мобов ВПЯТЕРО старше себя, и первая
зона была самой дорогой схваткой всей игры. Контракт в `mortality.test.ts`
переписан под замер — коридор 15–30 % на каждой зоне ПЛЮС отдельная проверка,
что разрыв «первая зона против последней» меньше 12 пунктов: односторонний
коридор пропустил бы возврат провала на краю лестницы.

**Замер 2 — штраф опыта за отставание цел.** Проверено по тому самому
распределению, по которому спавнит игра, на всех двадцати зонах:

- уровень, на котором отставшая зона ПЕРЕСТАЁТ платить опыт, **не сдвинулся
  ни в одной зоне** — он и был, и остался «верх полосы + 11»;
- полная доля держится на два уровня дольше (верх полосы + 1 → + 3).

Второе — прямое следствие клампа: прижатый к верхней границе моб даёт самый
мягкий разрыв, какой зона вообще может дать. Раньше среднее по всей полосе
размазывало ступени `XP_GAP_PENALTY` в ровный скат (1.0, 0.9, 0.8 …), теперь
видны сами ступени. Ступенчатой таблице это ближе, чем скат, которого в
данных никогда не было. Закреплено двумя тестами в `xp-gap.test.ts`.

**Замер 3 — цена боя на месте.** Медианный бой Стража: 14.6–20.5 %, в среднем
16.4 % при контракте 13–20 ± 2. Урон мобов не трогался, и не должен был:
стадия меняет, КАКИХ мобов выдают, а не сколько они бьют.

**Замер 4 — дрейф TTK не сузился, и потолок остаётся 0.5.** Здесь ожидание
задания не подтвердилось, и это стоит сказать прямо: 42.9 % → **46.3 %**,
разброс вырос. Разложение по механизмам объясняет, почему:

| | было | стало |
| --- | --- | --- |
| внутри зоны | 0.4–14 % | 2.4–19.8 % |
| по всей лестнице | 42.9 % | 46.3 % |

Внутризонного «за пять уровней зоны время убийства уходит с пятнадцати секунд
до восьми», ради которого стадия и затевалась, в приборе не было ВОВСЕ: модель
мерила СРЕДНЕГО моба полосы — число, которое от уровня героя не зависит. После
правки внутри зоны появился рост (а не падение): мобы поднимаются вместе с
героем, а снаряжение эталонный герой меняет только на переезде. Хуже всего
первая зона (10.1 → 12.1 с) — в ней он идёт в стартовом комплекте все пять
уровней и не одевается вообще.

А сами 46 % — это 12.1 с на первом уровне против 8.3 с на девяносто четвёртом,
то есть бюджет силы за сотню уровней (61 очко талантов, зачарования, зелья,
реликвия) против кривой здоровья мобов. Уровень спавна на него не влияет
никак. Сузить потолок нечем: 46.3 % — это 93 % от 0.5, контракт и так висит
на волоске. Настоящая ручка — здоровье мобов на верхних уровнях, то есть
контракт темпа с другого конца; это решение владельца, и оно ушло в открытые
вопросы.

**Замер 5 — цена пути в убийствах не сдвинулась, а «часы» оказались не тем
прибором.** Контракт полного пути в проекте меряется УБИЙСТВАМИ (`KILLS_PER_LEVEL`,
коридор 4700–5400), и они на месте: медиана 5178 → **5186** (+0.15 %).

Часы — печатаемая диагностика, ассерта на них нет, и вот с ними вышла история,
которую стоит записать. Трёхсидовая медиана показала 11.61 → **18.13 ч**, то
есть +56 % при бюджете стадии ±5 %. Прежде чем что-то откатывать, прогон
повторён на ДЕВЯТИ сидах, и распределение оказалось ДВУГОРБЫМ:

| режим | сиды | часов |
| --- | --- | ---: |
| быстрый | 1234, 31337, 808, 99, 60606, 2 | 11.44–12.76 |
| медленный | 4242, 555, 7 | 18.13–19.31 |

Медиана девяти — **12.20 ч** против 11.61 до стадии, то есть **+5.1 %**: ровно
бюджет, в пределах погрешности замера. А `RUN_SEEDS` — это `[4242, 7, 1234]`,
и два из трёх попали в медленный горб; медиана трёх прочитала 18.13 — на 49 %
выше медианы девяти. Горбы были и ДО стадии: в тройке до неё сид 1234 давал
15.51 ч против 11.4–11.6 у остальных двух.

Убийства ведут себя ровно наоборот: по девяти сидам 5157–5215, разброс **1.1 %**.
Поэтому контракт пути и стоит на них, а не на часах, — и это оказалось верным
решением задолго до этой ночи. В `data/balance.ts` рядом с `RUN_SEEDS` записано,
что печатаемые часы двугорбы и читать их как контракт при трёх сидах нельзя.


### Стадия 9 — золото от находок

**Диагноз.** Находки давали **0-1 % золота**. Цена продажи не зависела от
уровня вещи ВООБЩЕ — только от тира: `5 × sellMult`, то есть от 5 до 150
золота за что угодно с первого до сотого уровня. Сумка, набитая на сотом,
продавалась по цене сумки с первого, а типичный моб платил там 1621. Половина
игры — добыча, и она не платила.

**Что сделано.** Кран золота разделён на две доли одним числом в данных —
`GOLD_SOURCE_SHARE` в `data/balance.ts`: 30 % платит моб напрямую, 70 %
приносит продажа упавшего с него. Доли делят ОДНО И ТО ЖЕ
`MONSTER_BASE.goldReward`, поэтому общий кран не двигается; сумма обязана
быть единицей, и это отдельная проверка `content:check` с битым образцом.

Цена находки стала формулой (`itemSellPrice` в `data/loot.ts`): кривая ТА ЖЕ,
что у наград моба — `rewardScale(уровень)` плюс множитель зоны, — а база
не выбрана, а посчитана: доля крана, делённая на `DROP_CHANCE` и на средний
множитель тира (`EXPECTED_SELL_MULT`, тоже выведенный из весов рулетки, а не
вписанный). Средний множитель роли (`AVERAGE_ROLE_GOLD_MULT`) выведен из трёх
ролей: типичное убийство — не COMMON, а среднее по пулу, и единица вместо 1.1
увела бы кран на десятую часть при формально верной формуле.

Модель дохода (`goldPerHourAt` — единственная в игре) научена обоим
слагаемым и берёт их С ОДНОГО И ТОГО ЖЕ моба: находка падает с него и несёт
его уровень, а не уровень героя. На глубине разница копеечная, у первой
зоны — четверть дохода.

**Замер 1 — кран сохранён ТОЧНО.** Модель дохода до и после, по уровням
1/10/25/55/85/100: расхождение **0.00 % на каждом**, доля находок ровно
70.0 %. Это не подгонка, а следствие построения: обе доли делят одно число.

**Замер 2 — состав дохода в настоящем прогоне** (четыре часа тика, эталонный
герой своей зоны):

| уровень | мобы | находки |
| ---: | ---: | ---: |
| 25 | 35 % | 65 % |
| 55 | 32 % | 68 % |
| 85 | 30 % | 70 % |

**Замер 3 — продать против распылить: ×2.62, и не растёт.** Мера — сколько
находок нужно на ОДНО улучшение снаряжения по каждому пути: кованая вещь
стоит час дохода золотом (107 находок), самое дорогое зачарование — 120 пыли
(41 находка). Порог задания — три раза, и он не перейдён.

Важнее числа то, что оно **одинаково на всей лестнице**: в числителе и
знаменателе стоит одна и та же кривая. До правки выбора не было вовсе —
разрыв рос вместе с уровнем: **×60 на десятом уровне и ×1597 на сотом**,
распылять было выгоднее в полторы тысячи раз. Оба свойства закреплены
тестами в быстром наборе.

**Что пришлось починить по дороге: прибор дохода.** Перенос дохода на
находки сломал контракт «модель совпадает с прогоном» — 43-81 % расхождения,
и обе причины оказались в приборе, а не в игре.

1. **Мерился кошелёк, а надо кран.** Сумка — буфер на дюжину мест, и за
   первый час он ещё наполняется: до кошелька доходит только вытесненное.
   Непроданная находка — отложенное золото, а не потерянное. У прогона
   появился `incomePerHour` = кошелёк плюс подорожание сумки, и контракты
   дохода перешли на него.
2. **Часа замера стало мало.** Награда моба детерминирована; цена находки
   разыгрывается тяжелохвостой рулеткой — легендарка стоит тридцать обычных
   при вероятности 0.6 %. Разброс ОДНОЙ находки — 143 % от среднего, за час
   (60-70 находок) доход гуляет на ±17 %, а отношение соседних ступеней
   кривой — вдвое: замер прочитал ступень 20 → 30 как ×3.02, а следующую как
   ×0.96, и обе были шумом. Окно поднято до четырёх часов (240-280 находок,
   ±9 %), кривая стала ровной: 2.18, 1.90, 1.46, 1.33, 1.41, 1.33, 1.35,
   1.33, 1.17.

Цена этого — `gold.test.ts` подорожал с 197 до 543 с; число обновлено в
таблице очереди раннера, чтобы файл шёл первым. Дешевле было бы мерить форму
кривой по модели, но тогда контракт перестал бы смотреть на игру, а он именно
за этим и заведён.

**Golden сдвинут осознанно.** Кошелёк упал до трети (на шаге 3000: 967 → 290),
а цена сумки выросла с 50 до 607: то же золото, только теперь оно приходит
через находки.

## 4. Полный путь по трём сидам

Прогон 1..100 настоящим тиком, сиды `RUN_SEEDS` = 4242 / 7 / 1234, класс Страж.

| после стадии | часы по сидам | медиана | убийств (медиана) | смертей/ч (медиана) |
| --- | --- | ---: | ---: | ---: |
| до ночи | 11.01 / 11.02 / 10.94 | 11.01 | 5258 | 0.27 |
| 3 — целые бонусы | 10.78 / 16.80 / 11.64 | 11.64 | 5178 | 0.36 |
| 4 — две оси | 11.61 / 11.37 / 15.51 | 11.61 | 5178 | 0.00 |
| 5 — лестница | 11.61 / 11.37 / 15.51 | 11.61 | 5178 | 0.00 |
| 6 — привал ползунком | 11.61 / 11.37 / 15.51 | 11.61 | 5178 | 0.00 |
| 7 — храм | 11.61 / 11.37 / 15.51 | 11.61 | 5178 | 0.00 |
| 8 — мобы своего уровня | 18.13 / 19.31 / 11.44 | 18.13 | 5186 | 0.26 |
| 9 — золото от находок | 19.25 / 18.50 / 12.26 | 18.50 | 5202 | 0.24 |

**Убийств на уровень — главное число этой таблицы, и оно не двигалось.** Цена
пути в проекте задана таблицей `KILLS_PER_LEVEL` (коридор 4700-5400), и все
девять стадий прошли внутри него: 5178-5202, то есть разброс меньше половины
процента. Смертность тоже на месте: медиана 0-0.36 при потолке 1.

**А вот часы читать как контракт нельзя, и это находка ночи.** Прогон по
ДЕВЯТИ сидам показал двугорбое распределение: шесть сидов дают 11.4-12.8 часа,
три — 18.1-19.3, и какой горб выпадет, решает ранняя рулетка находок. Медиана
девяти — **12.20 ч**. В `RUN_SEEDS` три сида, и после стадии 8 два из трёх
легли в медленный горб: медиана трёх прочиталась как 18.13 — на 49 % выше
медианы девяти. Горбы были и раньше (в тройке до стадии 8 сид 1234 давал
15.51 против 11.4 у остальных), просто не попадали в медиану.

Убийства ведут себя ровно наоборот: по девяти сидам 5157-5215, разброс 1.1 %.
Поэтому контракт пути и стоит на них — и это оказалось верным решением задолго
до этой ночи. Пометка записана рядом с `RUN_SEEDS` в `data/balance.ts`, чтобы
следующий читатель печатаемых часов не сравнил их с прошлой ночью.

## 5. Дрейф темпа по зоне: до и после стадии 8

`TTK_DRIFT_MAX` **остался 0.5**, и это единственный пункт задания, который не
выполнен так, как задумывался. Замер не подтвердил ожидание:

| | до стадии 8 | после |
| --- | ---: | ---: |
| разброс TTK по всей лестнице | 42.9 % | **46.3 %** |
| разброс внутри зоны (макс. по зонам) | 14.0 % | 19.8 % |
| разброс внутри зоны (типичная зона) | 0.4-1 % | 2.4-8 % |
| потолок `TTK_DRIFT_MAX` | 0.5 | 0.5 |

**Внутризонного дрейфа, ради которого стадия затевалась, в приборе не было
ВОВСЕ.** Аудит записал «герой за пять уровней зоны уходит с 15 секунд на моба
до 8» — но модель мерила СРЕДНЕГО моба полосы, число, которое от уровня героя
не зависит, и внутри зоны показывала 0.4-1 %. Стадия изменила ЗНАК этого
дрейфа: TTK внутри зоны теперь растёт (мобы поднимаются вместе с героем, а
снаряжение эталонный герой меняет только на переезде), и хуже всего первая
зона — 10.1 → 12.1 с, потому что все пять её уровней он идёт в стартовом
комплекте и не одевается вообще.

**Настоящие 46 % — это 12.1 с на первом уровне против 8.3 с на девяносто
четвёртом**, то есть бюджет силы за сотню уровней (61 очко талантов,
зачарования, зелья, реликвия) против кривой здоровья мобов. Уровень спавна на
это не влияет никак. Сузить потолок нечем: 46.3 % — это 93 % от 0.5, контракт
и так висит на волоске и падает от любого лишнего процента силы. Ручка одна —
поднять здоровье мобов на верхних уровнях, а это контракт темпа с другого
конца; решение владельца, и оно в разделе 11.

## 6. Состав дохода: до и после стадии 9

**Суммарный кран не изменился — точно, а не приблизительно.** Модель дохода
(`goldPerHourAt`, единственная в игре) до и после:

| уровень | было | стало | расхождение | доля находок |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 13 042 | 13 042 | 0.00 % | 70.0 % |
| 10 | 25 647 | 25 647 | 0.00 % | 70.0 % |
| 25 | 72 764 | 72 764 | 0.00 % | 70.0 % |
| 55 | 223 126 | 223 126 | 0.00 % | 70.0 % |
| 85 | 486 424 | 486 424 | 0.00 % | 70.0 % |
| 100 | 680 752 | 680 752 | 0.00 % | 70.0 % |

Ноль здесь не подгонка, а следствие построения: обе доли делят ОДНО И ТО ЖЕ
`MONSTER_BASE.goldReward`, и `content:check` держит их сумму равной единице.

**Состав в настоящем прогоне** (четыре часа тика, эталонный герой своей зоны):

| уровень | мобы: было | мобы: стало | находки: было | находки: стало |
| ---: | ---: | ---: | ---: | ---: |
| 25 | 100 % | 35 % | 0 % | 65 % |
| 55 | 100 % | 32 % | 0 % | 68 % |
| 85 | 100 % | 30 % | 0 % | 70 % |

Ноль в столбце «было» — не округление. Средняя находка стоила 10.4 золота при
типичном мобе в 242 (уровень 25) и 1621 (уровень 85): цена продажи не зависела
от уровня вещи ВООБЩЕ. Теперь средняя находка стоит 679 и 4540 соответственно.

**Продать против распылить: ×2.62, и разрыв не растёт с уровнем.** Мера —
сколько находок нужно на ОДНО улучшение снаряжения каждым путём:

| | находок на одно улучшение | что получаем |
| --- | ---: | --- |
| продавать | 107 | пошлина кованой вещи (час дохода) |
| распылять | 41 | самое дорогое зачарование (120 пыли) |

Порог задания — три раза, и он не перейдён: выбор между покупками «продавать
лишнее» и «распылять лишнее» настоящий. Важнее самого числа то, что оно
**одинаково на всей лестнице** — в числителе и знаменателе одна и та же кривая.
До правки выбора не было: разрыв рос вместе с уровнем, **×60 на десятом и
×1597 на сотом**, распылять было выгоднее в полторы тысячи раз. Оба свойства
закреплены тестами быстрого набора.

## 7. Цена боя и худший бой после стадии 8

| контракт | коридор | замер после стадии 8 |
| --- | --- | --- |
| цена медианного боя (Страж, все 20 зон) | 13-20 % ± 2 | 14.6-20.5 %, в среднем 16.4 % |
| худший бой зоны (все 20 зон) | новый: 15-30 % | **18.0-25.8 %** |
| худший бой в стартовой зоне | — | 24.1 % (было 38 %) |

**Цена медианного боя не двигалась, и не должна была:** стадия меняет, КАКИХ
мобов выдают, а не сколько они бьют. Урон мобов не тронут.

**А лестница смертности выровнялась, и это главное, что стадия сделала с
игрой.** Было 15-42 % с провалом в стартовой зоне: новичок в одном белом
клинке встречал на лугу мобов ВПЯТЕРО старше себя, и первая зона оказывалась
самой дорогой схваткой всей игры. Теперь ему выдают +2 уровня максимум, и
коридор сузился вчетверо.

Контракт в `mortality.test.ts` переписан под замер и стал ДВУСТОРОННИМ:
коридор 15-30 % на каждой зоне плюс отдельная проверка, что разрыв «первая
зона против последней» меньше 12 пунктов. Односторонний коридор пропустил бы
возврат провала на краю лестницы — а именно он и был болезнью.

## 8. Golden: кто его двигал и почему

Из семи стадий, которым это разрешалось, эталон двинули **три**.

| стадия | сдвиг | почему |
| --- | --- | --- |
| 3 — целые бонусы | не двинулся | Округление идёт на генерации, а `averageArmorMods` осознанно оставлена непрерывной; конкретные вещи golden-героя на сетке округления не сместились. |
| 4 — две оси | 5 золота на шагах 12000 и 36000 | Автопродажа лишнего теперь смотрит на две оси, и на одном броске выбрала другую вещь. |
| 5 — лестница | не двинулся | Правка только запирает контент по уровню; golden-герой до этих уровней не доходит. |
| 6 — привал ползунком | не двинулся | Пресеты заменены шагом ползунка, но значение по умолчанию то же. |
| 7 — храм | не двинулся | `applyTempleWave` добавлен в конвейер, но golden-герой в храм не заходит. |
| 8 — мобы своего уровня | крупно | Герой девятого уровня в стартовой зоне больше не встречает мобов первого — ему выдают пятого. Опыт на шаге 12000: 835 → 1047, потому что моб пятого уровня платит герою девятого полную долю, а моб первого платил половину. |
| 9 — золото от находок | крупно | Кошелёк на шаге 3000 967 → 290, цена сумки 50 → 607. То же золото, только теперь оно приходит через находки. |

Стадии 1 и 2 эталон не двигали — это проверено явно перед их коммитами, и
балансный отпечаток после стадии 2 совпал побитово (1853 величины из 1853).

## 9. На что смотреть первым на живой странице

1. **Сумка: цена продажи.** Найди любую вещь и посмотри на кнопку «Продать» —
   там теперь трёх-четырёхзначное число, растущее с уровнем находки и её тиром.
   Это семьдесят процентов твоего дохода; раньше было ноль.
2. **Сумка: «Купить за золото».** Раздел был не реализован вовсе (диагностика
   стадии 1) — теперь в «Сумке» есть лестница покупок: расширение сумки на
   15/45/85 уровнях и правила разбора лишнего на 25/55/70.
3. **Бой в стартовой зоне на новом персонаже.** Мобы теперь идут ровно твоего
   уровня, а не «любого из полосы 1-5»: первые пять уровней перестали быть
   самой опасной частью игры (худший бой 38 % → 24 % запаса).

## 10. Попутные правки и что найдено, но не тронуто

**Fix-коммит один из десяти разрешённых:** `fix(run)` — полный путь меряется
медианой трёх сидов ВЕЗДЕ, а не только в ночном прогоне. Прибор соврал по
одному сиду (смертей/ч 1.02 при потолке 1) и почти уронил стадию 3; починен
инструмент, а не ослаблен контракт.

**Три прибора пришлось поправить внутри стадий** — все три сломались об
изменения, а не были сломаны изначально:

- `goldPerHour` в прогоне мерил кошелёк, а надо кран: сумка — буфер на дюжину
  мест, и непроданная находка это отложенное золото. Появился `incomePerHour`;
- окно замера дохода поднято с часа до четырёх: цена находки тяжелохвостая;
- сравнение связок оружия переведено с золота на убийства — вопрос теста про
  темп, а золото теперь несёт рулетку тиров.

**Найдено, но не тронуто:**

- **Контракт цены схватки с боссом стоит на герое без критов** (см. раздел 11).
- **Коридор доли автоатаки в бюджете силы** остаётся в режиме отчёта: замер
  28.7 % при поле 30 %, ручки для этого нет.
- **Частичный штраф золота за отставание** по-прежнему лежит предложением
  рядом с `XP_GAP_PENALTY` и не применён.

## 11. Что осталось открытым

1. **Разброс TTK по лестнице сузить нечем изнутри стадии.** Замер после
   спавна вокруг уровня героя — 46.3 % при потолке 50 %, то есть 93 % потолка.
   Внутризонная часть, ради которой стадия и затевалась, теперь мала (2.4-19.8 %,
   и почти вся — в первой зоне, где эталонный герой не переодевается вовсе);
   остаток — это 12.1 с на первом уровне против 8.3 с на девяносто четвёртом,
   то есть бюджет силы за сотню уровней против кривой здоровья мобов. Ручка
   одна — поднять здоровье мобов на верхних уровнях, а это контракт темпа
   (TTK 8-15 с) с другого конца. Решение владельца: оставить 0.5 как есть или
   тронуть кривую HP и пересчитать темп.

2. **Часы полного пути двугорбы, и трёх сидов на них не хватает.** Шесть сидов
   из девяти дают 11.4-12.8 часа, три — 18.1-19.3; какой горб выпадет, решает
   ранняя рулетка находок. `RUN_SEEDS` — три сида, и в эту ночь два из трёх
   легли в медленный горб: медиана трёх прочитала 18.13 ч против 12.20 по
   девяти. Контракт пути от этого не пострадал — он стоит на убийствах (разброс
   1.1 % по девяти сидам), — но печатаемая строка часов вводит в заблуждение
   любого, кто сравнит её с прошлой ночью. Варианты: девять сидов в ночном
   прогоне (втрое дороже самого дорогого теста матрицы), печатать вместе с
   медианой весь список, или найти и убрать сам источник двугорбости. Что
   именно её создаёт — не выяснено: убийства при этом почти не меняются, то
   есть медленный горб тратит время НЕ на бои.

3. **Контракт цены схватки с боссом стоит на герое без критов.** Тест данжей
   гоняет тик с `rng = () => 0.5`, а при постоянной половине бросок крита
   (`rng() < critChance`) не срабатывает НИКОГДА. Замер на настоящих сидах:
   схватки стоят 7–66 % запаса вместо документированных 69–89 %, привалов
   между боссами то один, то два, то ни одного. Прибор стабилен и
   воспроизводим — но мерит он героя, которого в игре не бывает. Починка
   тянет за собой пересчёт всего контракта «схватка ≈ 80 % запаса» и
   множителей боссов, поэтому внутрь маленькой стадии не помещается.
   Решение за владельцем: оставить как есть (стабильно, но занижено) или
   перевести на медиану трёх сидов и пересчитать числа боссов.
