# Аудит кода

Ночь читает, мерит и пишет. Игру не меняет: правка, двигающая golden, — это
изменение игры, и ей место в отчёте, а не в этом PR.

**Базовая линия ночи: golden `326c88bc2abdaad99074efd23eb634a3`.** Проверяется
после каждого коммита; ни в одной стадии он не двинулся.

Два списка находок — ПОДТВЕРЖДЕНО и ПРЕДПОЛАГАЕТСЯ — в стадии 9. Смешивать их
нельзя: подтверждённая находка проверена попыткой опровержения, предполагаемая
не проверена ничем, кроме чтения.

Отчёт пишется ПО ХОДУ, стадия за стадией.

---

## Стадия 1. Замеры, к которым всё привязано

Ни строки кода не изменено: `git diff` пуст, кроме этого файла. Все числа сняты
на четырёх ядрах, машина под замером ничем другим не занята.

### Размер сборки

Вся папка `dist` — **1.4 МБ**.

| кусок | сырой | gzip |
| --- | ---: | ---: |
| `assets/index-*.js` — основной | **822 КБ** | **289 КБ** |
| `assets/index-*.css` | 73 КБ | 12 КБ |
| пресеты `rich`/`mid`/`tree`/`fresh` (ленивые) | 2.7–6.7 КБ каждый | 1.2–1.9 КБ |
| `index.html` | 892 Б | 561 Б |

Плюс ассеты: шрифты 72 КБ (два `woff2`), спрайты 68 КБ (17 SVG), звук —
36 файлов `ogg`.

**Основной кусок занял 91 % порога CI** (`LIMIT=900000` в `ci.yml:122`).
Порог поставлен после удаления трёхмерного слоя, чтобы библиотека размером с
игру не проехала обратно; запаса осталось 78 КБ.

**Что в этот кусок попало зря:** `src/main.ts:4-5` импортирует
`BalancePage.svelte` и `ShowcasePage.svelte` СТАТИЧЕСКИ, а обе страницы
открываются только по `?debug=1`. `BalancePage` тянет за собой
`game/simulate.ts` — весь headless-прогон на 1388 строк. Замер выигрыша от
ленивого импорта — стадия 6.

### Тестовые тиры — фактическое время

| тир | команда | замер | что обещает документация |
| --- | --- | ---: | --- |
| сборка | `npm run build` | **3.4 с** | — |
| проверка контента | `npm run content:check` | **3.2 с** | — |
| быстрый набор | `npm run test` | **99 с** | CLAUDE.md «около минуты», README «~30 с» |
| проба баланса | `npm run test:balance:sample` | **18.3 мин** | CLAUDE.md «~3.5 мин», README «~2 мин» |
| визуальная регрессия | `npm run test:visual` | **7.8 мин** | CLAUDE.md «~6 мин» |

Полная матрица (`npm run test:balance`, обещано ~11 мин) в ночь не
прогонялась целиком: проба, которая должна идти вчетверо быстрее, взяла
18.3 минуты, то есть матрица заведомо выходит за час, отведённый на стадию.
Число остаётся неизмеренным, и это записано, а не замазано.

**ПРОБА ИДЁТ ВПЯТЕРО ДОЛЬШЕ ОБЕЩАННОГО, И ЭТО НЕ ЗАНЯТАЯ МАШИНА.** Первый
замер (18.5 мин) я отбросил как испорченный своими же фоновыми скриптами и
прогнал заново на пустой машине — вышло 18.3. Причина названа в самом
проекте: замеры CLAUDE.md сняты на ЧЕТЫРЁХ ядрах, а `maxWorkers: 4`
(`vitest.balance.config.ts:87`) на машине аудита раскладывается на два
свободных. Обещание в документе — не «сколько идёт проба», а «сколько она
шла на той машине», и читатель этого не знает.

**Быстрый набор был 99 с и стал 30 с** после переезда одного файла в дорогой
набор (стадия 2, нарушение 3): `dungeon-ladder.test.ts` в одиночку стоил
85 с. Обещание «около минуты» стало правдой впервые за долгое время.

### Время игрового тика

Тысяча шагов по `STEP_MS = 100` на четырёх состояниях, после прогрева JIT.
Бюджет шага — 100 мс; бюджет кадра — 16 мс.

| состояние | медиана | p95 | p99 | худший |
| --- | ---: | ---: | ---: | ---: |
| первый уровень | 0.091 мс | 0.452 | 0.642 | 1.86 |
| середина (`mid`) | 0.028 мс | 0.221 | 0.355 | 4.16 |
| поздний (`rich`) | 0.139 мс | 0.229 | 0.373 | 0.89 |
| дерево (`tree`) | 0.137 мс | 0.232 | 0.383 | 0.86 |

**Вердикт: запаса в 24 раза даже по худшему тику.** Худшие 4.2 мс на `mid` —
одиночный выброс (сборка мусора либо взятие уровня), в кадр 16 мс он входит.

### Догон оффлайна: синхронный блок

Вкладка на это время замирает — считает `applyOfflineProgress` одним куском.

| простой | блок |
| ---: | ---: |
| 1 ч | 132 мс |
| 6 ч | 547 мс |
| 12 ч | **664 мс** |
| 24 ч | 662 мс |
| год | 625 мс |

`OFFLINE_CAP_HOURS = 8`, поэтому всё, что дольше восьми часов, считается
одинаково: переполнения и зависания нет. **664 мс — это заметная пауза при
открытии вкладки после ночи**, и она приходится ровно на момент, когда игрок
ждёт экрана.

### Файлы, строки, экспорты

| каталог | файлов | строк |
| --- | ---: | ---: |
| `src/lib/game` | 91 | 29 381 |
| `src/lib/data` | 31 | 14 231 |
| `src/lib/ui` | 91 | 13 812 |
| `src/lib/stores` | 6 | 1 801 |
| `src/lib/render2d` | 5 | 1 494 |
| `src/lib/audio` | 6 | 945 |
| `src/lib/types` | 1 | 222 |
| `tests` | 11 | 2 481 |
| `scripts` | 2 | 161 |
| **всего** | **248** | **65 127** |

**Экспортов без единого употребления вне своего файла: 171.** Из них
**19 не употребляются вообще нигде** — ни в другом файле, ни в своём. Каждый
проверен отдельным `grep` по всему дереву: ровно одно вхождение, само
объявление. Разбор — стадия 6.

### Зависимости

Одна runtime-зависимость (`break_infinity.js`) и восемь инструментальных. Ни
одной неиспользуемой: каждая импортируется кодом или конфигурацией.

### Ошибки консоли и первый кадр

Собранная игра открыта в Chromium с перехватом `console` и `pageerror`,
двадцать секунд живого тика:

| замер | значение |
| --- | ---: |
| ошибок и предупреждений в консоли | **0** |
| `first-paint` | 40 мс |
| `first-contentful-paint` | 164 мс |
| до `data-ready="game"` (игра запущена) | 173 мс |

Ноль — это не «не смотрели»: перехват ловит и `warning`, и необработанное
исключение страницы. Шрифты лежат локально, из сети не тянется ничего, и
пустой консоли есть чем быть.

### Что уже видно из замеров

- **Тик не является проблемой.** Медиана 0.14 мс при бюджете шага 100 мс.
  Из них на арифметику `Decimal` приходится **≈6 мкс** (56 вызовов методов на
  тик × ~100 нс), то есть **4 %**; иммутабельная копия состояния (64 поля)
  стоит 189 нс. Разгонять тут нечего — и это ответ на вопрос, который иначе
  задавали бы каждую ночь.
- **Цена одной операции** (2 млн итераций): `Decimal.plus` — **157.5 нс**,
  сложение чисел — **2.6 нс** (в 60 раз дешевле), `Decimal.toNumber` —
  15.8 нс, `new Decimal(number)` — 55.7 нс. Число само по себе ни о чём не
  говорит: важно, что таких вызовов 56 на тик, а не 56 тысяч.
- **Сейв не растёт.** 1.9 КБ у свежего героя, 9.8 КБ после часа игры, 12.6 КБ
  после десяти часов. Растёт он сумкой (потолок 24 предмета) и хвостом лога
  (`COMBAT_LOG_SIZE`), оба ограничены, поэтому за сто часов будет то же
  число. Отдельного потолка сейву не нужно.

---

## Стадия 3. Детерминированность и время

### Math.random и часы

| что искали | где нашли | вердикт |
| --- | --- | --- |
| `Math.random` в `src/lib/game` | нигде | **соблюдается** |
| `Math.random` в остальном коде | `render2d/Scene2D.svelte:248,268` | **осознанное исключение**, названное комментарием на месте: дрожание всплывающих чисел — это вид, а не игра, и брать его из игрового потока нельзя, иначе картинка сдвинет бой |
| `Date.now` в `src/lib/game` | `save.ts:1807,1892,2013`, `rng.ts:29` | **соблюдается**: везде как ДЕФОЛТ инжектируемого параметра (`deps.now ?? Date.now`), тесты подставляют своё |
| `performance.now` в `src/lib/game` | `loop.ts:37` | то же: `opts.now ?? (() => performance.now())` |
| часы вне `game/` | `stores/game.ts:170` (инжектируемый `clock`), `stores/telemetry.ts`, `ui/DebugPanel.svelte`, `render2d`, `audio` | вне запрета: правило написано про `src/lib/game` |

**Ни одного нарушения.** Правило «случайность только через `rng.ts`» держится
grep-тестом `game/__tests__/rules.test.ts`, и он не обманывает: я проверил,
что запрет распространяется ровно на `src/lib/game`, а `Scene2D` в него не
входит по построению.

### Воспроизводимость

Два прогона одного сида по 36 000 тиков (час игры), отпечаток — md5 полного
payload сейва:

```
прогон 1: 37795c94892e2c6a4d1420d5107ab11a
прогон 2: 37795c94892e2c6a4d1420d5107ab11a
```

Сид +1 даёт другой отпечаток — то есть проверка не пустая. Сейв→загрузка без
хода времени даёт **тот же отпечаток**: круг «сохранил и прочитал» ничего не
теряет и не добавляет.

### Дельта времени: назад, вперёд и очень далеко вперёд

| дельта | что происходит | вердикт |
| --- | --- | --- |
| −1 мс | `applyOfflineProgress` возвращает состояние БЕЗ КОПИИ (тот же объект), отчёта нет | правильно |
| −1 час (часы перевели) | то же | правильно |
| −30 суток (сон машины, рассинхрон) | то же | правильно |
| +12 ч | начисляется 8 ч (`OFFLINE_CAP_HOURS`), блок 664 мс | правильно |
| +1 год | начисляется ровно столько же, что и за 8 ч, блок 625 мс | **переполнения и зависания нет** |

Классической беды idle-игр — «часы ушли назад, и больше никогда ничего не
начисляется» — здесь нет: `lastTimestamp` перезапишется ближайшим
сохранением, и это прямо прокомментировано в `save.ts:1921`.

### Позиция RNG не в сейве — известный долг, теперь с числом

Поток случайности начинается заново при каждой загрузке: в сейве лежит сид,
но не позиция. Замер: герой играет час, дальше два будущих по часу —
без перезагрузки и через сейв.

| величина | без перезагрузки | через сейв | расхождение |
| --- | ---: | ---: | ---: |
| золото | 28 614 | 30 784 | **+7.6 %** |
| уровень | 16 | 16 | 0 |
| находок в сумке | 24 | 24 | 0 |

**Расхождение в пределах обычного везения**, уровень и добыча не двигаются.

**Гипотеза «удачу можно доить перезагрузками» ОПРОВЕРГНУТА замером.** Если бы
начало потока было выгодным, частая перезагрузка приносила бы больше. Три
сида, час игры каждый:

| сид | без перезагрузок | каждые 5 мин | каждую минуту | каждые 10 с |
| ---: | ---: | ---: | ---: | ---: |
| 4242 | 10 458 | −15.1 % | −35.4 % | **−97.2 %** |
| 777 | 13 545 | −11.1 % | −14.7 % | **−97.9 %** |
| 31337 | 14 463 | −28.2 % | −27.3 % | **−97.6 %** |

Перезагрузка не доит удачу, а **отнимает незаконченный бой**: `payloadFromState`
не кладёт в сейв про моба НИЧЕГО (проверено обходом функции), и при загрузке
он спавнится заново, полным. Это ровно то, что CLAUDE.md обещает про метки
(«после загрузки нет ни того моба, ни той секунды боя»), — просто следствие
измерено. При перезагрузке раз в десять секунд герой не доводит до конца
почти ни одной схватки и застревает на пятом уровне вместо тринадцатого.

---

## Стадия 4. Сейв, миграции и отказы

### Путь миграции

`SAVE_VERSION = 30`. Образцов сейва в `__fixtures__` — 31 (v0…v29 плюс
`v20-temple-cleared`). **Версий без образца нет ни одной.** Каждый образец я
провёл через `readSave` → `stateFromPayload` → один тик: все 31 доезжают до
текущей версии и переживают тик живой игры.

### Испорченный сейв: что видит игрок

Проверено через `loadGame` — так, как это видит игра: из хранилища приходит
СТРОКА. Шестнадцать видов порчи.

| что подсунули | что игра сделала |
| --- | --- |
| обрезанная строка (половина JSON) | отказ `corrupt`, **копия сохранена** |
| пустая строка | отказ `corrupt`, копия сохранена |
| не JSON вовсе | отказ `corrupt`, копия сохранена |
| чужой JSON | отказ `corrupt`, копия сохранена |
| сейв из будущей версии (35 и 999) | отказ `newer-version`, копия сохранена |
| версия строкой / дробная | отказ `corrupt`, копия сохранена |
| массив, `null` | отказ `corrupt`, копия сохранена |

**Ни одного белого экрана, ни одного исключения, и на каждой ветке отказа
делается копия** — до того, как игрок сможет её затереть (`save.ts:1901`).
Это редкость: обычно ровно здесь idle-игры и теряют прогресс.

### Единственная дыра: испорченное ПОЛЕ, а не сейв целиком

Сейв с правильным конвертом и битым полем проходит проверку. Герой
тринадцатого уровня, час игры:

| поле испорчено | что стало |
| --- | --- |
| `gold: "NaN"` | золото 0, **NaN никуда не протёк** |
| `gold: "abc"` / `null` | золото 0 |
| `currentHp: "NaN"` | здоровье пересчитано, NaN не протёк |
| ранг таланта `NaN` / 999 / несуществующий талант | молча отброшено |
| `unlockedZoneIds: []` | зоны потеряны молча |
| `classId` несуществующего класса | принят |
| `level: "абв"` | **герой стал 4-го уровня вместо 13-го** |
| `level: "0"` | то же, 4-й |
| `level: "1e9"` | герой сотого уровня, 14 млн здоровья (упёрся в `LEVEL_CAP`) |

Хорошая новость: **NaN не заражает состояние ни в одном случае** — это
проверялось отдельно, сотней тиков после загрузки, и золото/здоровье/уровень
остались числами.

Плохая: **на этой ветке копия НЕ делается** (она делается только при отказе),
и ближайшее автосохранение затирает оригинал. Испорченный байт в одном поле
= тихая потеря прогресса без следа. Разбор и цена — стадия 9, находка П-2.

### Хранилище недоступно

| случай | ответ игры |
| --- | --- |
| `localStorage` бросает (приватный режим, запрет) | `saveGame → write-failed`, `loadGame → storage-unavailable` |
| квота исчерпана | `saveGame → quota-exceeded` (отдельный код!) |
| хранилища нет вовсе | `storage-unavailable` |

Три разных кода на три разных беды — то есть UI может сказать игроку, что
именно случилось, а не «что-то пошло не так».

### Рост сейва

| момент | размер |
| --- | ---: |
| свежий герой | 1 902 байт (base64 2 536) |
| час игры | 9 815 байт |
| десять часов | 12 593 байт |

Растёт сумкой (24 места) и хвостом лога — обе величины ограничены, поэтому
за сто часов останется около 13 КБ. **Потолка сейву не нужно.**

### Откат на предыдущую версию игры

Сейв версии 30, открытый сборкой со `SAVE_VERSION = 29`, получит отказ
`newer-version` и **копию** — прогресс не стирается, игрок увидит внятное
сообщение. Это лучший из возможных исходов, и он проверен образцом
«сейв из будущей версии».

---

## Стадия 8. Документация против кода

Правка документации — разрешённая правка аудита, и каждая ниже отдельной
строкой. Устаревшее ПОМЕЧЕНО, а не вычищено: по нему видно, откуда игра
пришла.

### Три документа в корне, о которых CLAUDE.md говорит, что их нет

`CLAUDE.md:129` (раздел «Архитектура») утверждает: *«Отдельного
docs/ARCHITECTURE.md в проекте нет: архитектура описана здесь, и второй копии
заводить не надо — она разъедется»*. Копия есть — **`ARCHITECTURE.md` в
корне на 682 строки**, плюс `PLAN.md` (278) и `AUDIT.md` (1231). Буква
правила соблюдена (в `docs/` их правда нет), а предсказание сбылось
буквально: копия разъехалась. Ниже — чем именно.

### ARCHITECTURE.md: что оказалось неправдой

| утверждение | что на самом деле | свидетельство |
| --- | --- | --- |
| § 5 «Существенных расхождений нет… автонадевание при полной сумке не срабатывает» | автонадевания в игре НЕТ вовсе — снесено ещё в итерации 5, о чём сказано в § 0 ЭТОГО ЖЕ файла: документ противоречит сам себе | `game/loot.ts` — ни одной автоэкипировки |
| долг № 4 «Полный инвентарь глушит дроп целиком: с 12/12 герой перестаёт находить» | при полной сумке `stashLoot` продаёт, распыляет или вытесняет худшее; сумка выросла до 24 лестницей покупок | `game/loot.ts:390` |
| долг № 5 «Оффлайн-агрегат не учитывает лут» | учитывает: внутри шага крутится та же `rollLoot` и та же `stashLoot` | `game/save.ts:1744,1755` |
| § 0 «попытка [храма] одна в сутки» | кулдауна у храма нет вовсе — ни строки в `game/temple.ts` и `data/temple.ts` | grep по обоим файлам пуст |
| § 1 «на десктопе бой — три колонки (герой / сцена / кнопки умений)» | постоянная зона на три полосы, кнопки по бокам сцены, восемь меню | `App.svelte`, CLAUDE.md «Экран и меню» |
| § 0 «шесть веток талантов по 61 очку» | три ветки на класс по 27 талантов, ёмкость 115–118 при 91 очке | `data/talents.ts` |
| § 4c замеры тиров | см. таблицу замеров стадии 1: расходятся все | — |

**Долг № 2 (позиция RNG не в сейве) — единственный, который подтвердился**, и
аудит добавил к нему число: 7.6 % золота за час.

**Правка:** в шапку файла добавлено предупреждение, что документ устарел
частями и правда о правилах живёт в `CLAUDE.md`; § 5 зачёркнут с разбором;
долги 4 и 5 помечены закрытыми со ссылками на код; к долгу 2 приписан замер.

### PLAN.md: чеклист описывает игру, которой нет

Пункты 6 («4 зоны», «честный прогноз опасности» — прогноз игроку больше не
показывается), 7 («автонадевание по урону в секунду») и 11 («2 ветки по 4»)
описывают снесённое. Пункт 8 в том же списке уже был помечен курсивом как
история — образец нашёлся в самом файле.

**Правка:** пункты 6, 7 и 11 помечены курсивом тем же способом, что и 8;
в шапку добавлено, что чеклист — история, а не текущее состояние.

### README и CREDITS

**CREDITS сверен с фактическим составом ассетов и сходится ПОЛНОСТЬЮ.**
Это стоит записать отдельно: обычно расходится именно он.

| что заявлено | что в коде |
| --- | --- |
| «Всего иконок в игре: 222» | 222 записи с `author:` в `ui/icons/manifest.ts` |
| Lorc 157, Delapouite 54, Sbed 5, Skoll 2, четверо по одной | ровно те же числа |
| «Все шестнадцать картинок в `public/sprites/`» | 16 файлов |
| Kenney UI + Digital Audio, лицензии рядом | 36 `ogg`, оба файла лицензий на месте |

README расходится только временем тиров (см. стадию 1) — правится там же.

---

## Стадия 5. Горячий цикл, Decimal и реактивность

Каждое утверждение — с замером. Прилагательных в этом разделе нет.

### Тик: где на самом деле время

| что | сколько |
| --- | ---: |
| тик целиком (`rich`, медиана) | **139 мкс** |
| из них арифметика `Decimal` | ≈ 6 мкс (**4 %**) |
| одна копия состояния `{...state}` (64 поля) | 0.19 мкс |
| бюджет шага | 100 000 мкс |

Вызовов методов `Decimal` на тик — **56** (замер счётчиком на прототипе,
1000 тиков пресета `rich`): `lt` 12.9, `toNumber` 9.9, `times` 8.2, `gt` 7.0,
`plus` 5.3, остальные меньше. При цене `plus` в 157 нс и сравнения дешевле
это и даёт 6 мкс.

**Вывод: разгонять тик нечего, и Decimal тут ни при чём.** Даже если заменить
его целиком на обычные числа, тик станет быстрее на 4 % — при запасе в 700 раз.
Эту находку стоит запомнить как НЕ-находку: её легко «увидеть» чтением и
потратить ночь.

### Decimal там, где хватило бы числа

Правило (CLAUDE.md, «Железные правила»): `Decimal` не применяется к
вероятностям, весам, **миллисекундам** и служебным счётчикам.

**Нарушение одно: `playtimeMs`** — игровое время в миллисекундах лежит
`Decimal`-ом (`state.ts:506`, в сейве строкой `save.ts:423`), и КАЖДЫЙ
потребитель тут же зовёт `.toNumber()`:

| место | что делает |
| --- | --- |
| `game/tick.ts:340` | `timestamp: swung.playtimeMs.toNumber()` — на каждом ударе |
| `game/abilities.ts:313,420` | то же на каждом применении умения |
| `stores/game.ts:243,649` | начало сессии |
| `ui/DebugPanel.svelte:52` | показ времени сессии |

Верхняя граница величины — не бесконечность: сто лет игры это 3.15e12 мс,
на три порядка меньше предела точного целого в double. **Decimal здесь не
нужен ни на одном горизонте.** Цена правки — миграция сейва (поле хранится
строкой), поэтому это находка, а не ночная правка.

### Реактивность: что пересчитывается на каждом тике

`gameState` обновляется РОВНО раз в шаг (`stores/game.ts:260`, внутри
`step()`), то есть 10 раз в секунду. Всё, что подписано, пересчитывается
столько же раз.

- **`BattlePanel.svelte:23-24` зовёт `estimateCombatRate` дважды** (`auto` и
  `manual`) в `$derived`. Выглядит дорого — **и не дорого**: у функции есть
  мемоизация с ключом по `state.stats` (WeakMap) плюс строка из тождеств
  объектов (`combat.ts:948`). Ключ НЕ включает текущее HP моба — только его
  уровень, скорость и максимумы, — поэтому внутри боя это попадание в кеш, а
  не пересчёт. Тождества берутся `WeakMap`-ом за постоянное время. **Не
  находка, проверено чтением ключа.**
- Панель к тому же смонтирована только в текстовом режиме и в сводке под
  открытым меню на узком экране (`App.svelte:183,197`) — на обычном десктопе
  её нет вовсе.
- `ensureStats` (`stats.ts:330`) защищён флагом `statsDirty`: пересчёта
  статов на тике нет, если ничего не менялось.

### Сцена: 10 кадров в секунду, и это видно

**`Scene2D` не имеет ни одного `requestAnimationFrame`** — и это ПРАВИЛО,
закреплённое тестом (`render2d.test.ts:64` запрещает таймеры в сцене).
Единственный источник перерисовки — подписка на `gameState`
(`Scene2D.svelte:209`), то есть **10 раз в секунду**.

Отсюда числа:

| эффект | длительность | сколько кадров получит |
| --- | ---: | ---: |
| всплывающее число (`FLOATER_LIFE_MS`) | 900 мс | **9** |
| затухание трупа (`DEATH_FADE_MS`) | 280 мс | **2–3** |
| вспышка попадания (`HIT_FLASH_MS`) | 220 мс | 2 (ей хватает: она двоичная) |

Всплывающее число ведёт `--life`, и от него зависят `bottom` и `opacity`
(`Scene2D.svelte:773-775`). **Ни `transition`, ни `animation` на `.floater`
нет** — значит число не плывёт, а прыгает девятью ступенями. У фигур это
сделано иначе и правильно: у них `transition: transform var(--dur-fast)`
(строка 553), и выпад сглаживается браузером.

**Вторая половина находки: `bottom` — свойство РАСКЛАДКИ, а не композиции.**
Каждое изменение `--life` заставляет браузер пересчитать положение элемента,
тогда как `transform: translateY()` считался бы композитором. Цена мала
(до `FLOATER_LIMIT` = 24 элементов десять раз в секунду), но правило проекта
про сцену звучит как «обычные элементы и CSS-трансформы», и `bottom` из
этого выпадает.

Обе половины — одна правка на пару строк CSS, но она сдвинет эталоны
скриншотов поз, а значит это работа отдельного PR, а не аудита.

### Звук

Голосовой лимит, склейка залпов и вытеснение по важности реализованы и
проверены ЧИСЛАМИ, а не на слух (`audio/mixer.ts:102-115`,
`mixer.test.ts` — двенадцать проверок, включая «одновременно звучит не
больше `SOUND_VOICE_LIMIT`» и «важное вытесняет неважное, а не наоборот»).
**Находок нет.**

### Догон оффлайна одним куском

`applyOfflineProgress` считается **одним синхронным блоком на 664 мс** (12 ч
простоя, см. стадию 1). Внутри он уже разбит на шаги по `OFFLINE_CHUNK_MIN`,
но шаги идут в одном цикле без выхода в событийный цикл, поэтому вкладка
замирает целиком. Игрок видит это как «страница подвисла при открытии».
Разрезать по кадрам — правка на десятки строк с показом прогресса, то есть
отдельный PR; замер для него готов.

---

## Стадия 2. Правила проекта против кода

Самая ценная стадия, потому что критерий здесь объективный: проект сам
записал свои инварианты, и их можно проверить по коду, а не по вкусу.

Разбор идёт по разделам `CLAUDE.md` плюс `ARCHITECTURE.md` и `PLAN.md`.
Каждое правило получает вердикт со свидетельством — файл и строка. Отдельная
колонка «чем держится» отвечает на вопрос, который важнее вердикта: **что
случится, если правило нарушат завтра.** Правило без теста — это правило,
которое держится вниманием, а внимание кончается.

Обозначения: `+` соблюдается, `!` нарушено, `~` соблюдается частично,
`?` автоматически не проверяется.


### Охват: 358 правил, разложенных по разделам

Из `CLAUDE.md` выписаны все утверждения, которые можно проверить по коду —
не пожелания, а инварианты со словами «только», «ровно», «никогда»,
«обязан», «держится тестом». Плюс два корневых документа (`ARCHITECTURE.md`,
`PLAN.md`), которые описывают ту же игру и потому проверяются так же.

| раздел | правил | + | ~ | ! | ? | не держит ничто |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| архитектура-железные | 22 | 17 | 3 | 0 | 2 | 8 |
| стек-команды-тиры | 26 | 22 | 0 | 2 | 2 | 5 |
| классы-статы | 19 | 18 | 1 | 0 | 0 | 2 |
| броня-экипировка | 34 | 30 | 2 | 1 | 1 | 3 |
| две-оси-умения | 26 | 24 | 1 | 1 | 0 | 1 |
| данжи-храм-забеги | 30 | 28 | 2 | 0 | 0 | 4 |
| автокаст-привал | 19 | 18 | 0 | 0 | 1 | 4 |
| таланты | 25 | 21 | 3 | 0 | 1 | 2 |
| зоны-цена-боя | 29 | 25 | 3 | 1 | 0 | 2 |
| дизайн-лестницы-крафт-золото | 27 | 26 | 1 | 0 | 0 | 6 |
| бюджет-прогон | 20 | 16 | 3 | 1 | 0 | 5 |
| экран-меню | 26 | 24 | 2 | 0 | 0 | 4 |
| сцена-снимки-golden | 32 | 29 | 2 | 0 | 1 | 2 |
| наследие-architecture-plan | 23 | 4 | 2 | 16 | 1 | 5 |
| **всего** | **358** | **302** | **25** | **22** | **9** | **53** |
Читать эту таблицу надо с последней колонки, а не с предпоследней. **302
правила из 358 соблюдаются, но 53 не держит ни один тест** — это и есть
главное число стадии. Правило, которое соблюдается и ничем не держится,
отличается от правила, которого нет, только тем, что сегодня оно верно.

Шестнадцать из двадцати двух нарушений сидят в одном разделе —
`наследие-architecture-plan`. Это не код: это два корневых документа,
описывающих игру, которой нет. Разбор — в стадии 8; здесь они посчитаны
честно, но выделять их в находки бессмысленно, находка там одна.

### Нарушения в коде: шесть, и каждое перепроверено вручную

Панель судей отбросила двенадцать заявок из двадцати двух. Ниже — то, что
пережило проверку И что я перепроверил сам, руками, после неё. Каждое
свидетельство ниже я открыл в файле, а не взял из отчёта агента.

**1. Очередь умения переживает смерть моба.** `CLAUDE.md`, раздел «Умения»:
«Очередь и наложенные эффекты живут на КОНКРЕТНОМ мобе: его смерть,
респаун, смерть героя и загрузка сейва их снимают». Три случая из четырёх
закрыты: смерть героя (`game/tick.ts:76`, `:162`, `:774`), загрузка сейва
(`game/save.ts:850`). Смерть моба и респаун — нет: `applyEffects`
(`game/tick.ts:376`) чистит только `activeEffects`, шаг респауна
(`game/tick.ts:884-893`) — `activeEffects`, `monsterWeaken`, `monsterBrand`.
`queuedAbilityId` не трогает ни один из них.

Что это значит в игре: `onNextSwing`, поставленный в очередь против моба A,
дожидается моба B и срабатывает по нему. Мана не теряется — она списывается
в момент удара, — то есть герой получает удар умением бесплатно перенесённым
на следующего противника. Урон от этого не пропадает, а сохраняется, то есть
ошибка играет ЗА игрока и потому незаметна.

Почему это не починено сегодня: одна строка `queuedAbilityId: null` в шаге
респауна меняет поток ударов, а автокаст в golden держит `onNextSwing` в
ротации. Golden сдвинется, а аудит его не двигает. Идёт в план как
отдельный PR с осознанным обновлением эталона.

**2. `GOLD_UPGRADES` — семь сущностей мимо `content:check`.** `CLAUDE.md`:
«ЛЮБАЯ новая сущность… обязана пройти `npm run content:check`… обязательные
`id`/`name`/`icon` с уникальными id». В `data/upgrades.ts` семь покупок, у
каждой `id`, `name`, `icon` (`:36-40` — сам тип, `:61`, `:70`, `:79`… — сами
записи). Схемы для них нет: `grep GOLD_UPGRADES src/lib/data/__tests__/schema.ts`
пуст, а список покрытия в `integrity.test.ts:34-53` перечисляет девятнадцать
файлов и `data/upgrades.ts` среди них не значится. Список ведётся руками —
в комментарии выше это сказано прямо, — и файл просто не дописали.

Проверено на дырке: сегодня дубликат `id`, пустое `name` или ссылка на
несуществующий значок в этом файле не роняют ни одну проверку. Единственное,
что его касается, — `game/unlock-ladder.test.ts:248-252`, и там сверяются
только id самих покупок, не поля.

**3. Дорогое в быстром наборе.** `CLAUDE.md`: «ВСЁ, ЧТО СТРОИТ ЭТАЛОННОЕ
ПРОХОЖДЕНИЕ ИЛИ ГОНЯЕТ СИМУЛЯЦИЮ ПО ЛЕСТНИЦЕ, живёт в дорогом наборе».
Список исключений `test:unit` (`package.json:16`) выбрасывает
`power-budget`, `balance-*`, `run`, `level-gap`, `gold`. А
`dungeon-ladder.test.ts:29` импортирует `buildSimState` и `referenceBuild`
из `simulate.ts` и зовёт их в телах тестов — и в исключениях его нет.
Цифры замера — ниже, в отдельной строке: правило про стоимость, значит
и нарушение измеряется секундами, а не фактом импорта.

**4. Еда даёт силу и не имеет строки в бюджете.** `CLAUDE.md`, «Бюджет
силы»: «НОВАЯ СИСТЕМА, ДОБАВЛЯЮЩАЯ СИЛУ, ОБЯЗАНА ПОЛУЧИТЬ СТРОКУ В
БЮДЖЕТЕ». `data/balance.ts:515` — `REST_FOOD_SPEEDUP = 2`;
`game/rest.ts:30` — `state.restSpeedupSource ? base / REST_FOOD_SPEEDUP : base`.
Привал вдвое короче — это прямая прибавка к аптайму, то есть к убийствам в
час. В `POWER_BUDGET` (`data/balance.ts:815` и ниже) строки про еду нет:
есть таланты, зачарования, зелья, реликвия, каденция, лечение, броня.
Еда — единственный множитель, который проходит мимо прибора.

**5. Проба гоняет три сида, а `CLAUDE.md` обещает один.** Раздел «Чего проба
НЕ ловит»: «**один сид.** Путь зависит от ранней рулетки находок… медиана по
трём сидам считается только ночью». В коде наоборот и с объяснением:
`run.test.ts:63-85` — блок «СИДЫ — ВСЕ ТРИ, ВСЕГДА», `const SEEDS = [...RUN_SEEDS]`,
переключателя `RUN_ALL_SEEDS` больше нет. Правку сделали, а слепое пятно в
документе не сняли: документ пугает дырой, которую закрыли.

**6. Числовые отступы в сцене, и тест туда не смотрит.** `CLAUDE.md`,
«Дизайн-система»: «В компонентах не должно быть ни одного hex-литерала и ни
одного отступа числом — только `var(--…)`. Держится тестом `ui/kit/kit.test.ts`».
`render2d/Scene2D.svelte:685` — `gap: 0.15rem`, `:702` — `gap: 0.3rem`; ни
то, ни другое не лежит на шкале 4/8/12/16/24/32. Тест их не видит:
`kit.test.ts:49` собирает `App.svelte`, `ui/*.svelte` и `ui/kit/*.svelte`,
а `render2d/` в обход не входит. Правило записано про компоненты, сцена —
компоненты; охват сторожа уже, чем правило.

### Что отброшено на проверке

Двенадцать заявок из двадцати двух не пережили попытку опровержения и в
списки находок не попали: `R-ARCH-6` (четыре слушателя Esc — оказалось, три
из четырёх слушают не Esc, а свои клавиши), `R-STAT-5`, `R-AUTO-3`,
`R-RUN-3`, `R-SCENE-13`, `R-SCENE-5` и ещё шесть. Это не потери: это цена
правила «ложная находка дороже пропущенной», и заплачена она здесь.

### Правила, которые не держит ничто

Пятьдесят три. Это не список нарушений — все они сегодня соблюдаются. Это
список того, что сломается молча.

- `R-ARCH-1` — `src/lib/game/` — логика; не знает про Svelte и рендер
- `R-ARCH-2` — Svelte-сторов РОВНО ТРИ: stores/game.ts, stores/ui.ts, stores/telemetry.ts
- `R-ARCH-6` — «Единственный на игру слушатель Esc — тоже здесь [App.svelte]»
- `R-ARCH-7` — Отдельного `docs/ARCHITECTURE.md` в проекте нет
- `R-HARD-3` — `Decimal` обязателен для неограниченно растущих величин (золото, урон, опыт, HP, уровни)
- `R-HARD-12` — Коммиты в стиле Conventional Commits; никогда не коммитить в `main` напрямую — только ве
- `R-HARD-13` — Нетривиальные игровые формулы комментируй одной строкой: что считает и почему
- `R-HARD-14` — Один PR = одна фича; не рефакторить то, о чём не просили
- `R-CMD-1` — Все названные в разделе команды существуют: build, test, test:unit, test:balance, test:b
- `R-CMD-5` — ВСЁ, ЧТО СТРОИТ ЭТАЛОННОЕ ПРОХОЖДЕНИЕ ИЛИ ГОНЯЕТ СИМУЛЯЦИЮ ПО ЛЕСТНИЦЕ, живёт в дорогом 
- `R-CMD-9` — Один уехавший эталон обновляется `npm run screenshots:update:one -- "имя"`, а не полным 
- `R-SAMPLE-1` — BALANCE_SAMPLE=1 сужает ОХВАТ, а не строгость: пороги, сравнения и ассерты те же самые
- `R-PROC-1` — Быстрый набор — минимум перед PR; PR, трогающий data/balance.ts, кривые или формулы боя,
- `R-CLS-3` — `classIt(cls)` из game/__tests__/class-set.ts для `preview` превращает провал в `console
- `R-CLS-4` — Список классов для прогона — `contractClasses(sample)`: готовые первыми, в выборке тольк
- `R-ARM-5` — В знаменателе уровень ГЕРОЯ, а не моба: разница уровней уже учтена levelGapDamageMult и 
- `R-ARM-6` — Цель — 45–55 % снижения полным актуальным комплектом на любом уровне; под неё подобрана 
- `R-EQ-15` — Границы экрана оба всплывающих окна (ItemTip и ItemCompare) считают ОДНОЙ функцией — ui/
- `R-ABIL-15` — Очередь и наложенные эффекты живут на КОНКРЕТНОМ мобе: его смерть, респаун, смерть героя
- `R-DUN-5` — На привале СТОИТ СЧЁТЧИК ЯРОСТИ и не выходит следующий босс.
- `R-DUN-6` — Урон босса падает от первого к третьему (CHAIN: 1.86 → 1.28 → 1.00; тир домножает все тр
- `R-RUN-5` — Модалка возврата НАЗЫВАЕТ вслух: из чего вышел и по какой зоне начислено.
- `R-RUN-6` — При входе в закрытую активность UI предупреждает ОДНОЙ строкой (ui/runText.ts), что закр
- `R-AC-11` — ПОРОГ ЛЕЧЕНИЯ СТОИТ НИЖЕ ПОРОГА ПРИВАЛА (0.55 против 0.6 по умолчанию).
- `R-AC-13` — `estimateCombatRate(state, mode)`: `'auto'` по умолчанию, `'manual'` — что выйдет, если 
- `R-AC-15` — Замер: порог лечения 0.55 снимает 28–33 % простоя на привалах при цели 20–35 %.
- `R-REST-4` — ПОДПИСЬ ПОЛЗУНКА ГОВОРИТ ДЕЙСТВУЮЩУЮ ДЛИНУ ПРИВАЛА (`restDurationMs`), а не константу из
- `R-TAL-7` — Тексты дерева — одним модулем ui/talentText.ts на узел и подсказку; всё словесное живёт 
- `R-TAL-21` — Стрелка ставится ТОЛЬКО там, где талант буквально дорабатывает предыдущий, и не больше т
- `R-ZONE-8` — zoneSpawnVariants(zone, heroLevel) — единственный источник «каких мобов этому герою выда
- `R-ZONE-9` — Опасность зоны НЕ пишется текстом в данных: forecastZone считает вердикт через ту же est
- `R-DS-2` — Тема ОДНА и тёмная; переключалки светлой/тёмной нет и не будет
- `R-DS-4` — Семантика цвета закреплена за токенами (--c-damage/--c-heal/--c-mana/--c-xp/--c-gold/--c
- `R-DS-5` — Примитивы ui/kit/ (Panel, Button, StatBar, IconSlot, Tooltip, NumberText, Tag) НЕ знают 
- `R-DS-6` — Шрифт один — Inter, лежит в public/fonts/ подмножествами кириллицы и латиницы, из сети н
- `R-LAD-7` — ЦЕНА ОКРУГЛЯЕТСЯ ДО ДВУХ ЗНАЧАЩИХ ЦИФР (GOLD_PRICE_SIGNIFICANT_DIGITS в data/upgrades.ts
- `R-LAD-8` — ЦЕНА ВИДНА ВСЕГДА, а не только когда хватает: она на кнопке, и рядом строка «не хватает 
- `R-BUD-2` — Коридоры лежат в `POWER_BUDGET` (`data/balance.ts`), прибор — `game/__tests__/power-budg
- `R-BUD-3` — НОВАЯ СИСТЕМА, ДОБАВЛЯЮЩАЯ СИЛУ, ОБЯЗАНА ПОЛУЧИТЬ СТРОКУ В БЮДЖЕТЕ; не помещается — умен
- `R-SIM-1` — `simulate({hours, zoneId, build})` крутит НАСТОЯЩИЙ конвейер тика; своей модели боя у пр
- `R-SIM-2` — Метрики считаются ТОЛЬКО по наблюдаемому снаружи состоянию (убийство — таймер респауна, 
- `R-SIM-4` — Автонадевание в прогоне выключено — иначе билд подменится на середине
- `R-SCR-1` — Вердикт зоны (forecastZone: по силам / риск / смерть / не по зубам) в интерфейсе НЕ пока
- `R-SCR-2` — Прогноза схватки с боссом до входа в данж нет: ограничения по уровню, зоне, смерти и «уж
- `R-SCR-15` — Состояние ротации кнопка ПОКАЗЫВАЕТ, но не заводит: «включён» — когда автокаст стоит хот
- `R-SCR-20` — Столбцы кнопок ЛИПНУТ К ВЕРХУ окна на десктопе.
- `R-2D-10` — Под prefers-reduced-motion тряска и сдвиги обнуляются, вспышки и числа остаются
- `R-GOLDEN-2` — Упал golden — рефакторинг изменил поведение, чини код; осознанное изменение баланса = уд
- `R-LEG-1` — Карта модулей раздела 1: все перечисленные файлы (`game/*`, `audio/*`, `data/*`, `stores
- `R-LEG-2` — «На десктопе бой — три колонки (герой / сцена / кнопки умений), на мобильном сцена подни
- `R-LEG-13` — Порядок конвейера тика раздела 3: шаг 17 — «`applyTempleWave` — волна храма засчитана; с
- `R-LEG-20` — Чеклист дорожной карты: пункты, описывающие то, чего в игре нет, ПОМЕЧЕНЫ курсивом прямо
- `R-LEG-23` — «Разрыв режимов — цель баланса, а не множитель в коде: ручная игра с зельями ≈ 1.5 × авт
---

