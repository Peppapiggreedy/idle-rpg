# Псарь: два тела

Отчёт ночи о третьем классе. Пишется ПО ХОДУ, стадия за стадией: что сделано,
что замерено, что не сошлось. Правило ночи — **ключи Стража и Изувера в
отпечатке не двигаются; у Псаря ключей раньше не было — они появляются**.
Проверяется матрицей и `npm run balance:diff` в стадии 9.

## Что было красным ДО ночи

Полная матрица на `main` (`2cf5221`) даёт три невыполненных контракта, и все
три — Изувера, на переходе снаряжения около шестого уровня:

| контракт | замер | коридор |
| --- | ---: | ---: |
| темп TTK, уровень 6 / hollow-quarry | 7.36 с | ≥ 8 |
| цена боя валово, уровень 6 | 8.75 % | ≥ 9 |
| разброс темпа `ttk-drift`, уровни 1..6 | 0.838 | ≤ 0.5 |

Причина разобрана в `docs/NIGHT-CRAFTUI-REAVER.md` («третья запись НЕ
починено»): эталонный герой переодевается разом на первом переезде, и ступень
темпа сидит в приборе, а не в классе. **В эту ночь эти три не чинятся** — ни
одной правки в общий код ради них. В стадии 9 матрица делится на две таблицы:
УНАСЛЕДОВАННОЕ КРАСНОЕ (эти три, «было до ночи») и НОВОЕ КРАСНОЕ.

## Стадия 1. Класс и энергия

### Класс — заполненная запись, а не механизм

`ClassDef` уже описан полями; третий класс — строка в `CLASSES`
(`data/classes.ts`): `id: 'houndmaster'`, имя «Псарь», `status: 'preview'`,
`treeRevision: 1`, род ресурса `'energy'`. `ResourceKind` стал объединением из
трёх литералов. Grep по `'mana'` и `'rage'` вне данных дал ровно два места,
и оба — интерфейс: словарь `ui/resource.ts` (слово и тон полоски) и объединение
тонов у `StatBar`/`NumberText`. Логика ни одного из литералов не читает — она
читает числа `ResourceDef` и статы, поэтому энергия молча в `else` уйти не
могла: `else` там нет.

### Энергия — через baseMods, НОВОГО ПОЛЯ НЕ НУЖНО

Первым действием проверено, хватает ли механизма Изувера. Хватает:

| требование | чем выражено | почему держится |
| --- | --- | --- |
| запас ровно 100 | `maxMana base 100` | предметы `maxMana` не несут; интеллект погашен |
| не зависит от характеристик | `intellect multiplier 0` | вклад интеллекта в запас И в восстановление считается от итога интеллекта, а итог — ноль |
| не зависит от уровня | то же | плоская прибавка уровня к восстановлению идёт ЧЕРЕЗ интеллект (`PER_LEVEL_ATTRIBUTES` → `INT_MANA_REGEN`), а не в `manaRegen` напрямую |
| восстанавливается быстро, в бою и вне его | `manaRegen base 12` | `applyRegen` идёт в бою и в паузе одинаково; 100 за 8.3 с |
| без паузы после траты | `regenDelay base 0` | |
| не копится боем, не тает | `perSwingDealt/perHitTaken/decayShare = 0` | |

`restRefill: true` — привал кончается полной полоской, но восстанавливает
энергию не он: за несколько секунд отдыха регенерация и так дошла бы до
полного. Подпирать регенерацию привалом нельзя (это контракт привала), и здесь
этого нет — флаг только записывает правду одним битом.

Порции `REGEN_TICK_S = 2` остались: полоска возвращается рывками по 24, и это
читается как «быстро», а не как ручеёк. Модель боя читает тот же реген:
`resourceIncome` для ресурса без дохода от ударов = `stats.manaRegen`,
`resourcePause` = 0 + полпорции.

Держится `classes.test.ts`: запас 100 и полный на старте; с нуля до полного за
десять секунд В БОЮ; шапка на +500 интеллекта и шестидесятый уровень не
двигают ни запас, ни реген (та же шапка на Страже двигает оба — тест
различает классы); свой и чужой удар не дают; вне боя не тает; модель читает
`manaRegen`.

### Первая кнопка и первый этаж — потому что этого требуют проверки

Быстрый набор не принимает класс без умений и без веток: `classes.test.ts` и
`content:check` требуют непустые `abilityIds` и `branchIds`, `BRANCH_SCHEMA`
— хотя бы один талант в ветке, `talents.test.ts` — по три ветки на класс со
стилями `damage/survival/autonomy`. Поэтому вместе с классом заведены:

- **Подсечка** (`undercut`, уровень 1): 20 энергии, откат 2 с, 1.5 удара
  оружия. Заполнитель — единственное допустимое совпадение роли у трёх
  классов; механизм всё равно свой (см. комментарий в `data/abilities.ts`).
- Три ветки — **Гон**, **Привязь**, **Тропа** — с ПЕРВЫМ этажом каждая: по два
  таланта на базовые числа (ускорение и урон Подсечки; здоровье и его
  восстановление; скорость энергии и длина привала). Остальные двенадцать
  этажей приходят стадиями 6–8.

### Два временных исключения в тестах — и где они снимаются

Проверки ФОРМЫ полного класса не отличают «класс строится» от «класс сломан».
Чтобы стадии шли отдельными зелёными коммитами, два теста получили
поимённое исключение `UNDER_CONSTRUCTION = {'houndmaster'}`:

| тест | что проверяет | снимается |
| --- | --- | --- |
| `abilities.test.ts` «у каждого класса ≥ 3 умений» | наполнение набора | стадия 5 |
| `talents.test.ts` форма ветки (13 этажей, ёмкость ≥ 60, повороты на 5/9/13) | форма дерева | стадия 8 |

В `main` без снятия они не уходят: это записано в самих тестах.

### Сейв: 33 → 34, форма прежняя

Миграция меняет только номер, и нужна она ради ЧТЕНИЯ В ОБРАТНУЮ СТОРОНУ:
сейв Псаря, открытый сборкой без Псаря, деградировал бы до Стража молча
(`classById` отдаёт класс по умолчанию на неизвестный id). Сборка без
третьего класса не знает и 34-й версии — она откажет кодом `newer-version`,
положив исходную строку в запасную копию. Тест: сейвы Стража и Изувера
проходят миграцию поле в поле. Фикстура `save-v33.json` добавлена, пресеты
скриншотов перегенерированы (`npm run presets:update`) — в них поднялся
только номер версии.

### Интерфейс

- Полоска энергии — свой цвет `--c-energy` (жёлто-оливковый), третий тон у
  `StatBar` и `NumberText`, строка в витрине `/ui`.
- Кнопка выбора класса склоняла имя приклеенным «а»: третий класс читался бы
  как «Псарьа». Склонение вынесено в `ui/classText.ts` (мягкий знак → «я»),
  с тестом по всем классам.
- Иконки: `class-houndmaster`, `ability-undercut`, шесть талантов первого
  этажа — все с game-icons.net, спрайт пересобран.

### Попутные находки

- **Счётчик иконок в CREDITS.md был устаревшим**: стояло 243 (Lorc 172,
  Delapouite 60, Skoll 2), в реестре до ночи было 251 (Lorc 178, Delapouite
  61, Skoll 3). Исправлено по факту: теперь 259 (Lorc 184, Delapouite 63,
  Skoll 3). Правка — минуты, записана как (а).
- Сторож `talents.test.ts` для флагов сравнивает ТОЛЬКО пару Страж–Изувер
  (`flagsOf('warden')`, `flagsOf('reaver')`). С третьим классом правило
  «флаги из одного набора, наборы не совпадают» надо переписать по всем
  парам — сделано будет в стадии 8, когда у Псаря появятся флаги.

### Проверки стадии

`npm run test`: 77 файлов, 1482 теста, зелёный.

## Стадия 2. Пёс в бою

### Что такое пёс в логике

Второй источник автоатаки со своим здоровьем — не эффект и не бафф, но и не
питомец: ни наведения, ни угрозы, ни своего выбора цели, бьёт то же, что
герой. Числа — в данных класса (`CompanionDef` в `data/classes.ts`), и все
они ДОЛИ ОТ СТАТОВ ГЕРОЯ, чтобы пёс не отставал от лестницы предметов:

| поле | Псарь | смысл |
| --- | ---: | --- |
| `maxHpShare` | 0.4 | запас пса — доля запаса героя |
| `hitShare` | 0.55 | укус — доля среднего удара оружия героя; криты — его |
| `swingTime` | 1.5 с | свой таймер замаха |
| `redirectShare` | 0.3 | доля входящего по герою (после его смягчения), уходящая псу |
| `returnSec` | 12 | сколько лежит павший |
| `regenShare` | 0 / 0.04 | доля запаса в секунду в бою / вне боя |
| `count` | 1 | псов на поле; капстоун добавит второго |

Псы хранятся СПИСКОМ `GameState.hounds: HoundState[]` (здоровье, замах,
таймер возврата). У Стража и Изувера список пуст, и каждый шаг тика,
касающийся псов, возвращает состояние тем же объектом.

### Где пёс входит в тик (`game/tick.ts`)

- `applyHoundTimers` — рядом с откатами: лежащий досиживает возврат и
  встаёт полным (`hound-return` в журнал), стоящий восстанавливается по
  ставке из данных.
- `applyHoundCombat` — после обеих рук героя и ДО проков: укус идёт в шину
  с пометкой `companion`, замахом героя не считается (ресурс от него не
  копится, проки не срабатывают), клеймо на цели его множит (общий
  `targetMultiplier`), стойка и разгон героя — нет.
- В `applyMonsterAttack` перенаправление стоит ПОСЛЕДНИМ, после брони,
  блока, стойки, упора и щита: доля уже дошедшего до полоски уходит псу,
  сумма частей равна удару. Пёс с пустым запасом ложится (`hound-down`).
- Воскрешение возвращает псов целыми; привал лечит стоящих на ту же долю,
  что героя, павших не поднимает.

### Модель боя видит пса в обе стороны

`houndModel` в `game/hound.ts` — первый порядок, как все метки в модели:
укусы входят в поток ударов (`hitStream`) после проков и до крита, урон в
секунду пса добавляется поверх урона героя, доля входящего по герою
умножается на `1 − redirect`. Доля «на ногах» — отношение времени до
падения (запас пса против перенаправленного входящего) к циклу «пал →
вернулся». Псы, лежащие ПРЯМО СЕЙЧАС, не считаются вовсе — обе оси героя
проседают, пока пса нет. Ключ кеша оценки получил число псов на поле и на
ногах.

Оси: урон — через `estimateCombatRate`, живучесть — `survival(...,
redirect)`: урон, ушедший псу, до героя не дошёл. У класса без пса
`redirect = 0`, и формула прежняя до бита.

### Проверки (`game/hound.test.ts`, 18 тестов)

- укусы идут по своему таймеру с пометкой спутника; у Стража шина чиста;
  лежащий не кусает;
- сумма урона по герою и псу равна удару, доля — из данных; у Стража удар
  доходит целиком;
- пёс с пустым запасом ложится на `returnSec` и встаёт полным; вне боя
  восстанавливается по ставке;
- пока пёс лежит, падают ОБЕ оси; с псом урон в секунду выше, потеря
  героя ниже; у Стража модель с пустым списком псов равна модели без него
  (`toEqual`);
- **оффлайн ≤ автокаст**: тем же методом, что у Стража (`combat-rate.test.ts`)
  — живая игра на `OFFLINE_EFFICIENCY` часа против часа оффлайна, золото
  расходится не больше 20 % на двух сидах; час оффлайна беднее часа тика;
- псы переживают сохранение: здоровье и таймер возврата читаются обратно,
  сейв без поля даёт свежий комплект, у Стража поле пустое в обе стороны.

Первая редакция теста оффлайна мерила ЧАС тика против часа оффлайна и
получила отношение 0.39: за час герой первого уровня уходит на десяток
уровней вперёд, и сравнивались разные герои. Метод Стража (доля часа)
даёт расхождение в пределах допуска — то есть модель пса верна, а неверен
был замер.

### Сейв: 34 → 35

Поле `hounds` списком; миграция — только номер: отсутствие поля загрузка
читает как свежий комплект, длину списка задают ДАННЫЕ класса, а не сейв
(капстоун мог быть снят). Фикстура `save-v34.json` — снимок `rich.json`
до бампа; пресеты перегенерированы (в них добавилось `"hounds": []`).

### Проверки стадии

`npm run test`: 78 файлов, 1502 теста, зелёный. Полная матрица с
отпечатком запущена после коммита стадии — её разбор ниже, в стадии 9,
вместе с итоговой.

### Матрица после стадии 2: ключи Стража и Изувера на месте

Полная матрица на коммите `d1c2dc8` (пёс в бою; 15 файлов, 215 тестов,
33 минуты) и `node scripts/balance-diff.mjs` против `docs/balance-baseline.json`:

| что | сколько |
| --- | ---: |
| величин в дампе | 2156 |
| ключей Стража и Изувера, изменивших значение | **0** |
| новых ключей (все с `houndmaster` в имени) | 267 |
| расхождений | 1 — `run/seed-4242/classes/total-hours-gap` |

**Находка, предсказанная заданием:** `run/seed-*/classes/total-hours-gap`
считается в `run.test.ts` как `(max − min) / min` по ВСЕМ классам, а не по
паре Страж–Изувер: с третьим классом в наборе значение уехало с 0.0094 на
1.664 (Псарь в один «Подсечкой» идёт полный путь заметно дольше). Значение
пары Страж–Изувер при этом не изменилось — изменился состав множества. Это
не регрессия баланса, а свойство ключа: он МНОЖЕСТВЕННЫЙ, а не парный.
По правилу ночи общий код не трогается; предложение владельцу — в стадии 9
(разложить ключ на парные `…/gap/<класс-A>-<класс-B>` или писать его только по
готовым классам).

## Стадия 3. Пёс на сцене

### Где стоит и как рисуется

Слой псов — между героем и эффектами (`Scene2D.svelte`, «слой 3½»), и
рисуется он ТОЛЬКО при непустом списке: у Стража и Изувера в разметке не
появляется ни одного узла, их снимки зелёные без перезакладки. Пёс стоит
впереди героя, ближе к мобу (`--hound-x = --hero-x + 14 %`), ростом чуть
меньше половины героя (`--hound-h = 0.42 × --figure-h`), второй пёс
встаёт за первым по `--slot` и на двенадцатую часть меньше — ближний
крупнее. Модель сцены получила `HoundView` и поле-СПИСОК
`SceneModel.hounds`, а не поле внутри героя: второго пса иначе пришлось бы
выносить отдельной стадией.

Состояния пса:

| состояние | как читается |
| --- | --- |
| стоит, бьёт | свой замах `--swing` и выпад `.lunge` теми же ручками `--lean/--lunge/--kick`, что у героя — в углу они так же обнулены |
| получил удар | `.kick` на теле, красная полоса `.flash.hound` над ним, красное число над его головой |
| лежит | `.down`: прижат к земле, приглушён и обесцвечен, НА ПЛОЩАДКЕ; вместо полоски — число секунд до возврата |
| кусает | приглушённое мелкое число над мобом (`.floater.companion`), чтобы два потока чисел героя и пса не сливались |

Таймер возврата живёт в состоянии игры: сцена читает из модели «лежит,
осталось N секунд» (`downSecLeft`), своих таймеров у неё нет — тест на
`setTimeout/setInterval/requestAnimationFrame` по-прежнему держится.
Готовность сцены (`data-scene="ready"`) ждёт и картинку пса: снимок Псаря
без пса был бы снимком другого класса.

Полоска здоровья пса — маленькая, над самим псом (`.hound .tag .bar`),
заливка цветом энергии: это тело Псаря, и оно читается его цветом, а не
зелёным героя и не красным моба.

Шина ударов: укус несёт `companion: true` и `companionIndex`, удар по псу —
`targetId: 'hound'` и тот же индекс; сцена отводит лапу псу, а не руку
герою, и красит удар по псу как удар по герою.

### Спрайт и лицензия

`public/sprites/hound.svg` — силуэт четвероногого, нарисованный для проекта
(viewBox 200×120, тот же стиль заливки и обводки, что у героя). Запись
`HOUND_SPRITE` в `data/sprites.ts` рядом с `HERO_SPRITE`, все поля на месте,
`content:check` держит путь и лицензию. Фраза-счётчик в CREDITS.md стала
«Все семнадцать картинок», в таблицу добавлена строка про пса.

### Пресет и снимки

Пресет `hound` (`__fixtures__/presets/build.ts`): Псарь двенадцатого уровня
с находками, надетыми моделью игрока, пёс со здоровьем 60 % — полоска
читается полоской, а не рамкой. Добавлен в конец `PRESET_NAMES`: первые три
пресета `presets.test.ts` читает позиционно. Снимки `scene-hound-390` и
`scene-hound-1280` — новые эталоны; прежние снимки сцены и меню (все на
Страже) остаются прежними.

### Проверки стадии (`render2d.test.ts`, +8 тестов)

- `HoundView` — список в `SceneModel`, у героя полей пса нет;
- у классов без спутника список пуст, слой стоит за `{#if}` на длину;
- лежачий пёс остаётся в списке приглушённым и с числом секунд из состояния;
- слой псов стоит между героем и эффектами (по порядку в разметке);
- место и размер второго пса считаются от номера в списке;
- полоска — внутри фигуры пса, до слоя эффектов;
- спрайт в реестре: путь, CC0, авторство проекта, viewBox;
- фраза-счётчик в CREDITS говорит «семнадцать» и называет файл пса.

`npm run test`: 78 файлов, 1512 тестов, зелёный.

## Стадия 4. Пороги автокаста по ресурсу

### Что уже было — и осталось на своём месте

`autocast.resourceAbove` («жать только пока запаса не меньше доли») лежит в
данных с ночи ярости и читается одной функцией `autocastAllows`. Для Псаря
он ставится на ДОРОГИЕ умения стадии 5 — «Серия» и «Спуск», — чтобы автокаст
не сжигал полоску на них, оставляя дешёвые кнопки без энергии. Ни одной
правки логики под это не потребовалось.

### Что действительно новое — ползунок «не тратить ниже N %»

Один на класс, по образцу ползунка привала:

| где | что |
| --- | --- |
| `GameState.resourceFloor` | доля 0..1, по умолчанию 0 (`RESOURCE_FLOOR_DEFAULT`) |
| `data/balance.ts` | `MAX_RESOURCE_FLOOR = 1`, `snapResourceFloor` — общий помощник `snapShare` с порогом привала: тот же шаг `REST_THRESHOLD_STEP`, тот же потолок-единица |
| `stores/game.ts` | экшен `setResourceFloor` прижимает к шагу, как `setRestHpThreshold` |
| `game/abilities.ts` | `passesReserve`: резерв умения и пол складываются ПО МАКСИМУМУ; руками игрок волен тратить всё |
| `game/rotation.ts` | `dutyCycle` читает тот же максимум: глубина всплеска трат считается до пола |
| `game/state.ts` | пол едет в `Rotation` рядом с рангами и свойствами — модель боя, оффлайн и книга читают одно поле |
| `game/combat.ts` | пол в ключе кеша оценки |
| `game/simulate.ts` | `SimBuild.resourceFloor` — прогон задаёт его полем, как порог привала |
| `game/save.ts` | версия сейва 35 → 36, миграция пишет ноль явно; чтение прижимает к шагу; мусор — умолчание, не отказ |
| `ui/ResourceFloorRow.svelte` | строка в подвале панели автокаста: ползунок, процент и ДЕЙСТВУЮЩЕЕ число («это 39 из 130 энергии») — от того же `stats.maxMana`, по которому считает автокаст |

Почему пол, а не четыре резерва: резерв отвечает «сколько держать под ЭТУ
кнопку», пол — «ниже чего не тратить вообще». Один вопрос — один ползунок.

Таланты трогать пол не могут по построению: он не `StatId` и в конвейер
статов не входит, так что и запись `'setting'` в `TALENT_STAT_RULE` ему не
нужна — ей защищают то, что в конвейере лежит (порог привала).

Страж и Изувер получают ноль: автокаст жмёт до дна, как и прежде, и модель с
нулевым полом даёт прежние числа (проверит матрица стадии 9).

Держится `resource-floor.test.ts` (8 тестов): прижимка к шагу и потолку;
ноль у всех классов по умолчанию; под полом платное умение не кандидат
автокаста, а рука его статуса не меняет; пол и резерв — по максимуму в обе
стороны; пол едет ротацией и билдом прогона; модель с полом 0.9 режет урон
умений и не поднимает темп ни у одного класса, у Стража — строго режет;
сейв пишет, читает, прижимает, мусор читает нулём, сейв прошлой версии
получает ноль.

### Два мёртвых поля — по одному решению на каждое

| поле | где читается | где задано | решение |
| --- | --- | --- | --- |
| `autocast.targetHpAbove` | `autocastAllows`, строка в книге | ни одним умением | ИСПОЛЬЗУЕТСЯ в стадии 5: «Хватка» не вешается автокастом на моба ниже 35 % — шесть секунд замедления умирающему не окупаются |
| `detonate.resourceMultiplier` | тик (`detonate`), модель (`abilityMods`), конвейер талантов (`detonateResourceMultiplier`), книга, схема | ни одним умением с тех пор, как детонатор Изувера стал «Багровой гранью» | ЗАПИСАНО: у Псаря детонатора нет (метка на цели — механизм Стража). Предложение владельцу — снять поле, настраиваемое имя и ветку схемы отдельной уборкой; в эту ночь общий код не трогается |

Снимки: обновлены `menu-autocast-390` и `menu-autocast-1280` (в подвале
панели появилась строка пола); остальные эталоны не тронуты.

## Стадия 5. Одиннадцать умений

### Набор

Сетка открытий — как у готового класса (1, 2, 4 … 20), хотя статус класса
`preview`. Все числа — в `data/abilities.ts`, все команды — ФЛАГИ С PAYLOAD'ом,
ни одной ветки по id в логике.

| ур. | умение | id | энергия | откат | удар героя | что делает пёс | автокаст |
| ---: | --- | --- | ---: | ---: | ---: | --- | --- |
| 1 | Подсечка | `undercut` | 30 | 2 с | 1.5 | — | — |
| 2 | Травля | `sic` | 30 | 12 с | 1.0 | кусает на 50 % чаще 10 с (`houndHaste`) | без стоящего пса — отказ |
| 4 | Подрез | `hamstring` | 40 | 6 с | 1.6 (+60 %) | удар героя сильнее, пока пёс грызёт цель (`packStrike`) | — |
| 6 | Отзыв | `recall` | 35 | 18 с | 0 | отходит на 5 с: не кусает, не принимает урона, лечит 50 % своего запаса (`recall`) | только пока пёс ниже 50 % (`autocast.houndHpBelow`) |
| 8 | Хватка | `grip` | 40 | 14 с | 0.8 | держит моба: замах того на 35 % длиннее 6 с (`grip`) | только по цели выше 35 % (`autocast.targetHpAbove`) |
| 10 | Серия | `flurry` | 50 | 10 с | 0.7 × 3 | кусает на каждый из трёх ударов (`flurry`) | только при энергии выше 60 % |
| 12 | Перевязка | `bandage` | 35 | 15 с | 0 | лечится на 40 % своего запаса (`houndHeal`) | только пока пёс ниже 60 %; без стоящего пса — отказ |
| 14 | Спуск | `unleash` | 30 | 12 с | 0 | кусает сразу, укус ×10 (`unleash`) | только при энергии выше 70 %; без стоящего пса — отказ |
| 16 | Скрадывание | `skulk` | 40 | 20 с | 0 | принимает на 30 % больше входящего вместо героя 10 с (`skulk`) | без стоящего пса — отказ |
| 18 | Оклик | `rally` | 15 | 45 с | 0 | павший встаёт с 50 % запаса (`rally`) | только когда есть павший |
| 20 | Свора | `pack` | 50 | 30 с | 0 | приходит второй пёс, пока кнопка в ряду (`pack`) | при полной своре — отказ |

Адресованы псу восемь из одиннадцати (травля, отзыв, хватка, перевязка,
спуск, скрадывание, оклик, свора), ещё два его ЧИТАЮТ (подрез, серия) —
против требуемой трети. Ни одного накопительного ресурса, ни одной метки на
цели, ни одной чужой пары «роль + механизм» (`spendAll`, `execute`, `window`,
`stance`, `ramp`, `resolve`, `absorb`, `freeCasts` у Псаря отсутствуют).
Держит `__tests__/hound-mechanism.test.ts` — в обе стороны: у Стража и
Изувера ни одной команды псу и нет поля `companion`.

Подсечка подорожала с 20 до 30: спецификация энергии — «умение стоит
треть-половину шкалы», и заполнитель не исключение. Один заполнитель уже
дороже регенерации (30 за 2 с против 12 в секунду): полоска ходит от полной к
пустой с первой кнопки, и «когда сбрасывать» — вопрос класса с первого уровня.

### Куда это легло

- **Данные** — десять интерфейсов payload'ов, десять полей `AbilityDef`,
  `autocast.houndHpBelow`, шестнадцать настраиваемых полей в
  `ABILITY_TUNABLE` (величины — `scale`, два порога — `shift`); конвейер
  `abilityTune` применяет их все, штучное округляет.
- **Состояние** — `GameState.houndMarks` (травля, отзыв, хватка, скрадывание
  как метки С ДЛИТЕЛЬНОСТЬЮ на своре, одним объектом на всех псов); в сейв не
  пишутся, тикают в `advanceCooldowns` рядом со стойкой и щитом.
- **Тик** — `activeCompanion(def, marks)` отдаёт числа спутника с учётом
  команд (замах короче, доля перенаправления выше или ноль); отозванный пёс
  не кусает и не принимает урона; хватка удлиняет замах моба и кончается
  вместе с ним (`applyHoundTimers`, одна проверка вместо четырёх мест);
  свора подрезается до ёмкости ряда (`houndCapacity`: комплект класса плюс
  `pack` у умений в ряду и открытых уровнем).
- **Коды отказа** — `no-hound`, `no-fallen-hound`, `pack-full`; слова в
  `ui/abilityText.ts`. Журнал получил событие `hound-command` — что сделал
  пёс, интерфейс читает по флагам умения.
- **Модель боя** — `abilityMods` собирает `HoundTune` (доли × аптайм
  команды: ускорение, доля молчания, лечение в секунду, прибавка к
  перенаправлению, замедление моба, ожидание оклика, лишние псы своры);
  `houndModel(state, monster, tune)` считает укусы и живучесть пса уже с
  ним; `commandDamage` добавляет урон подреза, спуска и укусов серии — от
  того, сколько псов стоит В СРЕДНЕМ. Хватка режет входящее и герою:
  `incoming *= 1 − u + u / (1 + slow)`.
- **Ротация** — серия кладёт в поток ударов три удара за каст (каждый
  квантует бой и может добить), в урон каста — три удара.
- **Сейв** — `houndsFromSaved` держит псов до ёмкости ряда: свора переживает
  перезагрузку.

### Спуск посчитан от энергии, а не взят на глаз

Энергия — ограничитель ротации, поэтому умение живёт или умирает по урону
НА ЕДИНИЦУ ЭНЕРГИИ. Подсечка: 1.5 / 30 = 0.050. Спуск при ×3.5 за 50 давал
0.55 × 3.5 / 50 = 0.0385 — хуже заполнителя, и модель честно ставила его в
минус (тест «команды видны модели» упал на нём первым). ×6 за 45 — 0.073:
выше заполнителя ровно настолько, насколько положено козырю с откатом
двенадцать секунд. Остальные для сравнения: подрез 0.064, серия 0.075
(с укусами), травля ≈ 0.07 (с ускорением пса за восемь секунд).

**Стадия 9 ужала укус пса дважды — до 0.28, затем до 0.22** (см. её раздел),
и Спуск пересчитан тем же правилом оба раза: ×8 за 30 при укусе 0.28, ×10 за
30 при укусе 0.22 — 0.073 на единицу энергии, в полтора раза выше
заполнителя. Травля при укусе 0.22 стала невидимой модели в паре с одной
Подсечкой (−0.4 %), и её цена с длиной пересчитаны по скану: 30 за 10 с.
В таблице выше стоят итоговые числа.

### Два мёртвых поля — итог

`autocast.targetHpAbove` использован «Хваткой» (0.35): держать умирающего
незачем. `detonate.resourceMultiplier` остаётся мёртвым — записано в стадии 4.

### Автокаст: пять правил задания — тиком

| правило | проверка |
| --- | --- |
| Отзыв не жмётся на целом псе | `autocastAllows` ложно при полном псе, истинно при 30 %, кандидатов нет при лежачем |
| Перевязка не жмётся без пса | статус `no-hound` при лежачем, не кандидат при целом, кандидат при 20 % |
| Оклик только при павшем | статус `no-fallen-hound` при стоящих, кандидат при павшем; поднимает с 50 % |
| Спуск не жмётся без пса | статус `no-hound` при лежачем; ждёт 70 % энергии |
| Свора зовёт один раз | второй пёс приходит; при полной своре — `pack-full` и не кандидат; снял кнопку — пёс уходит тиком |

Плюс: четвёрка по умолчанию (подсечка, травля, подрез, отзыв) на шестом
уровне за минуту боя опускает полоску ниже половины — главный вопрос класса
возникает, а не спит на полной шкале.

### Проверки стадии

`hound-abilities.test.ts` (20 тестов): каждая команда делает ровно то, что
обещает строка книги; пять правил автокаста; модель видит травлю, спуск,
серию, свору и подрез (урон в секунду выше, чем у одной Подсечки при равном
бюджете энергии); `авто ≤ рука` с каждой командой в ряду; свора переживает
сейв; у Стража и Изувера ни одной команды и пустые метки своры после пяти
секунд боя. `hound-mechanism.test.ts` (10 тестов) — правила развода классов.
Снято временное исключение Псаря в `abilities.test.ts`; `abilityLines.test`
покрывает десять новых полей; `content:check` — диапазоны payload'ов,
поддержка без удара, команды только у класса со спутником, настраиваемое
поле подкреплено флагом.

Иконки: десять новых (CC BY 3.0, Lorc и Delapouite), спрайт пересобран,
`CREDITS.md` — 269 иконок. Снимки: обновлены `scene-hound-390/1280` (ряд
действий Псаря заполнен четырьмя кнопками).

`npm run test`: 81 файл, 1564 теста, зелёный.

## Стадия 6. Ветка Гон — урон двух тел

### Машинерия под все три ветки — три флага, одна арифметика

| флаг | payload | что делает | где читается |
| --- | --- | --- | --- |
| `hound-tune` | `field` из `HOUND_TUNE_FIELDS` (укус, замах, доля перенаправления, запас, возврат, восстановление в бою и вне), `op` (`percent` или `points`), `value` × ранг | правит ЧИСЛО СПУТНИКА так же, как талант правит умение: сдвиги, потом доли | `companionOf(state)` — одна точка, которую читают тик, модель, сцена и сейв; без талантов возвращает объект данных бит в бит |
| `pack-tactics` | `bonusShare` | пока хоть один пёс стоит и не отозван, ВЕСЬ урон героя выше на долю | `outgoingMultiplier`; модель — × доля времени, когда пёс стоит (`hound.standing`) |
| `hound-avenge` | `bonusShare`, `durationSec` | пал пёс — метка своры на несколько секунд, герой бьёт сильнее | падение в `applyMonsterAttack`, множитель в `outgoingMultiplier`; модель — × min(1, падений в секунду × длительность), падения выдаёт `houndModel.fallsPerSec` |

Это флаги ОБЩЕГО набора: любой класс со спутником прочёл бы их той же
логикой, а класс без спутника ими воспользоваться не может — спутника нет.
`content:check` держит payload: поле из закрытого списка, величина не ноль и
не больше единицы за ранг, доли в 0..1, длительность положительна. Тексты
флагов собираются из payload (`ui/talentText.ts`), поле спутника названо
словом из закрытой по `HoundTuneField` таблицы.

### Ветка

Тринадцать этажей, 29 талантов, ёмкость 119 при глубине 60. Половина
талантов правит команды псу или самого пса (`hound-tune`: укус +6/+4/+4 %
за ранг тремя ступенями со стрелками, замах −5 % за ранг), остальное — руку
героя (ускорение, крит, сила атаки, правки Подсечки и Подреза). Стрелок
три: «Стремительность» ← «Быстрые руки», «Неотступный» ← «Острые клыки»,
«Клыки матёрого» ← «Неотступный».

| этаж | выбор |
| ---: | --- |
| 5 | **Стая** (флаг: +12 % урона героя, пока пёс стоит) против **Злого спуска** (укус Спуска ×1.5) — ровный множитель против всплеска |
| 9 | **Мститель** (флаг: пал пёс — 6 с +30 % урона героя) против **Четвёртого удара** (Серия из четырёх, пёс кусает четыре раза) — окно из потери против ровного прироста |
| 13 | **Двойной укус** (укус Спуска ×2) против **Второго замаха** (общий флаг `double-strike`, 20 %) — зубы против руки |

Пути: «Стая» (четвёрка по умолчанию — подсечка, травля, подрез, отзыв;
первый путь — прибор) и «Зубы» (подсечка, травля, серия, спуск). Оба
покрывают все 29 талантов на пару — `content:check` это держит.

`npm run test`: 81 файл, 1564 теста, зелёный (правка теста: метки своры
сравниваются с `NO_HOUND_MARKS` — у них появился пятый член `avenge`).
Иконок 277 (восемь новых для талантов Гона).

## Стадия 7. Ветка Привязь — живучесть через пса

Тринадцать этажей, 28 талантов, ёмкость 115 при глубине 60. Живучесть здесь
строится ВТОРЫМ ТЕЛОМ: шесть талантов правят самого пса (`hound-tune`: запас
+8/+6 % за ранг двумя ступенями со стрелкой, доля перенаправления +3 пункта
за ранг, возврат −8/−6 % двумя ступенями со стрелкой, восстановление в бою
+1 пункт за ранг), пятнадцать правят команды (перевязка, отзыв, хватка,
скрадывание, оклик), семь — запас и восстановление героя; ни щита, ни брони
ему не дано сверх одного процента к доспеху. Стрелок три.

| этаж | выбор |
| ---: | --- |
| 5 | **Зализать раны** (отзыв лечит вдвое) против **Второго дыхания** (общий флаг `faster-revive`: воскрешение вдвое быстрее) — пёс живёт против герой умирает дешевле |
| 9 | **Железная привязь** (скрадывание перекладывает на пса вдвое) против **Полного оклика** (павший встаёт целым) — пёс держит больше против падение стоит меньше |
| 13 | **Неутомимый оклик** (откат оклика ×0.4) против **Тени пса** (скрадывание вдвое дольше) — часто поднимать против долго прятаться |

Пути: «Привязь» (четвёрка по умолчанию, прибор) и «Тень» (подсечка,
скрадывание, перевязка, оклик). Иконок 281 (четыре новых).

`npm run test`: 81 файл, 1564 теста, зелёный.

## Стадия 8. Ветка Тропа — автономность энергии

Тринадцать этажей, 28 талантов, ёмкость 115 при глубине 60. Половина
талантов правит ЦЕНУ команд (по одному «лёгкому» на каждое из десяти платных
умений плюс ритм Подсечки), четверть — скорость возвращения энергии, длину
привала и восстановление вне боя, два таланта со стрелкой правят пса
(`hound-tune`: восстановление вне боя +2 пункта за ранг двумя ступенями).
Стрелок три. Ни одного таланта на порог привала: настройка игрока таланту не
принадлежит (правило записано в CLAUDE.md и держится `content:check`).

| этаж | выбор |
| ---: | --- |
| 5 | **Отдых снимает усталость** (`rest-clears-cooldowns` 0.5) против **Ритма охоты** (`kill-refunds-cooldowns` 0.7) — пауза против непрерывности |
| 9 | **Полпривала** (`shorter-rest` 0.5) против **Второй серии** (`ability-extra-charge` у Серии) — меньше ждать против больше выстрелить |
| 13 | **Неутомимая серия** (цена Серии ×0.5) против **Дыхания охоты** (Травля вдвое дешевле и в полтора раза дольше) — рука против пса |

Все флаги Тропы — из общего набора Стража и Изувера: своей машинерии ветка
не заводит. Пути: «Тропа» (четвёрка по умолчанию, прибор) и «Ритм»
(подсечка, травля, серия, спуск). Иконок 283 (две новых).

### Тест «флаги из одного набора» стал парным

Проверка сравнивала два класса: флаги Изувера ⊆ флаги Стража, доля общего не
меньше половины, наборы не равны. С третьим классом она переписана: эталон
набора — ГОТОВЫЙ класс (`DEFAULT_CLASS`), флаг превью-класса обязан быть у
готового, кроме флагов спутника (`COMPANION_FLAGS` в `data/talents.ts` —
машинерия любого класса с псом, а у Стража читать её нечем); каждая ПАРА
классов делит не меньше половины меньшего набора и не совпадает целиком.
Снято временное исключение `UNDER_CONSTRUCTION` в `talents.test.ts`: форма
всех девяти веток проверяется одинаково. `content:check` держит и обратную
сторону: флаг спутника у класса без спутника — замечание.

Первый прогон поймал согнутую стрелку («Стоянка на ходу» стояла не в столбце
опоры) — исправлено перестановкой столбцов на этаже.

`npm run test`: 81 файл, 1564 теста, зелёный.

## Стадия 9. Контракты, бюджет, матрица, ключи

### Строка пса в бюджете силы — и второй пёс, ужавший первого

Правило бюджета: система, добавляющая силу, получает строку в тот же день.
Пёс получил `POWER_BUDGET.multipliers.hound`, мерится СНЯТИЕМ (Псарь на
потолке с псом против него же с пустым списком псов), и рядом с ним стоит
второе требование задания — второй пёс «Своры» обязан помещаться в строку
умений (лучшая четвёрка против четвёрки по умолчанию ≤ 1.15), причём
УЖИМАНИЕМ ПСА, а не расширением коридора.

Первый замер при укусе 0.55 удара оружия: строка пса ×1.379, лучшая
четвёрка `sic + grip + bandage + pack` ×1.350 — второй пёс не помещался.
Скан укуса в одном прогоне (эталонное прохождение считается один раз):

| укус пса | строка пса | лучшая четвёрка со сворой | лучшая без своры |
| ---: | ---: | ---: | ---: |
| 0.55 | ×1.379 | ×1.350 | ×1.031 |
| 0.45 | ×1.320 | ×1.265 | ×1.032 |
| 0.35 | ×1.262 | ×1.176 | ×1.034 |
| 0.30 | ×1.233 | ×1.149 | ×1.034 |
| 0.25 | ×1.204 | ×1.127 | ×1.035 |

На потолке 0.28 помещался (×1.139), и на нём стадия остановилась бы — но
паритет четвёрок трёх классов мерится на ДВАДЦАТОМ уровне, и там та же
четвёрка `undercut + sic + bandage + pack` дала **×1.165**: второй пёс на
двадцатом стоит дороже, чем на сотом (пёс — доля удара героя, а умения героя
на двадцатом ещё слабы). Второй скан — по двум срезам сразу:

| укус пса | лучшая со сворой, ур. 20 | лучшая со сворой, потолок | строка пса, потолок |
| ---: | ---: | ---: | ---: |
| 0.28 | ×1.165 | ×1.139 | ×1.221 |
| 0.25 | ×1.146 | ×1.127 | ×1.204 |
| 0.22 | ×1.132 | ×1.114 | ×1.186 |
| 0.20 | ×1.122 | ×1.106 | ×1.175 |

Взято **0.22** — с запасом под потолок на обоих срезах. Ужатие потянуло за
собой две команды, и обе пересчитаны тем же правилом «урон на единицу
энергии», что и в стадии 5:

- **Спуск** — ×8 за 30 при укусе 0.22 давал 0.059 на единицу энергии, едва
  выше заполнителя (0.050); теперь ×10 за 30 — 0.073, в полтора раза выше,
  как положено козырю с откатом двенадцать секунд.
- **Травля** — при укусе 0.22 стала НЕВИДИМОЙ модели: в паре с одной
  Подсечкой −0.41 % урона в секунду, и тест «команды видны модели» упал на
  ней первым. Скан пяти вариантов по двум срезам (цена 35/30, длина 8/10/12
  с, доля 0.5/0.75): взято 30 за 10 с при той же доле 0.5 — +0.87 % в паре,
  лучшая четвёрка со сворой ×1.136 на двадцатом и ×1.118 на потолке. Доля
  ускорения не тронута: её читают тесты и таланты Гона.

Итоговый замер: строка пса **×1.19** на потолке (коридор 1.05–1.42: ±20 %
вокруг замера, пол поднят до 1.05 по правилу «пол выше единицы»), лучшая
четвёрка `undercut + sic + flurry + pack` **×1.118** на потолке и **×1.136**
на двадцатом (коридор 1.01–1.15). Невостребованных в верхней пятёрке на обоих
срезах трое: отзыв, спуск, скрадывание — все три команды ЗАЩИТЫ или всплеска,
которых модель долгосрочных средних по построению ценит меньше, чем игрок в
данже. Доли урона на потолке — в таблице матрицы ниже.

Что ужатие значит для игрока: пёс — пятая часть урона и треть живучести на
любом уровне, а не половина. Обе оси героя с псом и без (средняя
экипировка, автокаст по умолчанию):

| уровень | урон с псом / без | живучесть с псом / без |
| ---: | ---: | ---: |
| 10 | 95.3 / 78.3 (×1.22) | 3509 / 2709 (×1.30) |
| 20 | 156.1 / 128.6 (×1.21) | 5227 / 4025 (×1.30) |
| 55 | 386.0 / 321.7 (×1.20) | 11464 / 8896 (×1.29) |
| 85 | 609.1 / 511.3 (×1.19) | 16867 / 13112 (×1.29) |

Живучесть от укуса не зависит вовсе (перенаправление 0.3 не тронуто), урон
просел с ×1.26 до ×1.21 против первого замера при укусе 0.28.

Пёс на ногах — **83–86 % времени** на всех трёх замерах тиком (полчаса в
своей зоне, уровни 20 / 55 / 85: 83.3 / 84.2 / 86.0 %), падений — 25 / 24 /
21 за полчаса, то есть одно на семьдесят-восемьдесят секунд: ритм «пал →
вернулся» виден, но не правит боем.

### Паритет четвёрок трёх классов

Одна таблица на всех — `balance-abilities.test.ts`, блок «паритет четвёрок
по классам»: лучшая четвёрка каждого класса на двадцатом уровне против его
же четвёрки по умолчанию, коридор строки умений один. У Стража контракт
роняет прогон, у Изувера и Псаря — `[превью]`. Числа — в таблице матрицы
ниже.

### Таблица развода — три столбца

Печатает `hound-mechanism.test.ts` (быстрый набор) по данным умений, и у
каждого класса ровно один признак не ноль:

| класс | вопрос | меток на цели | читают полоску | команд псу |
| --- | --- | ---: | ---: | ---: |
| Страж | что висит на мобе | 4 | 0 | 0 |
| Изувер | в каком я состоянии | 0 | 8 | 0 |
| Псарь | что сейчас делает пёс | 0 | 0 | 10 |

### Полная матрица — как шла и что показала

`BALANCE_DUMP=.balance-dump npm run test:balance`, все двадцать зон, три сида,
три класса. Машина этой ночи шла вдвое медленнее обычной (импорт модулей
2059 с против ~1000, стена 3830 с против ~2000): **два теста упёрлись в свой
срок 1800 с** — «Страж: три чистых билда на 22 очках» (`balance-talents`) и
«таблица пути» (`run.test`). Оба — таймауты, не контракты: свои величины они
успели записать в дамп (в эталоне не потерялся ни один ключ), но их
`expect` не дошёл до конца. Итог раннера: 2 упало (таймауты), 219 прошло.

Ключи отпечатка: в эталоне 1889, в дампе 2076. **Ключи Стража и Изувера на
месте** — с одной поправкой, разобранной ниже (утечка правила потока
ударов, исправлена fix-коммитом, до правки двигала 14 ключей Стража на доли
процента). Новых ключей 187: 185 Псаря и два новых ключа паритета четвёрок
(`abilities/warden|reaver/level-020/best-four-over-default/value` — это
НОВЫЕ ключи готового и превью-класса, а не сдвинутые: тест паритета
появился этой ночью и пишет все три класса одной таблицей).

### УНАСЛЕДОВАННОЕ КРАСНОЕ — было до ночи, не чинилось

| контракт | класс, срез | замер | коридор | статус |
| --- | --- | ---: | ---: | --- |
| темп TTK 8–15 с | Изувер, ур. 6, hollow-quarry | 7.36 с | ≥ 8 | `[превью]`, как на `main` |
| цена боя валово 13–20 % | Изувер, ур. 6, hollow-quarry | 8.75 % | ≥ 9 | `[превью]`, как на `main` |
| разброс TTK по уровням | Изувер, 1..6 | 0.838 | ≤ 0.5 | `[превью]`, как на `main` |

Числа совпали с прогоном на `main` (`2cf5221`) до третьего знака: ночь их не
тронула ни в какую сторону.

### НОВОЕ КРАСНОЕ — контракты Псаря, все под `[превью]`

| контракт | срез | замер | коридор | что это значит |
| --- | --- | ---: | ---: | --- |
| темп TTK 8–15 с | ур. 5, shepherds-meadow | 15.54 с | ≤ 15 | стартовая полоса чуть медленна: пёс на пятом уровне ещё мал, а Подсечка одна |
| разброс TTK по уровням | 1..6 | 0.62 | ≤ 0.5 | та же ступень переодевания на первом переезде, что у Изувера (0.84) |
| цена боя валово 13–20 % | ур. 6, hollow-quarry | 8.38 % | ≥ 9 | перенаправление 0.3 снимает с героя больше, чем стоит бой на входе в зону |
| цена схватки с боссом 60–95 % | sunken-barrow | 98.0 / 89.9 % | ≤ 95 | первый босс данжа снимает почти весь запас: лечения героя у класса нет, а пёс от босса не спасает |
| цена худшего боя ≤ 30 % | glasswaste | 32.2 % | ≤ 30 | здоровяк верхнего края полосы |
| лестница покупок ровная | самая пологая ступень | ×1.187 | ≥ ×1.2 | доход Псаря на одном из уровней растёт медленнее, чем цена следующей ступени |
| **полный путь: доходит до потолка** | сиды 4242, 1234 | **застрял на 16 уровне** | 100 | см. ниже — главная находка матрицы |
| путь стоит 4700–5400 убийств | сид 4242 | 47 222 | ≤ 5400 | следствие застревания |
| ни одна полоса не съедает времени не по ширине | полоса 1–9 | 21.1 % времени на 9.1 % уровней (×2.32) | ≤ ×1.7 | следствие застревания |
| классы проходят путь за сопоставимое время | три класса | разрыв 166 % | ≤ 40 % | следствие застревания; ключ `total-hours-gap` |
| паритет четвёрок (ур. 20) | — | ×1.136 | 1.01–1.15 | **зелёный** |
| строка пса в бюджете | потолок | ×1.190 | 1.05–1.42 | **зелёный** |
| второй пёс в строке умений | потолок | ×1.118 | ≤ 1.15 | **зелёный** |
| доли урона Псаря | потолок | автоатака 50.9 · умения 34.5 · проки 0.0 · пёс 14.5 % | сумма 100 | **зелёный** |

Плюс известное расхождение из `docs/AUDIT-FOLLOWUP.md` (доля привалов в
пути против записи в CLAUDE.md), у Псаря оно вырождается в 0.007 % — герой,
застрявший на шестнадцатом уровне, не отдыхает вовсе.

### Главная находка: путь Псаря застревает на шестнадцатом уровне

Три сида полного пути 1..100: **4242 — 47 222 убийства и потолок не взят
(застрял на 16), 7 — 5 389 убийств и потолок взят как у Стража (5 237),
1234 — 48 743 и потолок не взят.** Один сид из трёх проходит путь как
готовый класс, два — не проходят вовсе. Это не «класс на 166 % медленнее»,
а бимодальность: зависящая от ранней рулетки находок точка, после которой
модель игрока в прогоне перестаёт вести героя вперёд. Семнадцатый уровень
не наступает десятки тысяч убийств — значит опыта нет, то есть герой стоит
в зоне, отставшей от него на шесть и больше уровней (`XP_GAP_PENALTY` даёт
ноль), и `offlineZone` не выпускает его выше: все зоны по уровню читаются
прогнозом как смертельные. Пёс в модели ЖИВУЧЕСТЬ ГЕРОЯ ПОДНИМАЕТ (ось
×1.3), поэтому подозрение — не в числах спутника, а в том, как прогноз зоны
складывает падение пса с ценой боя, либо в самой модели игрока (выбор
экипировки по `modelRate`) на билде, где пёс — заметная доля урона. Ночь
установить механизм не успела; это первый вопрос следующей ночи Псаря и
причина, по которой `status: 'preview'` у класса стоит по праву.

**Поправка к находке стадии 2 про `total-hours-gap`.** Ключ действительно
считается по множеству классов, а не по паре, и пара Страж–Изувер не
сдвинулась (их `total-hours` совпали с эталоном побитово) — но ДВИНУЛСЯ он
не из-за арифметики множества, а потому что третий класс на первом сиде
реально идёт путь в 2.66 раза дольше. Уже на стадии 2 (один Псарь с одной
Подсечкой) это был тот же застрявший путь; тогда он был прочитан как
свойство ключа. Запись в CLAUDE.md поправлена.

### Утечка правила потока ударов — исправлено fix-коммитом

Стадия 5 научила `hitStream` не считать команду псу без удара героя ударом
потока (её укусы идут псом) — и записала это как «каст с нулевым уроном не
удар». Правило зацепило Стража: **заслон и стойка (ноль урона) считались
ударами потока до пса**, квантовали бой и добивание, и общее правило
сдвинуло 14 ключей Стража на 0.02–0.4 % — все в путях талантов Оплота,
Бдения и Гнева (`talents/warden-*/path-*/damage`, `kills-per-second`,
`pace-gap`, `damage-gap`; `balance/pacing` и `fight-cost` Стража не двигались:
в четвёрке по умолчанию нулевых кастов нет). Исправление — предикат
`isHoundCommand` (переехал в `game/hound.ts`, читают его тик и модель):
поток пропускает РОВНО команды псу, а нулевые касты Стража считаются как
раньше. Проверено пробой (`BALANCE_SAMPLE=1` в отдельный дамп) после
правки: все 18 ключей, которые двигала утечка в полном прогоне (14 путей
талантов Стража и 4 ключа `vigil-vs-wrath`), совпали с эталоном побитово.
Остальные 23 расхождения пробы с эталоном — свойство выборки (три зоны,
четверть часов: `no-dominant/open-zones`, `monotone`, `model-vs-tick`,
`same-zone`), и в полном прогоне ни один из них сдвинут не был. Открытый
вопрос владельцу — считать ли
заслон и стойку ударами потока правильно; если нет, это отдельное осознанное
движение отпечатка на доли процента, а не правка ночи Псаря.

### Таблица ключей отпечатка

| группа ключей | было | стало | движение |
| --- | ---: | ---: | --- |
| Страж, все группы | 1391 | 1391, без изменений | **0 сдвинутых** (после fix-коммита; до него 14 в путях талантов) |
| Изувер, все группы | 187 | 187, без изменений | **0 сдвинутых** |
| `run/seed-4242/classes/total-hours-gap` | 0.0094 | 1.664 | сдвинут — застрявший путь Псаря, см. выше |
| `abilities/warden/level-020/best-four-over-default` | — | ×1.071 | новый (тест паритета) |
| `abilities/reaver/level-020/best-four-over-default` | — | ×1.031 | новый (тест паритета) |
| `balance/fight-cost/houndmaster/*` | — | 81 ключ | новые |
| `balance/pacing`, `balance/talents`, `balance/telemetry` Псаря | — | 6 + 9 + 13 | новые |
| `gap/overrun`, `gap/own-zone-ttk` Псаря | — | 18 + 9 | новые |
| `gold/income*`, `gold/ladder` Псаря | — | 16 + 3 + 1 | новые |
| `power/houndmaster/*` | — | 8 | новые: строка пса, четвёрка, четвёрка со сворой, четыре доли, пол коридора |
| `run/seed-*/houndmaster/*`, `run/median/houndmaster/*` | — | 17 + 3 | новые |
| `abilities/houndmaster/level-020/*` | — | 1 | новый |

Точные числа групп — `docs/balance-baseline.json` (сортирован по ключу).

### Эталон отпечатка — как записан

`npm run balance:baseline` пишет эталон из дампа целиком, а дамп этой ночи
нёс два дефекта, которые в эталон попасть не должны: значения Стража
ДО fix-коммита (утечка выше) и два таймаута. Поэтому эталон собран так:
прежний эталон (все ключи Стража и Изувера без изменений) + все ключи Псаря
из полного прогона + новый `total-hours-gap` + ключ паритета Изувера из
полного прогона (его четвёрка без нулевых кастов, утечка её не касалась) +
ключ паритета Стража из пробы ПОСЛЕ fix-коммита. Сборка проверена в обе
стороны: сверка собранного эталона с полным дампом даёт РОВНО 18 расхождений
(ключи утечки, до правки) и ни одного лишнего или потерянного ключа — то
есть все ключи Псаря вошли; сверка с пробой после правки — 18 из 18 ключей
утечки на месте. Итого 2076 величин против 1889. **Ночной прогон на `main` обязан этот эталон подтвердить**: если он
покажет лишние или потерянные ключи Псаря — это следы таймаутов, и эталон
переписывается `npm run balance:baseline` уже с чистого прогона.

### Статус класса — по факту

Псарь остаётся **`preview`**, и это не формальность. Зелёные контракты:
паритет четвёрок, строка пса, второй пёс в строке умений, доли урона,
развод классов, аптайм пса, ни одного сдвинутого ключа готового класса.
Красные: полный путь застревает на двух сидах из трёх (главное), цена
схватки с боссом выше коридора, цена боя и темп на входе в первые зоны, одна
ступень лестницы покупок. Переключение в `ready` — одна правка данных, но
раньше, чем путь пройдёт на трёх сидах, её делать нельзя.

## Стадия 10. Документация

### CLAUDE.md — что записано и где

- **«Классы»** — три класса, три ресурса; энергия через baseMods без нового
  поля `ResourceDef`; спутник как второе тело (`companion`, `game/hound.ts`,
  список `GameState.hounds`); модель боя видит пса в обе стороны; команды
  псу флагами и метки своры (`houndMarks`, `activeCompanion`); ёмкость своры
  производная (`houndCapacity`); флаги спутника `hound-tune` /
  `pack-tactics` / `hound-avenge` как общая машинерия (`COMPANION_FLAGS`);
  Изувер и Псарь — `preview`; находка про ключ `total-hours-gap`.
- **«Классы разведены механизмом»** — третий вопрос: «что сейчас делает
  пёс»; таблица развода 4 / 8 / 10; парная проверка флагов трёх классов.
- **«Умения»** — коды отказа `no-hound` / `no-fallen-hound` / `pack-full`;
  флагов двадцать шесть: шестнадцать общих и десять команд псу.
- **«Автокаст»** — пол ресурса «не тратить ниже N %» (одно правило в двух
  местах, `passesReserve` и `dutyCycle`); пороги по псу (`houndHpBelow`,
  `houndHeal.autocastBelowHpShare`, `targetHpAbove` у Хватки); команды без
  адресата не кандидаты по СТАТУСУ.
- **«Бюджет силы»** — строка спутника, замер снятием, второй пёс ужиманием
  пса по скану на двух срезах.
- **«Экран и меню»** — строка меню «Умения» знает про пол ресурса.
- **«Боевая сцена»** — слой псов между героем и эффектами.

`docs/ARCHITECTURE.md` и `docs/PLAN.md` в проекте нет — CLAUDE.md говорит это
прямо («второй копии заводить не надо — она разъедется»), и ночь их не
заводила.

### Обещания прежних ночей — проверены grep'ом

По заданию просмотрены упоминания «реагент», «мастерство», «источник
рецепта», «полка» и «ярость» в `docs/` и CLAUDE.md на предмет обещаний
будущего («будет», «позже», «следующей ночью», «TODO»). Висящих обещаний,
которые эта ночь должна была бы закрыть или которые она сломала, не нашлось:
все пять тем описаны в настоящем времени и держатся своими проверками. Ни
один прежний документ не упоминает третий класс — Псарь появился без долга
перед планом и без плана, который надо было бы переписать.

### Что НЕ трогалось

Общий код — только по необходимости стадий: `CombatRate.houndDamagePerSecond`
(четвёртая доля урона для бюджета), `contractClasses` и `CLASS_SET`
(третий класс сам встал в выборку по статусу), парная проверка флагов
талантов (прежняя требовала равенства наборов и с третьим классом
ломалась по построению). Ветки Изувера, его умения, числа Стража, урон
мобов, кривые — не тронуты: ключи Стража и Изувера в отпечатке это
показывают (см. таблицу ключей стадии 9).
