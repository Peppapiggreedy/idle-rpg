{
  "name": "idle-rpg",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json && tsc -p tsconfig.tests.json",
    "test:unit": "vitest run --exclude \"src/lib/game/__tests__/power-budget.test.ts\" --exclude \"src/lib/game/__tests__/balance-*.test.ts\" --exclude \"src/lib/game/__tests__/run.test.ts\" --exclude \"src/lib/game/level-gap.test.ts\" --exclude \"src/lib/game/__tests__/gold.test.ts\" --exclude \"src/lib/game/__tests__/dungeon-ladder.test.ts\"",
    "test": "npm run check && npm run test:unit",
    "test:balance": "vitest run --config vitest.balance.config.ts",
    "test:balance:sample": "BALANCE_SAMPLE=1 npm run test:balance",
    "test:visual": "playwright test",
    "test:all": "npm run test && npm run test:balance && npm run test:visual",
    "content:check": "vitest run src/lib/data/__tests__/integrity.test.ts",
    "balance:dump": "rm -rf .balance-dump && BALANCE_DUMP=.balance-dump npm run test:balance",
    "balance:diff": "npm run balance:dump && node scripts/balance-diff.mjs",
    "balance:baseline": "node scripts/balance-diff.mjs --write",
    "screenshots": "npm run test:visual",
    "screenshots:update": "playwright test --update-snapshots",
    "screenshots:update:one": "playwright test --update-snapshots -g",
    "screenshots:list": "playwright test --list",
    "presets:update": "UPDATE_PRESETS=1 vitest run src/lib/game/__tests__/presets.test.ts",
    "icons:build": "node scripts/build-icons.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.13.3",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "break_infinity.js": "^2.2.0"
  }
}
