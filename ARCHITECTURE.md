# ARCHITECTURE.md — реальное состояние кода

Снимок после итерации 5: игра стала КОНЕЧНОЙ. Потолок сотого уровня, кривая
опыта по таблице убийств, лестница открытий, ручная экипировка с разбором
добычи, привал между боями, шесть веток талантов по 61 очку, травничество
с зельями, зачарование, реагенты боссов с уникальными рецептами и проками,
восемь данжей из одного шаблона плюс героический режим, храм испытаний
и цепочка преквестов к вратам рейда. Документ описывает то, что есть в `src/`
фактически. Обновляется вместе с кодом.

## 0. Что изменилось в итерации 5 (кратко)

| Что | Где | Почему так |
|---|---|---|
| ПОТОЛОК 100 и кривая опыта ПО ТАБЛИЦЕ УБИЙСТВ | `data/balance.ts`, `game/formulas.ts` | у конечной игры есть конец. Кривая задана не формулой, а таблицей «сколько убийств стоит уровень», и опыт выводится из неё через реального моба своей полосы: авторская запись читается как замысел, а не как подобранная экспонента |
| Полоса мобов вместо «самой дальней открытой зоны» | `data/zones.ts` (`zoneForMonsterLevel`) | зоны открываются заметно быстрее, чем герой начинает в них выживать. Цена уровня, уровень боссов и модель прогона считаются по полосе, КОТОРАЯ ПО СИЛАМ, а не по той, куда пустили |
| Автонадевание снесено, добыча разбирается руками | `game/loot.ts`, `game/equipment.ts` | находка стала решением: `stashLoot` кладёт в сумку, продаёт или вытесняет худшее, а метка апгрейда показывает НА СКОЛЬКО лучше — долей прироста урона в секунду из той же `estimateCombatRate` |
| Привал между боями | `game/rest.ts`, `game/combat.ts` (`farmCycle`) | просадка HP кончалась ровно одним способом — смертью. Теперь цикл фарма квантован боями: герой доводит схватку до конца и уходит отдыхать после убийства |
| Лестница открытий | `data/progression.ts`, `ui/ProgressionPanel.svelte` | в конечной игре растущие числа держать перестают. Держит «через десять уровней будет то, чего я ещё не видел» — и названия будущих ступеней ОТСУТСТВУЮТ в разметке, а не приглушены стилями |
| Шесть веток талантов по 61 очку | `data/talents.ts`, `game/talents.ts` | три ветки на класс, 13 этажей, требование этажа 0/5/…/60. На этажах 5, 9 и 13 — концептуальные таланты в один ранг: они не прибавляют число, а включают поведение флагом с payload в данных. Ни одного `if (талант === '...')` |
| Заряды умений | `game/abilities.ts` | капстоун ветки урона даёт умению второй заряд. Заряды копятся откатом ПО ОДНОМУ, а «полный комплект» — это отсутствие записи, поэтому у героя без таланта в сейв не едет ничего |
| Сила блока растёт от живучести | `data/balance.ts` (`VIT_BLOCK_VALUE`) | без этого «сила блока» была свойством ОДНОГО предмета — щита, — и всё, что её множит, у изувера давало процент от нуля |
| Травничество и зелья | `data/herbs.ts`, `game/potions.ts` | трава набегает ВРЕМЕНЕМ, а не убийством, — отдельный тип данных со своим входом. Зелье пьётся только руками: ни автокаст, ни оффлайн его не трогают, и в модель боя оно входит лишь в режиме `'manual'` |
| Зачарование | `data/enchants.ts`, `game/enchanting.ts` | лишняя находка распыляется в пыль, пыль тратится на зачарование. Оно живёт полем на самой вещи, поэтому снятая вещь уносит его с собой, а сравнение предметов чинить не нужно: конвейер разворачивает его сам |
| Реагенты, уникальные рецепты и проки | `data/reagents.ts`, `data/procs.ts` | вещь можно СПЛАНИРОВАТЬ: реликвия тира выводится из данжа, её числа заданы данными, а прок обязателен со внутренним кулдауном — без потолка темп срабатываний рос бы вместе с ускорением |
| Данж — шаблон, героика — его четвёртый параметр | `data/dungeons.ts`, `data/heroic.ts` | восемь данжей и двадцать четыре босса выводятся из ТИРА, а `buildDungeon(spec, difficulty)` разворачивает один и тот же набор параметров в две цепочки. Второй лестницы данных нет |
| Храм испытаний | `data/temple.ts`, `game/temple.ts` | единственная геометрия в боевых числах игры, и она осознанная: забег обязан КОНЧИТЬСЯ. Попытка одна в сутки, часы для храма идут только вперёд |
| Преквесты и врата рейда | `data/quests.ts`, `game/quests.ts` | цель одного из четырёх перечислимых типов; логика ветвится только по ТИПУ. Прогресс набирается из уже существующих событий лога, поэтому в тик не добавлено ни одного счётчика |
| ЛЕСТНИЦА РЕДКОСТЕЙ СТАЛА ПОЛОГОЙ | `data/rarity.ts` | 1-2-4-8-16 ломало игру, хотя ни один тест не падал: эталон прогона одет в среднее по рулетке (1.8), а живой игрок за пять тысяч убийств собирает лучшее в каждый слот (16). Разрыв в девять раз не догоняет ничто — сила мобов линейна по уровню, сила вещи линейна по её уровню, а редкость сидит сверху множителем. Прогон полного пути показал это временем убийства: двадцать секунд на втором уровне против полутора на сотом |

## 1. Карта модулей

### Точка входа

| Файл | Отвечает за | Импортирует |
|---|---|---|
| `src/main.ts` | композиционный корень. Четыре ветки: `debugRoute()` даёт `'balance'` / `'ui'` / null, `presetName()` — режим съёмки. На отладочных страницах и в режиме съёмки игровой цикл не запускается и сейв не трогается. Каждая ветка ставит `data-ready` на `<html>` — по нему Playwright понимает, что страница готова | `App.svelte`, `ui/BalancePage.svelte`, `ui/ShowcasePage.svelte`, `ui/route`, `ui/preset`, `stores/game` |
| `src/App.svelte` | компоновка экрана вокруг боевой сцены: сцена (или боевая панель в текстовом режиме) видна ВСЕГДА, под ней лог и HUD данжа, ниже вкладки разделов и один активный раздел. На десктопе бой — три колонки (герой / сцена / кнопки умений), на мобильном сцена поднимается наверх через `order`. Никакой логики | все компоненты `ui/`, `stores/ui` |
| `src/app.css` | глобальная основа: импорт `ui/fonts.css` и `ui/tokens.css`, сброс `box-sizing`, фон/цвет/шрифт `body`, одно кольцо фокуса на всю игру. Собственных чисел не имеет | `ui/fonts.css`, `ui/tokens.css` |
| `src/vite-env.d.ts` | объявление `__BUILD_TIME__` (подставляется Vite define) | — |

### `src/lib/game/` — логика

| Файл | Отвечает за | Импортирует |
|---|---|---|
| `numbers.ts` | реэкспорт `Decimal` (единственная точка импорта break_infinity) и `formatNumber` (1.23K/…/1.00e15) | break_infinity.js |
| `rng.ts` | тип `Rng`, `createRng(seed)` (mulberry32), `randRange(rng, min, max)` (при min = max поток не расходуется), `randomSeed()` без Math.random. Единственный источник случайности | numbers |
| `events.ts` | шина `AttackEvent`: `emit`/`subscribe`. Логика эмитит, UI подписывается (задел под всплывающие числа урона) | types |
| `stats.ts` | **конвейер статов — единственный источник правды производных чисел**: 13 стат, модификаторы {stat, kind: **base**/flat/percent/multiplier, value, source}, порядок base → +flat → ×(1+Σpercent) → ×multiplier; `base` заменяет дефолт `BASE_STATS` (последний выигрывает при дублях); производный `swingTime = weaponSpeed/(1+haste)` вне `StatId` — модификатор на него невозможен; `collectModifiers` собирает источники в порядке экипировка → апгрейды; кеш `statsDirty`/`ensureStats` (прогресс замаха трогать не нужно — он в долях); `applyModifiers` (чистое ядро), `explainStat`/`explainSwingTime` для панели | numbers, state (тип), data/{balance,upgrades,slots} |
| `dungeons.ts` | правила данжа ОБЕИХ сложностей: `dungeonStatus` (коды отказа `level`/`wrong-zone`/`dead`/`already-inside`), `enterDungeon(state, id, difficulty)`, `leaveDungeon`, `advanceDungeon` (цепочка боссов; флаг достижения по ключу `clearKey`), `enrageMultiplier`/`secondsToEnrage`, `clearedXpBonus` (у героики свой, более крупный) | numbers, state, zones, bossAbilities, data/{dungeons,heroic} |
| `bossAbilities.ts` | три способности героических боссов, и все читаются ТОЛЬКО по `effect.kind` с payload из данных: `bossDispel` (рассеивание по отметкам времени боя), `bossSwingTime` (ускорение ниже доли HP — чистая производная, в состоянии не хранится), `punishResourceSpend` (отдача за трату ресурса, зовётся из единственной точки списания) | numbers, state, data/{dungeons,heroic} |
| `temple.ts` | храм испытаний: `templeStatus` (коды отказа + кулдаун суток), `enterTemple`/`leaveTemple`, `clearTempleWave`, `advanceTemple`, `templeClock` (часы идут только вперёд), `templeSeed` (сид из `saveId` и номера суток). Бросок бойца волны — СВОИМ потоком `createRng`, поток симуляции не вычерпывается | numbers, rng, state, data/temple |
| `quests.ts` | цепочка преквестов: `activeQuest`, `advanceQuests(state, events)` (прогресс из уже существующих событий лога), `goalTarget`/`goalProgress`/`goalDone` (ветвление ТОЛЬКО по типу цели), `progressionGateOpen` (какую ступень лестницы держит цепочка — знают данные) | numbers, state, data/quests |
| `potions.ts` | травничество и зелья: `gatherHerbs` (трава набегает временем, без единого броска rng), `potionSlots`/`potionStatus` (коды отказа `locked`/`dead`/`empty`/`active`), `drinkPotion`, `advancePotions`, `statsWithoutPotions`/`statsWithPotionPlan` — две картины статов для модели боя | numbers, stats, state, data/{herbs,recipes,balance} |
| `enchanting.ts` | распыление и зачарование: `dustValue`, `disenchantStatus`/`disenchantItem`, `enchantStatus`/`enchantItem`. Зачарование живёт полем на вещи, поэтому отдельного списка «наложенных» в состоянии нет | numbers, state, data/enchants |
| `talents.ts` | правила дерева: `earnedPoints`/`availablePoints` (очки — производная от уровня, отдельного счётчика нет), `heroBranches`/`heroTalents` (дерево берётся ПО КЛАССУ), `talentStatus` (коды отказа `other-class`/`no-points`/`max-rank`/`branch-locked`), `investTalent`, `resetTalents`/`resetCost`, и девять читателей флагов поверх ОДНОЙ `flagPayload` — ни одного `if (талант === '...')`. Чистые производные (`rankOf`, `talentModifiers`) живут в данных и оттуда же читаются `stats.ts` — цикла импортов нет | numbers, stats, state, data/{talents,balance} |
| `rotation.ts` | раскладка ротации по приоритетам под ограничение маны: `rotationRate(stats, settings, plan)`, где `plan` — две оси, «через задержку реакции или сразу» и «только отмеченное галкой или всё». Отсюда берутся обе цифры урона в секунду; своей формулы урона нет | numbers, combat, state, data/{abilities,balance} |
| `abilities.ts` | активные умения: `abilityStatus` (коды отказа `dead`/`cooldown`/`gcd`/`no-mana`), `useAbility` (мгновенное бьёт сразу, `onNextSwing` встаёт в очередь / снимается с неё), `consumeQueuedAbility` (замах заменяется умением, мана списывается здесь), `advanceCooldowns`, `autocastStep` (таймер реакции по каждому умению + применение первого доступного по приоритету) и `autocastCandidates`. Своей формулы урона нет — зовёт `rollSwing` с долей `weaponDamagePercent` | numbers, combat, state, data/{balance,abilities} |
| `zones.ts` | доступ к зонам (`isZoneUnlocked`), переходы (`travelToZone`, `activateAbility`, `setAbilityAutocast`, `moveAbilityPriority`, `investTalentPoint`, `resetTalentTree`, `enterDungeonRun`, `leaveDungeonRun`, `reviveInZone`, `retreatZone`) и **честный прогноз опасности**: `zoneRate` (темп зоны — среднее по пулу × уровням, смертность один раз на зону по средней потере HP) и `forecastZone` → `ZoneForecast` с кодом вердикта `safe/risky/deadly/hopeless`. Своей формулы боя нет — зовёт `estimateCombatRate` | numbers, combat, state, data/{balance,zones} |
| `equipment.ts` | надеть/снять/сравнить: **`equipStatus` — правила ХВАТА с кодом отказа** (`two-handed-needs-both`, `shield-offhand-only`, `occupied-by-two-handed`; текст рендерит UI), `equipItem` (снятое возвращается в сумку; отказ — ничего не делать), `unequipItem` (нужен свободный слот сумки), `isEquipped`, `isUpgrade` и `upgradeShare` (**строго по `estimateCombatRate().killsPerSecond`**, не по сумме статов; автонадевания больше нет — решение принимает игрок), `compareItem` → `EquipComparison` с производными числами для UI | combat, stats, state, data/{balance,slots,items}, types |
| `combat.ts` | **ЕДИНСТВЕННЫЙ дом боевых формул**: `rollSwing` (бросок урона оружия + вклад силы атаки + крит), `rollMonsterDamage`, `swingDamageRange`/`expectedSwingDamage`/`critFactor`, `estimateCombatRate` (урон/с, убийств/с, uptime, время до смерти; цикл «фарм → смерть → воскрешение»). tick вызывает эти функции, оффлайн-агрегат — только `estimateCombatRate`; своей формулы нет ни у кого | numbers, rng, state, stats, data/balance, types |
| `formulas.ts` | `xpToNextLevel` — цена уровня из ТАБЛИЦЫ УБИЙСТВ (`killsToNextLevel`) через опыт настоящего моба своей полосы (`zoneForMonsterLevel`), а не из подобранной степени; `applyXp` (перенос остатка, мультиуровень, остановка на `LEVEL_CAP`) | numbers, data/{balance,zones,monsters}, types |
| `loop.ts` | планировщик: rAF + аккумулятор, фикс. шаг `STEP_MS=100`, максимум 10 шагов/кадр, сброс «долга»; метрики fps/tps; `setSpeed(m)` — дебаг-ускорение игрового времени (лимит шагов за кадр сохраняется). Лимита кадров нет: он резал только частоту пробуждений, а единственная опция производительности — текстовый режим. Технические константы живут здесь — это не баланс | — |
| `state.ts` | `GameState`, `createInitialState(seed?)`, `spawnMonster`, `pushEvent`, `COMBAT_LOG_SIZE`. Общая зависимость tick/loot/save — взаимных импортов между ними нет | numbers, formulas, rng, data/{monsters,balance}, types |
| `tick.ts` | **конвейер тика** из шести чистых шагов `(state, ctx) => state` (см. раздел 3); реэкспорт state-модуля для совместимости | numbers, formulas, loot, rng, state, data/{balance,monsters}, types |
| `upgrades.ts` | `buyUpgrade`, `ownedCount` | numbers, formulas, state, types |
| `loot.ts` | `rollRarity` и `rollSlot` (взвешенные рулетки), `rollLoot` (шанс → редкость → слот → имя → модификаторы), `armorMods`/`averageArmorMods` (главный атрибут разыгрывает лут, а не слот), `stashLoot` (три исхода: в сумку, на продажу, вытеснить худшее), `sellItem` (надетое продать нельзя)/`sellPrice` | numbers, rng, state, equipment, data/{rarity,loot,slots,items}, types |
| `save.ts` | формат сейва v19, миграции с нулевой версии, localStorage (инжектируемый), оффлайн-прогресс агрегатом (плюс сбор трав и дожиг зелий), base64 экспорт/импорт, коды ошибок загрузки. Миграция 18→19 переносит старые ранги талантов картой соответствия внутри класса; непереносимое возвращается свободными очками | numbers, formulas, state, stats, potions, data/* |
| `simulate.ts` | **прогон баланса без UI**: `simulate({hours, zoneId, build, freezeLevel, travel})` крутит настоящий `tick` N игровых часов и отдаёт метрики (убийств/золота/опыта/смертей в час, время до следующего уровня, итоговый уровень, урон за ману). Своей модели боя нет; метрики читаются только по наблюдаемому состоянию и шине `AttackEvent`. `BALANCE_PRESET` — общие числа для теста и страницы `/balance` | numbers, loop, rng, formulas, state, stats, tick, loot, zones, data/* |
| `__fixtures__/presets/build.ts` | сборка пресетов `fresh` / `mid` / `rich` настоящими функциями игры (покупка апгрейдов, вложение очков, надевание лута) и выгрузка их в json через `payloadFromState`. В бандл не попадает — импортируется только тестом | numbers, rng, state, stats, formulas, equipment, loot, talents, zones, save, data/* |
| `index.ts` | фасад: реэкспорт всего публичного | все выше |

Тесты: рядом с кодом (`*.test.ts`) + `__tests__/golden.test.ts` (характеризационный,
час симуляции против эталона `__snapshots__/golden.json`) и `__tests__/fixtures.test.ts`
(реальные сейвы v0…v7 из `__fixtures__/`).

### `src/lib/audio/` — звук

| Файл | Отвечает за | Импортирует |
|---|---|---|
| `mixer.ts` | ЧИСТОЕ расписание звука: выбор варианта (никогда два одинаковых подряд), разброс высоты и громкости, склейка одинаковых событий в окне 150 мс, потолок голосов, приглушение фона. Ни three, ни WebAudio — поэтому все правила против усталости слуха проверяются числами в node | numbers, data/{balance,sounds} |
| `manager.ts` | ворота (`isAudible`: жест игрока, громкость, скрытая вкладка, текстовый режим, ускорение отладки), отображение события боя на кью (`cueForEvent`, лут — по редкости), `SoundPlayer` и подписка на шину лога. Слой ТОЛЬКО ЧИТАЕТ: ни одного экшена стора игры | mixer, game/events, data/{sounds,rarity} |
| `engine.ts` | WebAudio и ничего больше: контекст создаётся ТОЛЬКО по жесту, кеш хранит промис буфера, узлы громкости на категорию, снятие связей после отзвучавшего сэмпла | data/balance, mixer, manager |
| `index.ts` | сборка слоя: `startAudio`, `unlockAudioOnGesture`, `attachUiSounds` (щелчки одним слушателем на документ — примитивы `ui/kit` про звук не знают), свой поток случайности | stores/{game,ui}, engine, manager, game/rng |

### `src/lib/data/` — статические данные и баланс

| Файл | Содержимое |
|---|---|
| `balance.ts` | **общий баланс**: `BASE_STATS`, `UNARMED`, `PER_LEVEL` и `PER_LEVEL_ATTRIBUTES`, ставки атрибутов (`STR_ATTACK_POWER`, `AGI_HASTE`, `AGI_CRIT`, `INT_MAX_MANA`, `VIT_MAX_HP`, `VIT_BLOCK_VALUE`), `LEVEL_CAP` (100) и таблица `KILLS_PER_LEVEL` с `killsToNextLevel`, `AUTOCAST_DELAY_MS`, `AUTOCAST_MAX_LOSS`, `OFFLINE_EFFICIENCY` (0.2), `TALENT_FIRST_LEVEL`, уровни открытия механик (`POTION_UNLOCK_LEVEL` 40, `ENCHANT_UNLOCK_LEVEL` 50, `UNIQUE_RECIPE_LEVEL` 60), `itemLevelScale`, коридор темпа (`TTK_*`), 24 слота инвентаря, потолок оффлайна и его шаг, автосейв, воскрешение |
| `monsters.ts` | роли мобов (`RUNT`/`COMMON`/`BRUTE`), база моба первого уровня и **линейный масштаб от уровня** (`MONSTER_GROWTH`): hp, урон и награда растут прямой, потому что сила героя тоже растёт прямой — на вещах, а сила вещи линейна от её уровня; `buildMonster(архетип, уровень, множитель зоны)` |
| `abilities.ts` | по три умения на класс, открываются УРОВНЯМИ (`unlockLevel` 1/4/8): мгновенное и два «на следующий замах». Урон — доля удара оружия, не множитель к силе атаки |
| `dungeons.ts` | **шаблон**: восемь `DungeonSpec` (тир, зона входа, уровень, тир лут-пула, реагент, интерьер), `CHAIN` из трёх ступеней и `TIER_GROWTH`; `buildDungeon(spec, difficulty)` разворачивает их в цепочку боссов обычной ИЛИ героической сложности. Сто сорок четыре числа боссов выведены, а не выписаны |
| `heroic.ts` | героика ОДНОЙ записью (`HEROIC`): множители, сдвиг ярости, тир лута, бонус к опыту и три способности боссов по МЕСТУ в цепочке |
| `talents.ts` | шесть веток по три на класс, каждая ровно 61 очко: 13 этажей, `BRANCH_RANKS`, требование этажа 0/5/…/60, `CONCEPT_ROWS` (5, 9, 13) — концептуальные таланты в один ранг. Эффект — либо модификаторы `stats.ts`, либо ФЛАГ с payload; `rankOf`, `talentModifiers(ranks, classId)` и `fillBranchRanks` — чистые производные |
| `zones.ts` | 20 зон полосами по пять уровней мобов (1-5, 6-10 … 96-100): пул архетипов, множитель награды, требование по уровню, флаг безопасной; `zoneForLevel` (самая дальняя открытая) и `zoneForMonsterLevel` (полоса, которая по силам — на ней держатся цена уровня и уровни боссов) |
| `temple.ts` | храм испытаний: параметры входа, пул бойцов, `TEMPLE_WAVE_GROWTH` (единственная геометрия в боевых числах — забег обязан кончиться), сутки попытки и рубежи наград |
| `quests.ts` | цепочка из семи преквестов: цель одного из ЧЕТЫРЁХ перечислимых типов, награда в единицах игрока, `opensStepId` — какую ступень лестницы отпирает цепочка |
| `progression.ts` | лестница открытий: десять ступеней по десять уровней, что именно открывается и ссылки на данжи и механики |
| `herbs.ts` | три травы: зоны и `perMinute` — трава СРЕЗАЕТСЯ ВРЕМЕНЕМ, а не падает с моба, и поэтому это отдельный тип данных |
| `enchants.ts` | зачарования по слотам: цена в пыли, модификаторы конвейера, `DUST_BY_RARITY` и `ENCHANT_FLAT_STATS` (где плоская прибавка не обесценится с уровнем) |
| `procs.ts` | проки реликвий: шанс, внутренний кулдаун и эффект (лишний удар долей удара ОРУЖИЯ либо возврат доли запаса HP). Таблица реликвий выведена ПО ТИРАМ данжей |
| `reagents.ts` | по реагенту на тир каждой сложности: обычный и героический. Ради них в конкретную ступень и ходят |
| `materials.ts` | материалы зон: вес в рулетке и список зон |
| `recipes.ts` | четыре профессии (кулинария, кузнечное дело, травничество, реликварий), рецепты и три вида выхода (`FoodOutput`, `ItemOutput`, `PotionOutput`); уникальные рецепты реликвий выводятся из тира данжа |
| `rarity.ts` | 5 тиров: веса рулетки, цвета редкостей (единственное место), ПОЛОГАЯ лестница `bonusMult` (1…2.3) и крутая `sellMult` (1…30) |
| `loot.ts` | прилагательные, шанс дропа, базовая цена, имя-заглушка битого предмета |
| `slots.ts` | семь слотов, их названия и веса в рулетке дропа |
| `items.ts` | оружие с заметно разной скоростью и **одинаковым** отношением средний урон/скорость, щиты, `ARMOR_ATTRIBUTES` (главный атрибут брони, который разыгрывает лут), существительные брони по слотам |
| `sprites.ts` | реестр картинок боевой сцены: путь, автор, лицензия; спрайт героя, спрайты мобов и МАППИНГ «архетип моба → спрайт» (босс и запасной силуэт отдельными записями), фоны ПОЛОСАМИ уровней (`BACKGROUND_BANDS`, смыкаются без дыр). `monsterSpriteFor`, `backgroundForLevel` — чистые выборки; покрытие архетипов и смыкание полос держит `content:check` |
| `render.ts` | бюджеты слоя представления — НЕ баланс: потолок всплывающих чисел (24, на телефоне 10), срок их жизни, множитель скорости, выше которого чисел нет, длительности вспышки/крита/выпада/лечения/затухания. Живут в data по тому же правилу, что и баланс: настраиваемые числа не закапываются в компонентах |

### `src/lib/stores/game.ts` — мост логика↔UI

`writable(GameState)` (наружу `readonly`), `loopMetrics`, `offlineReport`,
`saveNotice` (**коды** `NoticeCode`, не строки). Экшены:
`sellInventoryItem`, `equipInventoryItem`, `unequipSlot`,
`disenchantInventoryItem`, `enchantInventoryItem`, `drinkPotion`, `craftRecipe`,
`travelToZone`, `activateAbility`, `setAbilityAutocast`, `moveAbilityPriority`,
`investTalentPoint`, `resetTalentTree`, `enterDungeonRun(id, difficulty)`,
`leaveDungeonRun`, `enterTempleRun`, `leaveTempleRun`,
`setRestThreshold`, `interruptRest`,
`exportSaveString`, `importSaveString`, `initGame`,
`persistNow`, `startGameLoop`/`stopGameLoop`. Rng создаётся один раз из
`state.rngSeed`. Автосейв: счётчик копит шаг конвейера `applyAutosaveCounter`,
стор проверяет его после тика, сохраняет и сбрасывает.

### `src/lib/stores/telemetry.ts` — интервал решений

Локальная телеметрия: сколько проходит между решениями игрока (смена зоны,
надевание, очко таланта, порог привала, приоритет и резерв автокаста, крафт).
Отметки живут в памяти вкладки, наружу не уходит ничего — ни сети, ни
хранилища, ни идентификаторов, и это закреплено тестом по исходнику. Наружу
отдаёт медиану (не среднее: один долгий перерыв не должен красить сессию) и
флаг тревоги; показывает их отладочный оверлей.

### `src/lib/stores/ui.ts` — состояние экрана

Модуль держит ТРИ РАЗНЫЕ ВЕЩИ, и разделены они по одному признаку — что
переживает перезагрузку, а что нет.

**1. Настройки машины — переживают, в СВОЁМ ключе localStorage**
(`idle-rpg:ui`), намеренно отдельно от сейва: текстовый режим и громкость —
свойства машины, а не прогресс, и переезжать вместе с экспортом сейва не
должны. Отсюда `isTextMode()` (текстовый режим — только явный выбор `'on'`:
автоопределения нет, потому что двумерной сцене нечем не завестись),
`setTextMode`, `setVolume` и `sanitizeUiSettings` — разбор, который собирает
НОВЫЙ объект, а не чинит прежний, поэтому лишние ключи прежних сборок
(`fpsLimit`, `drawers`) отбрасываются сами.

**2. Открытое меню — НЕ переживает.** `MENU_IDS` (семь: `hero`, `bag`,
`world`, `talents`, `craft`, `log`, `settings`), `MENU_SIDE` (сторона: слева
где меняешь, справа где читаешь — ПРАВИЛО ЛЕЖИТ ДАННЫМИ, а не расстановкой в
разметке), `menusOn(side)`, стор `openMenu` и три действия — `setMenu`,
`toggleMenu` (повторное нажатие закрывает), `closeMenu`. Одно меню за раз.
Ни в сейв, ни в настройки машины не пишется: это «где я сейчас», а не
настройка, и после перезагрузки игрок хочет видеть бой.

**3. Что игрок несёт — НЕ переживает.** `Carry` — либо находка из сумки
(`{ from: 'bag', itemId }`), либо надетое (`{ from: 'slot', slot }`);
третьего источника предметов в игре нет. Стор `carriedItem` и действия
`takeItem` / `toggleCarried` / `releaseItem`, плюс чистая `sameCarry`.
Перетаскивание — состояние экрана, а не игры: «я держу клинок над курткой»
переживать перезагрузку не должно.

### `src/lib/ui/` — дизайн-система, компоненты и модуль маршрута

**Токены и шрифт.** `tokens.css` — единственный источник визуальных величин:
палитра (поверхности, текст, акцент, семантика), шкала отступов 4/8/12/16/24/32,
радиусы, тени, типографика, длительности, брейкпоинт 720px. Тема одна, тёмная;
переключалки нет. Цветов редкостей в токенах намеренно нет — они в
`data/rarity.ts`, а в CSS попадают через `--rarity-color` из `kit/rarity.ts`.
`fonts.css` объявляет Inter из `public/fonts/` (кириллица + латиница,
переменный шрифт, один файл на подмножество) — из сети ничего не тянется.

**Примитивы `ui/kit/`** — не знают про игровое состояние, только пропсы:
`Panel` (поверхность с заголовком/подвалом, тона plain/quiet/raised, dense),
`Button` (primary/ghost/danger × sm/md, disabled и loading разведены),
`StatBar` (значение числами, семантика тона, плавность за игровым тиком),
`IconSlot` (ячейка предмета: рамка по редкости, состояние «пусто»),
`Tooltip` (чистый CSS, hover и focus, принудительное открытие для витрины),
`NumberText` (`formatNumber`, табличные цифры, тон и знак),
`Tag` (редкость из данных либо семантический тон).
`kit.test.ts` держит правило «ни hex-литерала, ни отступа числом» на всех
компонентах — тест проверен на намеренно испорченном файле.

**Как устроен экран.** Сверху вниз: `NoticeBar`, ПОСТОЯННАЯ ЗОНА
(`[data-permanent]`, четыре блока — сцена, ряд действий, полоски героя,
порог привала), затем строка `.menus` из трёх частей — левый столбец кнопок,
панель открытого меню (`.pane`), правый столбец кнопок.

Пустая панель ОСТАЁТСЯ В РАСКЛАДКЕ РАСПОРКОЙ (`min-height: 0`), а не
исчезает: убранная через `display: none`, она схлопывала строку, и правый
столбец переезжал к середине экрана поверх отладочной панели, которая и
перехватывала нажатия. Столбцы липнут к верху окна (`sticky`) — меню бывают
на десять экранов.

Открытое меню УВОДИТ СЦЕНУ В УГОЛ: `.stage.mini` уходит из потока
(`position: fixed`, правый нижний угол, ширина из `data/render.ts`). На узком
экране угла нет — вместо сцены встаёт `BattlePanel compact`, одна строка с
именем, уровнем и здоровьем моба. Ширину окна для этого выбора берёт
подписка `bind:innerWidth`, а не медиазапрос: медиазапрос умеет спрятать
элемент, но не умеет подменить компонент.

На мобильном оба столбца кнопок сливаются в полосу у низа окна
(`position: fixed`) и делят её по числу кнопок — пять слева, две справа;
место под неё в конце страницы резервирует `.tabbar-space`.

**Боевая часть (видна в любом разделе).** `BattleScene` — боевая сцена,
`BattlePanel` — та же информация текстом: она встаёт на место сцены в текстовом
режиме, при `?scene=off` и — пропом `compact`, одной строкой — вместо сцены на
телефоне под открытым меню. `CombatLog` — лента меню «Журнал»; **рендер событий
лога** (весь русский текст боя) живёт в её функции `eventText`. `ActionBar` —
одинаковые квадратные кнопки умений И ЗЕЛИЙ с заливкой кулдауна и сквозной
нумерацией хоткеев; здесь же ЕДИНСТВЕННЫЙ на игру `svelte:window onkeydown`,
поэтому спрятать бар под меню нельзя — хоткеи умрут глобально (держится
тестом по случаю на каждое из семи меню). `VitalsBar` —
одна строка с уровнем и полосками. `DungeonHud` — место в цепочке боссов,
метка героики, уловка текущего босса и выход; `TempleHud` — номер волны,
рекорд и выход из храма. Тексты причин отказа живут рядом с фичами:
`abilityText.ts`, `potionText.ts`, `questText.ts`. `statText.ts` — знак и
число ОДНОЙ прибавки к стату: `formatNumber` режет всё до целого (он про
золото и урон), и через него дробные прибавки талантов показывались нулём
(«+0 множителя крита», «+-0 паузы»). Собирают такие строки три места —
таланты, предметы, разбор стата — и считают они их теперь одинаково.

**Семь меню.** «Герой» — `StatsPanel` (клик по стате раскрывает раскладку по
источникам), `EquipmentPanel` и `InventoryPanel` рядом; ИСКЛЮЧЕНИЕ ОСОЗНАННОЕ
— иначе перетаскивать находку на слот неоткуда, два меню разом не
открываются. «Сумка» — только `InventoryPanel`. «Мир» — `ZonePanel`,
`TemplePanel`. «Таланты» — `TalentPanel` (три ветки СВОЕГО класса),
`AbilityPanel`, `ProgressionPanel` (лестница открытий), `QuestPanel`.
«Крафт» — `CraftPanel`, `EnchantPanel`. «Журнал» — `CombatLog`.
«Настройки» — `SettingsPanel`.

**Кукла и перетаскивание.** `MenuButtons` рисует один столбец кнопок (сторона
приходит пропом, состав — из `MENU_SIDE`), `menuText.ts` держит названия и
иконки. `dropTarget.ts` — ЧИСТАЯ функция «можно ли положить сюда»: одна на все
три пути (перетаскивание, «нажал вещь → нажал слот», долгое нажатие) и на
клавиатуру. Она сводит ответы `equipStatus` / `unequipStatus` в тройку
`{ fits, allowed, reason }`; `fits` — «слот про эту вещь» (подсветить),
`allowed` — «можно прямо сейчас», и разница между ними и есть строка отказа.
`axisText.ts` — общие названия, форматирование и строки ДВУХ осей апгрейда:
ими пользуются и окно сравнения (`ItemCompare`), и подсказка куклы, поэтому
числа там совпадают по построению. `IconSlot` получил состояния
`drop='carried'|'target'|'dim'`, `draggable` и видимое кольцо фокуса.

**Что где ещё.** `ZonePanel`
собирает предупреждение из вердикта и разрыва в уровнях — в данных зоны
такого текста нет. `ItemMods` — модификаторы предмета человеческим текстом.
`SettingsPanel` — экспорт/импорт сейва, текстовый режим, громкость,
ссылки на CREDITS и issues. Дальше `OfflineModal`, `NoticeBar` (карта
`NoticeCode` → текст),
`DebugOverlay` (`?debug=0` скрывает), `DebugPanel` (рендерится только при
`?debug=1`: множитель скорости симуляции ×1/×10/×100 через `loop.setSpeed`,
чит-кнопки дебаг-экшенами стора, удары/мин по шине, сид rng, ссылка на прогон
баланса), `BalancePage` (отдельная страница `/balance` под `?debug=1`: те же
таблицы, что печатает `game/__tests__/balance.test.ts`, посчитанные на лету
через `simulate`; между строками уступает кадр браузеру, чтобы таблица
наполнялась постепенно).

`ShowcasePage` — витрина `/ui` под `?debug=1`: вся палитра, все редкости,
вся шкала типографики, отступы, радиусы, тени и каждый примитив во всех
состояниях на одной странице.

`route.ts` — не компонент: `debugRoute(location)` отвечает, какая отладочная
страница открыта (`'balance'`, `'ui'` или null), `presetName(location)` —
какое заранее заданное состояние просят снять; обоим нужен `?debug=1`.
`isSceneDisabled(location)` (`?scene=off`) — единственный, кому `?debug=1`
не нужен: он ничего не подменяет, а только убирает картинку.
Пути понимает два — чистый `/idle-rpg/ui` (его отдаёт GitHub Pages через
`404.html`, копию `index.html` делает плагин `pagesSpaFallback` в
`vite.config.ts`) и хеш `#/ui`, работающий на любом статическом хостинге.

`preset.ts` — загрузка состояния для съёмки: находит json через
`import.meta.glob` (в основной бандл пресеты не попадают, грузятся отдельными
чанками только при `?state=`), прогоняет его через ту же `migrateSave` +
`stateFromPayload`, что и живой сейв, и отдаёт в `applyScreenshotState`.

### `src/lib/types/index.ts`

Только используемое: `Monster` (с `level`), `MonsterTemplate`, `UpgradeDef`, `Rarity`, `Item`
(`{id, name, rarity, slot, mods}` — прямых полей бонуса нет), `CombatEvent` (включая `ability` и `effect` — с id умения, а не именем),
`AttackEvent` (у умения `abilityId` заполнен). `Decimal` импортируется из `game/numbers`, не из break_infinity.
Реальный формат сейва — `SavePayloadV20`, экспортируется из `save.ts`.

## 2. Состояние

- Один `writable<GameState>` в `stores/game.ts`; состав — в `game/state.ts`:
  totalTicks, playtimeMs, gold, level, currentXp, xpToNext, swingProgress
  и offhandSwingProgress (доли 0..1), currentHp/currentMana,
  heroState (`alive`/`dead`/`resting`)/reviveMsLeft/restMsLeft/restTotalMs,
  **equipment** (`Record<SlotId, Item|null>`), stats + statsDirty, inventory,
  itemSeq, rngSeed, **saveId**, **currentZoneId**, **lastSurvivedZoneId**,
  monster, respawnMsLeft, combatLog, msSinceAutosave, **gcdMsLeft**,
  **abilityCooldownsMs**, **abilityCharges**, **queuedAbilityId**,
  **activeEffects**, **procCooldownsMs**, **activePotions**, **herbProgress**,
  **materials** (мешок: материалы, травы, еда, склянки, реагенты),
  **enchantDust**, **talents** (id → ранг), **talentResets**,
  **dungeonRun** (данж, СЛОЖНОСТЬ, номер босса, время боя) и **dungeonsCleared**
  (ключ — `clearKey(id, difficulty)`), **templeRun**/**templeBestWave**/
  **templeLastRunAtMs**, **questProgress**, **restHpThreshold**/
  **restSpeedupSource**,
  **abilitySettings** (галка автокаста, приоритет и резерв по каждому умению),
  **autocastReadyMs** (таймер реакции; в сейв не пишется).
  Источники статов — `level`, `talents`, `equipment` (вместе с зачарованиями)
  и `activePotions`; прямых полей урона нет.
- Мутации — только через экшены стора: цикл (tick), надевание/продажа/разбор,
  крафт, глоток зелья, зачарование, вход и выход из данжа и храма, initGame,
  импорт сейва, сброс счётчика автосейва.
- UI читает `$gameState` (readonly); чистые функции (`formatNumber`,
  `sellPrice`, `upgradeShare`) и данные (`RARITY_BY_ID`, `TEMPLE`) компоненты
  импортируют напрямую — это разрешено правилами.

## 3. Бой: дискретные удары в конвейере тика

`tick(state, dtMs, rng, emitAttack?)` — счётчики, затем чистые шаги
`(state, ctx) => state` в фиксированном порядке; факты тика передаются через
контекст (убитый моб, число замахов, голова лога на входе):

1. `applyRevive` — мёртвый герой: отсчёт; по нулю — полный HP и откат в последнюю зону, где он выживал (`lastSurvivedZoneId`), иначе в безопасную
2. `applyRest` — привал: отсчёт и выход с полным запасом и свежим мобом
3. `applyPotions` — длительности зелий; стоит ДО кулдаунов, чтобы истёкшее зелье не било этим тиком
4. `applyCooldowns` — кулдауны, GCD и заряды умений идут игровым временем
5. `applyBossDispel` — рассеивание героического босса по отметкам времени боя; стоит ДО ярости, чтобы обе читали один и тот же `fightMs`
6. `applyEnrage` — время боя с боссом; по нему растёт его урон
7. `applyAutocast` — таймер реакции по каждому умению и применение первого доступного по приоритету
8. `applyPendingKill` — моб, добитый мгновенным умением ВНЕ тика: смерть оформляет конвейер, а не умение
9. `applyCombat` — удары правой руки по прогрессу замаха (доля `dt/swingTime`, остаток переносится); умение из очереди ЗАМЕНЯЕТ автоатаку; талант-флаг может добавить повторный замах
10. `applyOffhandCombat` — удары левой руки по своему таймеру
11. `applyProcs` — броски проков надетых вещей по ударам ЭТОГО тика; урон прока может добить моба
12. `applyEffects` — тики урона по времени; стоят ДО наград, потому что тоже могут убить моба
13. `applyKillRewards` — золото + событие `kill` (с id архетипа и зоны); талант-флаг может срезать откаты
14. `applyLevelUps` — опыт через `applyXp` + событие `levelup`. Число опыта считает одна `killXp`: награда моба × бонус за пройденные данжи × доля за разрыв уровней (`xpGapShare`). Шаги 13 и 14 берут её обе, поэтому в лог и в прокачку идёт одно и то же число, а не два похожих
15. `applyMaterialDrop` — материал зоны и реагент босса
16. `applyLootDrop` — бросок дропа, затем `stashLoot`: в сумку, на продажу или вытеснить худшее
17. `applyTempleWave` — волна храма засчитана; стоит ДО решения об отдыхе, иначе смерть в том же тике съела бы рекорд
18. `applyRestCheck` — пора ли на привал (в данже и храме привала нет)
19. `applyLethalCheck` — герой мог упасть не от удара моба (героическая отдача)
20. `applyMonsterAttack` — ответные удары моба по своему `swingTime`; блок, отражение и возврат ресурса — здесь же
21. `applyResourceGain` — доход ресурса класса
22. `applyRegen` — HP (в бою `hpRegen`, вне боя `hpRegenOutOfCombat`), ресурс
23. `applyRespawn` — пауза после смерти моба; в данже ведёт к следующему боссу, в храме — к следующей волне
24. `applyHerbGather` — сбор трав временем; ни одного броска rng
25. `applyQuests` — прогресс заданий из событий, которые этот тик уже объявил
26. `applyAutosaveCounter` — копит игровое время для автосейва

rng потребляется в порядке: бросок урона оружия, крит-ролл, затем (на убийстве)
шанс дропа / редкость / слот / прилагательное / существительное-модель. Бросок урона моба поток не трогает, пока `damageMin = damageMax`. Лог — последние 8 событий. Формулы боя (все — в `combat.ts`):

```
swingTime   = weaponSpeed / (1 + haste)
swingDamage = (rand(weaponDamageMin, weaponDamageMax)
               + attackPower × weaponSpeed / AP_NORMALIZATION) × weaponDamagePercent
                                                    (AP_NORMALIZATION = 14)
```
`weaponDamagePercent` равен 1 у автоатаки и берётся из данных у умения — поэтому
умения масштабируются от оружия и делят с боем одну формулу. Побочный эффект,
ради которого так и сделано: при равном уроне в секунду медленное оружие даёт
БОЛЬШЕ урона за ману, чем быстрое (цена умения фиксирована, а удар крупнее).

Во второй формуле стоит БАЗОВАЯ `weaponSpeed`, а не ускоренная `swingTime`: иначе
haste сократился бы сам с собой и не повышал урон в секунду. Медленное оружие
получает больший вклад силы атаки за удар — ровно настолько, чтобы сравняться
по урону в секунду с быстрым (14 силы атаки = +1 урона в секунду при любой скорости).
База — `BASE_STATS` в `data/balance.ts`, безоружные значения — `UNARMED`
(скорость 2.0с, урон 8–12), апгрейд — flat-модификатор +14×счётчик к силе атаки.
Надетое оружие ЗАМЕНЯЕТ базу тремя модификаторами `kind: 'base'`
(`weaponSpeed`, `weaponDamageMin`, `weaponDamageMax`, source `equipment:weapon`);
снятое — возвращает `UNARMED`. Прогресс замаха хранится долей 0..1, поэтому
переодевание в середине замаха не даёт ни сброса, ни мгновенного удара.
Темп боя (dps с матожиданием критов, убийств/с) считает ТОЛЬКО
`estimateCombatRate` — им пользуются и UI, и оффлайн-агрегат; тест держит
расхождение оффлайна с реальной симуляцией часа в пределах 5%.
Спавн берёт моба из пула ТЕКУЩЕЙ зоны: бросок уровня из диапазона, затем бросок
архетипа. Лут в оффлайне не выпадает.

### Штраф опыта за отставание

`XP_GAP_PENALTY` в `data/balance.ts` — ступени `gap <= maxGap -> share`, где
`gap = уровень героя − уровень моба`. Разрыв берётся по КОНКРЕТНОМУ мобу, а не
по зоне: полоса зоны шириной пять уровней, и одно число на полосу соврало бы
половине её пула — на границе ступени половина мобов платит полную награду, а
половина половинную.

Штраф бьёт ТОЛЬКО по опыту. Золото, материалы, травы и лут не трогаются, и это
не оговорка: низкие материалы нужны для рецептов на любом уровне, и если
отставшая зона перестанет платить и за них, идти за ними станет некуда.

Знают про штраф все, кто считает опыт во времени: `killXp` в тике, `zoneRate`
в `game/zones.ts` (а через неё прогноз зоны и оффлайн-агрегат). Кривая
СТОИМОСТИ уровня (`xpToNextLevel`) его не знает намеренно — она считается по
зоне, куда игра ведёт героя, где разрыва нет по определению; добавь штраф туда,
и уровень дешевел бы ровно настолько, насколько дешевеет моб.

Прогноз зоны отдаёт `xpShare` (1 / 0.5 / 0) по СРЕДНЕМУ уровню мобов — тем же
разрывом, что и `levelGap` рядом с ним, чтобы два числа в одной строке
интерфейса были об одном и том же. Настоящее начисление идёт по каждому мобу,
поэтому зона на границе ступени платит между двумя долями; точное число видно
там же, в `xpPerHour`.

Эффект замерен прогоном: до штрафа оба крайних стиля игры (сидеть на месте /
переезжать в лучшую зону) давали ОДИН И ТОТ ЖЕ результат — стартовый луг
оставался лучшей зоной до восемнадцатого уровня, и переезд не окупался вовсе.
Со штрафом переезд наконец окупается, ценой полутора-двух минут на первой
десятке уровней.

## 4. Сохранение

- Версия формата: **21** (`SAVE_VERSION`), ключ `idle-rpg-save`. Автосейв 15 с
  (значение в `data/balance.ts`) + `visibilitychange`.
- Формат — `SavePayloadV20` (Decimal строками). Кроме прежних полей он несёт:
  мешок (`materials` — материалы, травы, еда, склянки, реагенты), `enchantDust`,
  действующие зелья, внутренние кулдауны проков, заряды умений, `saveId`,
  забег по храму с рекордом и отметкой попытки, прогресс цепочки заданий,
  пороги привала и `dungeonRun` СО СЛОЖНОСТЬЮ; `dungeonsCleared` хранит ключи
  двух видов — голый id обычной версии и `<id>:heroic`.
- Всё, что приходит из сейва, ДЕГРАДИРУЕТ, а не роняет игру: битый забег
  (чужой данж, индекс за цепочкой, чужой храм) выкидывает наружу, ранг чужого
  таланта отбрасывается, а свой режется по `maxRank`; зачарование не для этого
  слота и неизвестный прок отбрасываются, а предмет остаётся носимым;
  неизвестный id зоны деградирует до безопасной; кулдаун чужого умения или
  больше своего максимума подрезается; мусорная сложность забега читается как
  обычная — потерять забег хуже, чем пройти его на ступень легче. Очередь
  `onNextSwing` и наложенные эффекты НЕ сохраняются: они висели на мобе,
  а моб при загрузке спавнится заново.
- Прямых полей урона в формате нет — статы пересчитываются из источников.
  Не сохраняются: monster/respawn, combatLog, xpToNext, msSinceAutosave,
  `herbProgress` (недорезанная доля пучка прогрессом не считается).
- Миграция 19 → 20 ввела ХВАТ. В 19-й версии двуручность жила отдельным полем
  `hands`, а щит не был отмечен никак — его опознавали по слоту, в котором он
  лежал, и формат просто не умел сказать «так нельзя». Хват проставляется по
  МОДИФИКАТОРАМ (базовая скорость оружия против базы блока), а не по слоту:
  правленый сейв мог положить щит куда угодно. Незаконные связки при этом
  расформировываются — двуручное с занятой второй рукой, щит в главной,
  двуручное во второй; снятое уходит в сумку, не влезло — по действующему
  правилу вытеснения в золото уходит самый дешёвый из претендентов.
- Миграция 20 → 21 снесла КУЛДАУН ХРАМА. Попытки в сутки больше нет: награду
  выдают этажи выше рекорда, а рекорд нельзя побить дважды, поэтому запрет
  оказался лишним. `templeLastRunAtMs` выброшен, добавлен `templeCleared`.
  РЕКОРД при этом сохраняется, хотя постановка говорила «рекорд 0»: в 20-й
  версии `templeBestWave` значил ровно то же самое, и им же открыты рецепты
  рубежей — обнулить его значило бы отобрать уже открытые рецепты.
- Миграций двадцать одна, и каждая описана в `MIGRATIONS` рядом со своим
  номером. Последняя, 18→19, самая содержательная: снесено автонадевание,
  уровень подрезан потолком, опыт пересчитан с прежней кривой на новую
  с сохранением ДОЛИ полоски, а ранги старого общего дерева перенесены
  в дерево своего класса картой соответствия — непереносимое возвращается
  свободными очками, и прогресс не теряется ни на очко. Сейв из будущей
  версии и битый JSON → коды `newer-version`/`corrupted`, текст рендерит
  NoticeBar. Фикстуры всех версий — в `__fixtures__`, тесты грузят их
  через `loadGame`.
- ПРЕРВАННЫЙ ЗАБЕГ НЕ ПЕРЕЖИВАЕТ ЗАГРУЗКУ. `resumeOutside` — одна точка на
  весь закрытый контент (данж, храм, будущий рейд): забег расформировывается,
  прогресс цепочки сброшен, лут за убитых боссов остаётся, герой выходит в
  зону из `offlineZone`, и оффлайн считается по ней. Раньше закрытая внутри
  данжа вкладка не приносила ничего — наказание за невнимательность.
  Два свойства при этом РАЗНЫЕ: время не пропадает, но попытка не
  засчитывается. Расформирование живёт в `stateFromPayload`, а не в
  `applyOfflineProgress`: иначе переведённые назад часы (оффлайн не считается
  вовсе) оставили бы героя запертым внутри.
- Оффлайн: считается по модели **автокаста** и режется на `OFFLINE_EFFICIENCY`
  (железное правило `оффлайн <= автокаст <= ручная игра`); сам агрегат забег
  не проходит — цепочка боссов активна, и своей модели боя с боссом у него
  нет; потолок 8 ч, награда агрегатом ШАГАМИ по 1 мин —
  набранные уровни меняют живучесть, а с ней и темп. Травы за оффлайн
  срезаются тем же урезанным временем, а склянки ДОЖИГАЮТСЯ полным: придержать
  зелье, закрыв вкладку, нельзя. Часы назад — ничего.
- ЛУТ В ОФФЛАЙНЕ (долг №5 закрыт): внутри шага по числу убийств крутится та же
  `rollLoot` и та же `stashLoot`, что в тике. Второй модели дропа нет,
  отдельного коэффициента у лута нет — убийства уже урезаны. Оценка ценности
  кешируется на время неизменных СТАТОВ (не на шаг: шагов за восемь часов
  четыреста восемьдесят, и кеш на шаг стоил 947 мс против 176 мс); на правила
  сумки кеш не влияет, это отдельный тест.

## 4b. Слой представления: сцена, оверлей, текстовый режим

Одно состояние — три представления, и ни одно не пишет обратно.

| Кто | Что читает | Что показывает |
|---|---|---|
| `render2d/Scene2D.svelte` | `gameState`, `simSpeed`, `offlineReport`, шина `game/events` | фон зоны, силуэты героя и моба, эффекты удара и всплывающие числа |
| `ui/BattleScene.svelte` (рама) | `gameState` | пропорции (4:3 / 16:9) и подпись поверх: имя, уровень и здоровье моба |
| `ui/BattlePanel.svelte` | `gameState` | то же самое словами и полосками — текстовый режим |

**Движка нет.** Сцена — обычные элементы страницы и CSS: ни three, ни
WebGL, ни канвы. Слои сверху вниз: фон зоны (одна картинка на полосу
уровней) → спрайт моба → спрайт героя → слой эффектов → всплывающие
числа. Заводится в любом браузере, поэтому у сцены нет ни «не завелась»,
ни автоопределения текстового режима: текст — только явный выбор игрока
либо `?scene=off`.

**Правило, которое нельзя нарушать.** Сцена ТОЛЬКО ЧИТАЕТ игровое
состояние. Из `stores/game` ей позволены read-only сторы и ничего больше —
ни одного экшена. Держится тестом `render2d/render2d.test.ts`, который
читает исходники папки и падает на любом другом импорте (проверено
намеренно испорченным образцом). Тот же тест держит запрет на
`setInterval`/`setTimeout` в компоненте: движение сцены идёт от состояния,
а не от своих таймеров.

**Выборка состояния** собрана одной чистой функцией `render2d/model.ts`
(`sceneModel`): фаза сцены (`fight`/`respawn`/`rest`/`dead`), доли здоровья
и прогресса привала, спрайт и фон. Её видно в диффе целиком, и она
проверяется в node; чтение состояния его не меняет — закреплено тестом.
Ради сцены в тик не добавлено ни одного поля.

**Эффекты — состояние, а не ключевые кадры.** Удар ставит метку времени,
каждый тик состояния решает, жив ли ещё эффект, и вешает класс.
Длительности — `data/render.ts`. Так эффект замирает вместе с игрой,
не зависит от `prefers-reduced-motion` (там анимации обрезаны до
миллисекунды; остаются числа и вспышки, уходят тряска и сдвиги) и в
точности воспроизводится позой для эталона: `?pose=<idle|swing|hit|crit|heal|rest>`
на пресете вешает те же классы, только застывшие.

**Всплывающие числа** — очередь `render2d/floaters.ts` с потолком из
`data/render.ts` (24, на телефоне 10). Бюджет держится ОТБРАСЫВАНИЕМ новых,
а не вытеснением старых; выше `FLOATER_MAX_SPEED` отладочного ускорения
чисел нет вовсе; при `document.hidden` не создаются.

**Бюджеты** (`tests/budgets.spec.ts`, замеры с прогона):

| Что | Предел | Факт |
|---|---|---|
| Узлы DOM за 30 мин игрового времени | +5% | держится (сумка и лог прогреваются до замера) |
| Кадры в тяжёлом разделе («Мир») против лёгкого («Настройки») | не ниже 0.8 | держится (мемо панелей, `ui/memo.ts`) |
| Всплывающие числа одновременно | 24 / 10 на телефоне | потолок в `data/render.ts` |
| Всплывающие числа при `document.hidden` | ноль | ноль |

**Текстовый режим** размонтирует сцену, а не прячет её (тест проверяет
`[data-phase]` в нуле). Лимит кадров из настроек (60/30/15) — про игровой
цикл: он режет частоту перерисовки состояния, и сцена, читающая состояние,
режется вместе с ним.

**Картинки — данные.** Реестр `data/sprites.ts`: путь, автор, лицензия,
«архетип моба → спрайт» и «полоса уровней → фон». Сейчас это цветные
силуэты-заглушки в `public/sprites/`, но реестр настоящий: замена картинки
не трогает ни код сцены, ни тесты. Ни одно состояние сцены не блокируется
отсутствием картинки: нет спрайта под архетип — берётся запасной, и это
падение `content:check`, а не пустое место на экране.

**Трёхмерного слоя в проекте нет.** Он удалён целиком — папка `render3d/`,
`three` с загрузчиком моделей, GLB-модели и реестр клипов: около 200 кБ
сжатого бандла и 8.5 МБ ассетов. Бюджеты видеопамяти и правила dispose
ушли вместе с ним. Джоб `build` в CI держит порог основного куска
(900 кБ), чтобы библиотека такого веса не вернулась незамеченной.

## 4a. Визуальная регрессия

`playwright.config.ts` собирает игру, поднимает `vite preview` и снимает два
комплекта, и ОБА сравниваются с эталонами в `tests/screenshots/` попиксельно:

- **интерфейс** (`?scene=off`) — три пресета × три ширины (390 / 768 / 1280),
  СЕМЬ МЕНЮ × две ширины, кукла с подсвеченным слотом во время выбора
  находки, витрина `/ui` и точный снимок значений токенов;
- **сцена** (без `scene=off`) — три пресета × две ширины (390 / 1280),
  семь поз (`?pose=…`: покой, замах, попадание, крит, лечение, привал, новый
  уровень) на пресете `rich` при 1280, уменьшенная сцена в углу при открытом
  меню и строка-сводка вместо неё на 390. Двумерную сцену рисует тот же DOM, что и
  интерфейс, поэтому она воспроизводима до пикселя; прежний запрет
  сравнивать сцену относился к WebGL и ушёл вместе с ним. Эти эталоны —
  замок: убрать сцену или сломать эффект незаметно нельзя.

Оба комплекта пишутся в `test-results/current` до сравнения, поэтому уходят
в артефакт независимо от того, сошлись эталоны или нет. Эталон обновляется
ПО ОДНОМУ: `npm run screenshots:update:one -- "имя снимка"` (список —
`npm run screenshots:list`). Полное обновление ради одного уехавшего снимка
узаконило бы заодно все остальные — включая настоящую регрессию.

Отдельно, измерением, а не глазами, проверяются два требования к мобильному:
на 320px нет горизонтального переполнения (`scrollWidth - clientWidth <= 0`)
и каждая вкладка раздела не ниже 44px.

Воспроизводимость держится на четырёх вещах: игровой цикл в режиме съёмки не
запускается, сид случайности пришивается явно (сейв его не хранит), поверх
пресета прокручивается фиксированное число тиков, а анимации выключены через
`prefers-reduced-motion`. Отладочные оверлей и панель в этом режиме скрыты —
в них живые счётчики и время сборки.

Разделение труда между проверками: картинка ловит вёрстку, размеры и
начертание; цвет она не ловит вовсе (цветной текст — сотые доли процента
пикселей), поэтому палитру стережёт `tests/tokens.spec.ts` — точное сравнение
значений всех токенов, считанных с живой страницы.

Коротким СЛОВОМ картинка тоже не стережётся, и это выяснилось на живой
поломке: заголовок героя сменился с «Воин» на имя класса, вёрстка не
поехала — и сравнение прошло, потому что разница не дотянула до
`maxDiffPixelRatio`. Значит, текст проверяется своими тестами: имена
ресурса и причин отказа — `ui/resource.test.ts`, «занята двуручным» —
проверкой на живой странице в `tests/screenshots.spec.ts`. Эталоны при этом
имеет смысл иногда пересобирать целиком (`--update-snapshots=all`): без
этого на картинке остаётся старое слово, и человек, который смотрит артефакт
глазами, видит вчерашнюю игру.

**Локаторы браузерных тестов живут в `tests/screen.ts`, и меню ищется
по ПОДПИСИ, а не по номеру.** Это не стиль, а вывод из поломки: когда
разделов стало четыре вместо пяти, `nth(4)` начал молча открывать чужую
вкладку, а `nth(3)` — «Настройки» вместо «Мира». Вместе с уехавшими
в выдвижку характеристиками и сменившими класс кнопками умений это
уронило девятнадцать браузерных тестов из шестидесяти двух — и заметно
стало только тогда, когда набор прогнали ЦЕЛИКОМ, а не по одному файлу
со снимками. Отсюда правило: перед PR гоняется весь `npm run test:visual`,
а не только тот файл, который правили.

## 4c. Наборы проверок и CI

Проверки разделены ПО СТОИМОСТИ, потому что одна цифра объясняет всё:
матрица баланса занимала 684 секунды из 770 секунд всего набора юнит-тестов,
то есть 89%. Гонять её после каждой правки — значит ждать одиннадцать минут
ради теста, который правку даже не затрагивает.

| Скрипт | Что внутри | Замер |
|---|---|---|
| `npm run test` | типы + юниты + golden + контент; браузера нет, матриц нет | ~30 с |
| `npm run test:balance` | полная матрица зон/уровней/классов + путь 1..100 | ~13 мин |
| `npm run test:balance:sample` | та же матрица ВЫБОРКОЙ (`BALANCE_SAMPLE=1`) | ~2 мин |
| `npm run test:visual` | сборка + Playwright (63 теста) | ~6 мин |
| `npm run test:all` | всё подряд | ~19 мин |

**Выборка сужает ОХВАТ, а не строгость.** Пороги, сравнения и ассерты —
те же самые до буквы; меняется только число прогоняемых клеток: три зоны
вместо двадцати (начало лестницы, середина, конец — то есть края, где
разница максимальна), два уровня вместо пяти, один класс вместо двух,
вчетверо меньше игровых часов на клетку и половина сидов (меньше двух брать
нельзя: среднее по одному прогону — это уже не среднее, а один бросок).
Тест «оба класса проходят путь за сопоставимое время» в выборке
ПРОПУСКАЕТСЯ целиком, а не смягчается: сравнивать нечего, когда класс один.

**CI — пять параллельных джобов** (`ci.yml`): `typecheck`, `unit`, `content`,
`balance`, `visual`. Общее время равно самому долгому, а не сумме. Два
дорогих джоба запускаются по фильтру диффа: `balance` — если тронуты
`src/lib/data/` или `src/lib/game/`, `visual` — если тронуты `src/lib/ui/`,
`src/lib/render2d/`, `App.svelte`, стили или сами тесты. Метка `full-checks`
на PR включает оба принудительно И переводит баланс на полную матрицу.

Подготовка окружения вынесена в composite action
(`.github/actions/setup`): пять джобов настраивали бы кеш пятью копиями,
и первое же расхождение означало бы, что половина джобов качает зависимости
заново. Кешируются npm (через `setup-node`), браузер Playwright (по хешу
`package-lock.json`) и трансформация vitest (`node_modules/.vite`).

**Артефакт со снимками уезжает при ЛЮБОМ исходе** — это единственный способ
увидеть игру до merge, и оптимизация не имеет права его отнять.

**Ночной прогон** (`nightly.yml`, 03:00 UTC на `main`): `test:all` без
выборок и без фильтров. Полноту, которой PR-фильтры не добирают, добирает
он. Красный ночной означает ровно одно: виноват один из мержей прошедшего
дня, и подозреваемые — коммиты со времени последнего зелёного ночного.

## 5. Расхождения с CLAUDE.md

Существенных расхождений нет: раздел «Экипировка» в CLAUDE.md описывает то, что
в коде. Оговорка одна — при полном инвентаре дроп не бросается вовсе, поэтому
и автонадевание при полной сумке не срабатывает (см. долг №4).

## 6. Техдолг под умения, таланты и визуальный слой

| # | Долг | Оценка | Почему |
|---|---|---|---|
| 1 | Оффлайн-агрегат усредняет темп по пулу зоны и считает смертность по средней потере HP; в бою запас HP переносится между схватками. Расхождение ~6% за час. | Терпит | Тест на 8% ловит уезд формулы; точнее — только проигрывание тиков |
| 2 | `rng` создаётся в сторе один раз, но позиция потока не сохраняется; реплеи «с середины» невозможны. | Терпит | Для сид-ранов достаточно текущего |
| 3 | ~~`estimateCombatRate` оценивает удары на моба без критов~~ — **закрыто**: крит входит в поток ударов (`hitStream`), девять замеров уровень × крит сходятся с прогоном в 10 % (`balance.test.ts`). Стало возможным после того, как метрика сравнения стала непрерывной (`farmCycle` без floor/ceil, `rate-continuity.test.ts`). | Закрыто | — |
| 4 | Полный инвентарь глушит дроп целиком: с 12/12 герой перестаёт находить и надевать вещи, пока не продаст. Видно в golden — статы упираются в потолок к 3000-му шагу. | **Мешает** | Лечится либо автопродажей худшего, либо надеванием мимо сумки; и то и другое — отдельная фича |
| 5 | Оффлайн-агрегат не учитывает лут: за 8 ч герой возвращается с золотом и опытом, но без новых вещей. | Терпит | Осознанное упрощение; лут в оффлайне — отдельная фича |

Закрыто ранее: монолитный tick → конвейер; лог строками → события; баланс →
`data/balance.ts`; типы-фантомы; взаимный импорт tick↔loot; дубли цветов;
оффлайн-формула, дублировавшая бой → `estimateCombatRate`; накопительная
мутация урона → конвейер статов; экипировка поверх конвейера (оружие задаёт базу
боя модификаторами `kind: 'base'`, прогресс замаха в долях); **захардкоженный
моб → зоны как данные** — спавн стал функцией `(zone, rng)`, `currentZoneId`
живёт в состоянии и в сейве.
